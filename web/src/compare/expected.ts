import { readdirSync } from 'node:fs'
import { repoPath } from '../test/repoPath'
import type { ExactRegionSpec } from './exactRegions'
import type { GapRegion } from './regions'

/**
 * 每条剧本在**当前这个移植进度下**应该是什么结果。
 *
 * 为什么需要这张表：Web 版还没做对话框、素材重编码还差一层，跨端比对现在
 * 必然红。
 * 一条"现在肯定红"的流水线不会有人看，第二天就变成噪声；而把阈值放松到能过，
 * 就变成了这个项目最贵的那种检查 —— 通过条件是"没找到问题"。
 *
 * 所以这里显式声明每条剧本的预期，**两头都会红**（`src/assets/knownMissing.ts`
 * 是同一个套路）：
 *
 * - `match` 的剧本出现偏离帧 → 红，报告指出第一个偏离的帧号；
 * - `gap` 的剧本一帧都不偏了 → **也红**，因为那说明缺口补上了，这张表过期了。
 *
 * 否则"缺口还在"与"表早就该改了"看起来会一模一样。
 *
 * **每笔账都有上界**（xl-l3o）。上面那两条只管"有没有偏离"，不管偏了多少 ——
 * 于是渲染整个崩掉也照样绿。实测（2026-09-06）：把基准侧 `snapshotImage()` 换成
 * 一张全黑的 1024×640，`dorm-walk` 的最差帧从 0.7401% 跳到 99.9565%，
 * `tools/compare-frames.sh` 退出 0、报"1/1 条剧本符合预期"。所以整屏表态要写
 * `maxRatio`（每帧的占比上界），分区表态要给每个缺口区写 `maxPixels`
 * （每帧的像素数上界）。
 *
 * **余量为什么是 2 倍。** 上界要挡住"什么都没画出来"，又不能让另一台机器的正常
 * 抖动变红。这条流水线上最会抖的东西是**后备字体**：两端都退到各自的后备链，
 * 同一帧的差异会跟着链子走。本仓库记录的实测（xl-9bd.17 / xl-9bd.18 的注释，
 * 数字是当时量的、不是这次）里，`dorm-intro` 最会写字的那一帧 t=800 在
 * **5.5397% → 5.6325% → 2.8482%** 之间来回，最宽的一档是 1.98 倍；这一次重量
 * 到的正是 2.8482%，与记录里改完之后的那个数一致。所以取 **2 倍**：它盖得住
 * "这台机器挑了另一条后备链"，而盖不住"这一帧没画出来"（全黑 ≈ 100%，比任何
 * 一条剧本的上界都高出一截）。
 *
 * 重编码那几条（大地图的有损 WebP）其实比字形稳得多 —— 两端的解码器都是定死的
 * —— 2 倍对它们是宽的；这里仍然用同一条规则，宁可规则统一、余量说得清，也不要
 * 每条剧本各拍一个数。
 */
export interface Expectation {
  /**
   * - `match` —— 两端逐像素相等。
   * - `gap`   —— **比过了**，剩一笔说得清、有上界的账。
   * - `unassembled` —— **一帧都还没比过**：取图页装配不出这个驱动器，整条
   *   流水线在这条剧本上非零退出（见 `replay/drivers.ts`）。它不是 `gap`：
   *   `gap` 的每一个数都该是量出来的，而这种剧本一个像素都没画过，写任何
   *   上界都是编的。web 侧把那个面板做出来之后，这一条要么变 `match`、
   *   要么带上真量出来的 `maxRatio` / `gaps` 变成 `gap`。
   *
   * 这个取值是 xl-1vu.4 与 xl-1vu.5 合流时定的：两张票各自都写了
   * `status: 'gap'` 且刻意不写上界（理由都对——没量过），而 xl-l3o 同时立了
   * 「每条 gap 都必须有上界」。三者都对，说明缺的是一个状态，不是一个数。
   *
   * - `unpainted` —— **驱动器装得出，可这条剧本会走进一层还没实现的绘制**，
   *   `battleDrawList` 当场抛（xl-rh9.11）。同样一帧都没比过，所以同样不许带
   *   任何量出来的数。
   *
   *   为什么它不能并进 `unassembled`：那一个说的是「整个面板还没做」，由
   *   `replay/implemented.ts` 那份**驱动器**名单裁决，而这一个是同一个面板里
   *   某几层还没画。两者归的票不是一张，而且 `unassembled.ts` 的双向对撞是
   *   拿驱动器名单撞的 —— 把 `battle` 从那份名单里摘掉，五条已经在比的战斗
   *   剧本会一起哑掉。
   *
   *   它也不是一个逃生舱：`drawList.test.ts` 里那条「unpainted 的剧本必须真的
   *   抛，而且抛的那句话点的正是它挂的那张票」会把它撞回来。哪天那几层画出来
   *   了，那条判据先红。
   */
  readonly status: 'match' | 'gap' | 'unassembled' | 'unpainted'
  /** `gap` / `unassembled` / `unpainted` 必须说清楚差在哪／为什么比不了、归哪张票。`match` 不写。 */
  readonly why?: string
  readonly issue?: string
  /**
   * **幅度上界**（xl-l3o）：每一帧的差异占比都必须 ≤ 这个数。
   *
   * 整屏表态的 `gap` 必须写它（写了 `gaps` 的分区表态不用 —— 那边的上界逐区
   * 挂在 `GapRegion.maxPixels` 上，且缺口区之外一个像素都不许差）。没有上界
   * 时通过条件只剩一句"有偏离帧"，于是**渲染整个崩掉也照样绿**：实测把基准侧
   * `snapshotImage()` 换成全黑图，最差帧从 0.7401% 跳到 99.9565%，流水线退出 0。
   *
   * 取值一律**实测**：跑一遍 `tools/compare-frames.sh`，读报告里那条剧本的
   * 最差帧占比，再按下面每条剧本注释里写明的余量放大。判据与"为什么是每帧
   * 不是所有帧之和"在 `verdict.ts`。
   */
  readonly maxRatio?: number
  /**
   * **分区表态**（xl-1vu.3）。写了它，这条剧本就换一套更硬的判据（判据本身在
   * `regions.ts`，这里只放表）：
   *
   * - 列出来的矩形是**缺口区**，照旧记账，且**每个区各自双向红** —— 某个区
   *   一帧都不差了，就是那笔账还完了而表没改；
   * - 矩形**之外**全是**硬比区**，每一个像素都必须在容差内相等。不是"超过
   *   阈值才算偏"，是**一个都不许有**。
   *
   * 为什么值得多写这几行：不写分区时，"这条剧本红着"这件事本身没有分辨力 ——
   * 缺口挪到了别的地方、多长出一块新的偏离，`status: gap` 照样通过。写了分区，
   * 偏离多出一个像素落在没人声明过的地方就会红。
   *
   * 缺口区**不许互相重叠**，也不许把整屏铺满（那就该老老实实写 `status: match`）。
   */
  readonly gaps?: readonly GapRegion[]
  /**
   * **逐像素相等区**（xl-aq0）。矩形不写在这里，**每一帧从行为真值现读**
   * （`exactRegions.ts` 说了算），区里一个超容差的像素都不许有。
   *
   * 它与上面的 `gaps` 是两套东西，可以同时挂在一条剧本上：`gaps` 是**整屏
   * 切分**（列出来的是缺口，其余全硬比），而放提示图的那几条战斗剧本还欠着
   * 满屏的背景动画重编码（xl-7ip），切不成矩形；这一套反过来，**只声明极小的
   * 几块「这里必须逐像素相等」**，屏幕其余部分照旧由 `maxRatio` 兜着。
   *
   * 为什么要它：`maxRatio` 挡不住小面积的回归。提示图的缩放采样（xl-ttu）改回
   * GPU 那一版会让 111 个像素重新差起来，而 111/655360 = 0.017%，远在上界之下
   * —— 全套检查一条都不红。见 `exactRegions.ts` 的文件头。
   */
  readonly exact?: readonly ExactRegionSpec[]
}

/** 逻辑画布的尺寸。缺口区的矩形必须落在里面。 */
export const CANVAS_WIDTH = 1024
export const CANVAS_HEIGHT = 640

export const EXPECTED: Readonly<Record<string, Expectation>> = {
  'battle-min': {
    status: 'gap',
    // **第一条进跨端逐帧比对的战斗剧本**（xl-rh9.9）。原版 `BattlePanel.paint()`
    // 那 25 层照序画出来之后，实测（`tools/compare-frames.sh battle-min`，
    // `--every 25`，17 帧，容差 8）：
    //
    //   硬比区 17 帧**逐像素相等**
    //   panel-text-zhang  合计 29697、最差 1825
    //   panel-text-yu     合计 28971、最差 1829
    //   panel-text-lu     合计 26895、最差 1696
    //
    // 也就是说：背景图、开场云雾、行动条与七颗小头像、三个人的走图、三只怪的
    // 走图、四颗指令按钮（连**哪一颗亮着**）、指示图、技能动画的每一帧、被击
    // 动画、飘起来的伤害数字、怒气槽、血条与灵力条、游标 —— 全部逐像素相等。
    // 剩下的差异**只有状态栏那九行字**。
    //
    // 差异的分布是量出来的，不是看出来的：17 帧合起来，超容差的像素落在
    // 六段列区间与三段行区间上，六段列区间恰好是三个人各自的两块
    // （`当前等级 :N` 一块、`hp/hpMax` 与 `mp/mpMax` 一块），三段行区间恰好是
    // 三行字的高度：
    //
    //   列 92–168 / 240–327（张） 414–490 / 562–649（文） 736–812 / 884–951（陆）
    //   行 547–561（等级）  578–592（血）  598–612（灵力）
    //
    // 下面三个缺口区就是按这个分布画的（每人一块，把那两段列区间与三段行区间
    // 一起裹住），**区外一个像素都不许差**。
    //
    // 这笔账是什么：原版 `BattlePanel` 用的字体是 `文鼎粗钢笔行楷`
    // （`new Font("文鼎粗钢笔行楷", Font.BOLD, 15)`），那款字没有随游戏交付，
    // 两端各自退到本机的后备链，同样的基线坐标画出来的笔画不同。与 dorm-intro /
    // dorm-walk 剩下的那一笔是**同一笔账**（xl-9bd.17 已裁定不打包字体），
    // 只是换了个面板。位置是准的：三行字的行区间两端逐像素对齐，说明基线算对了。
    why: '状态栏那九行字的字形（原版 文鼎粗钢笔行楷 未交付，两端各退到自己的后备链）',
    issue: 'xl-9bd.17',
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 1825 个（2026-09-07，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 3650,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-yu',
        // 实测最差帧 1829 个，上界取 2 倍。
        maxPixels: 3658,
        rect: { x0: 410, y0: 545, x1: 652, y1: 614 },
        why: '第二格（文敏）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-lu',
        // 实测最差帧 1696 个，上界取 2 倍。
        maxPixels: 3392,
        rect: { x0: 732, y0: 545, x1: 974, y1: 614 },
        why: '第三格（陆雪琪）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
  },
  'battle-menus': {
    status: 'gap',
    // 点得下去的「技」与「物」那一条（xl-rh9.11，461 步）。xl-rh9.12 把药品
    // 菜单 / 技能菜单 / 提示图 / 敌我两层战斗状态图标画出来之后，它从
    // `unpainted` 换成了这条**真量出来的** gap。
    //
    // 实测（`tools/compare-frames.sh battle-menus`，`--every 25`，19 帧，
    // 容差 8）。逐帧的账，以及"状态栏那块之外还差多少"这一列 —— 后者是把
    // battle-min 那三个文字缺口区（88..974 × 545..614）扣掉再数的：
    //
    //   t=0                 0.3925%   状态栏之外 **0** 个
    //   t=25/50/75/175/250/275 0.72..0.82%  状态栏之外 **0** 个
    //   t=100/150           0.86/0.98% 之外 313/1110 —— 六行存货数字，每行 53 个
    //   t=125               **0.9805%** 之外 1110 = 药品介绍文字 845 + 存货 265
    //                                                              ← 最差帧
    //   t=200/225           0.8105/0.8226%  之外 1/80   追星破月 第 2/27 帧
    //   t=300/325/350       0.8234/0.8223/0.8223%  之外 12/5/5  浪里寻花 第 3/28/53 帧
    //   t=375..450          0.80..0.82%  状态栏之外 **0** 个
    //
    // ⚠️ **t=200/225 那一行是 xl-9do 之后 2026-09-07 又重量了一次**：追星破月
    //   那 41 张源是 1240×744，被 xl-9do 裁到画布，区外从 13/62 变成 11/104
    //   （同一台机器上先后两次实测，裁之前那一遍读到的正是 13/62，与 xl-7ip
    //   记的逐字相同）。**最差帧仍是 #125、仍是 0.9975%、上界不动** —— #125 是
    //   药品菜单那一帧，`anim.bgDrawn=false`，跟背景动画毫无关系。
    //   这条剧本另外三帧的背景动画是 浪里寻花，源 1066×639、没被裁，58/11/5
    //   三个数一个没动。
    //
    //   **t=225 那一帧是变差的：62 → 104。** 按票面原本的口径（「区外像素数逐个
    //   相等，数变了就说明裁到肉了」）它就是裁到肉，所以这里明写，不让它混在
    //   「重量」两个字里被读成中性。为什么仍然接受：那条口径本身是**做不到**的
    //   （有损编码器的量化参数是对整张图算的，输入变小必然重掷骰子，见
    //   `bake.ts` 的 `backgroundAnimCrop` 头注），而 1240×744 那一档**整体是
    //   赚的** —— 115 张相对源 JPEG 的超容差像素合计 49537 → 24680，减半；
    //   这条剧本上 t=200 也从 13 降到 11。上界一条没动。2026-09-07 用户裁定。
    //
    //   ⚠️ **这五个数没有任何门禁在守**：流水线只核 `maxRatio`（整屏占比上界），
    //   而这一趟五个数都动了、四条上界一条没动。要发现这一层的变化，只能像本票
    //   这样把两端的 PNG 逐帧拉出来按状态栏三块矩形扣一遍再数。xl-7ip 那趟也是
    //   人工数的。把它做成判据是另一张票的事，这里只把「它现在是人工的」写明白。
    //
    // ⚠️ **xl-x6w 之后 2026-09-08 又重量了一次**（`-sns 0` + 越界就裁，见
    //   `bake.ts` 的 `BATTLE_LOSSY_SNS`）：放背景动画那五帧的区外从 11/104/58/11/5
    //   变成 **9/80/67/5/5**，五帧合计 189 → 166。**t=100/125/150 那三个数
    //   （313/1221/1110）一个都没动** —— 那三帧是药品菜单，`anim.bgDrawn=false`，
    //   一笔背景动画的账都没有。**这本身是这一趟改的是编码而不是渲染的现成判据**：
    //   动的恰好是放背景动画的那五帧，不动的恰好是不放的那三帧。
    //   **最差帧仍是 #125、仍是 0.9975%、上界仍是 1.9949%，一个数没动。**
    //
    // ⚠️ **xl-ttu 之后 2026-09-08 又重量了一次，提示图那笔账还清了。** 缩放采样
    //   改成按原版的定点规律在 CPU 上算（`battle/render/scaledBlit.ts`），三个
    //   放提示图的帧区外变了：**t=125 从 1221 掉到 1110、t=200 从 9 掉到 1、
    //   t=300 从 67 掉到 12**；不放提示图的那六个数（313 / 1110 / 80 / 5 / 5 与
    //   四个 0）一个没动。**最差帧仍是 #125，但它从 0.9975% 掉到 0.9805%，
    //   上界跟着从 1.9949% 收到 1.9610%**（仍是实测最差帧的 2 倍）。
    //
    //   提示图那三块矩形现在**逐像素相等**：t=125 的 80×16 是 0/1280、
    //   t=200 的 20×4 是 0/80、t=300 的 30×6 是 0/180（矩形从真值的
    //   `reminder.dx1/dy1/dx2/dy2` 读，不是量的）。t=200 剩下的那 1 个在
    //   (782,93)、d=9，是技能动画的有损重编码，离提示图那块 270 个像素远。
    //
    //   **改前那五个数是这一趟自己跑出来的，不是抄上一行的**：把渲染器换回
    //   改前那一版（`git show HEAD~1:…/battleRenderer.ts`）重跑一遍，读到的正是
    //   313/1221/1110 与 9/80/67/5/5 —— 与上面 xl-x6w 那段记的改前读数逐字
    //   相同（那八个数现在只在那一段里留着，逐帧表已换成改后的）。顺带证明了
    //   一件事：
    //   **这一趟的改动没有任何单元测试在守** —— 渲染器换回去，`pnpm test` 全绿。
    //
    // ⚠️ **最后四行是 xl-7ip 之后 2026-09-07 重量的，最差帧换人了。** 重量前
    // 那五帧是 0.9352 / **4.2029** / 2.78 / 3.92 / 2.54%，#225 的区外是满屏；
    // 换成 `cwebp -q 95` 之后它们的区外掉到 13 / 62 / 58 / 11 / 5 个，最差帧
    // 从 #225 变成 #125 —— 而 #125 是药品菜单那一帧，`anim.bgDrawn` 是 false，
    // 它的 1221 个区外像素与背景动画毫无关系，重量前后一个都没变。
    // **也就是说这条剧本的上界现在由字形那一笔（xl-9bd.17）定着。**
    //
    // 三件事由这张表钉住，都是这一票的正面判据：
    //
    // 1. **t=175 与 t=275 状态栏之外差 0 个像素** —— 这两帧技能菜单开着
    //    （分别是文敏那组三颗与张小凡那组两颗），背板、每颗按钮的贴图、
    //    返回按钮、技能说明图**逐像素相等**。t=275 那一帧说明图挂的还是文敏
    //    上一轮的（`isDrawIntro` 原版从不清），这个原版缺陷也是逐像素对上的。
    // 2. **t=375..450 状态栏之外差 0 个像素**，而那几帧怪物身上挂着「体力下降」
    //    ——怪物那层状态图标逐像素相等；我方那层由 t=250/275 同理钉住。
    // 3. 药品菜单那几帧之外的差异**只剩字**：六行存货数字每行恒 53 个、
    //    介绍文字 845 个，背板与七颗按钮贴图、金创药那张介绍图都不差。
    //
    // 剩下的两笔账：
    //
    // - **背景动画的有损重编码**（xl-7ip，已还过一轮）。`image/背景动画/` 那
    //   753 张源文件是 JPG，烘焙器对非 PNG 走 `cwebp -q`，于是满屏差一点点。
    //   五条老剧本一次技能都没用过，所以这笔账到 xl-rh9.12 才露面，当时它是
    //   最差帧 #225 的唯一成因（27544 个像素里状态栏那块只占 5000 上下）。
    //   xl-7ip 把战斗素材那条路的档位从 q80 提到 q95（`bake.ts` 的
    //   `BATTLE_LOSSY_QUALITY`，六个档位的全量代价实测在那条常量的头注里），
    //   这一笔从「满屏」缩到「五帧合计 149 个像素」，最差帧因此换成了 #125。
    //   **没有还清**：归零只有 `-lossless` 一条路，要给仓库永久加 89 MiB、
    //   单个技能的按需下载从 2.9 M 涨到 16.3 M，2026-09-07 裁定不换。
    //
    // **提示图缩放那一笔（xl-ttu）已经还清了**，所以它不在上面那个列表里。它原先是
    // 111 个像素（只在 t=125）：提示图是战斗里唯一真的在缩放的一层（源 128×24 →
    // 目标 80×16，k=1.6），差异图上一排**周期 5** 的竖条，正是 `1.6i+0.8` 落在
    // 整数上的那几列。它不是采样网格整体错位（那两种平移 xl-rh9.12 都实测过，
    // 各自把这一块从 111 推到 800 以上），而是两端在纹素边界上打平时的取整不同。
    // 现在按原版的定点规律在 CPU 上算，那三块矩形逐像素相等。
    //
    // ⚠️ **顺带纠正上面两段对背景动画那笔账的高估。** xl-x6w 段说「放背景动画
    // 那五帧的区外……五帧合计 189 → 166」、xl-7ip 段说「缩到五帧合计 149 个
    // 像素」，两句都把那五帧的区外**整个**算作背景动画的账。这一趟证明不是：
    // 其中 **63 个是提示图**（t=200 的 8 个 + t=300 的 55 个），背景动画在那五帧
    // 上的真实账是 **103**。那两段的读数没错，错的是归属，所以只在这里更正，
    // 不去改它们记下的当时读数。
    //
    // 上界按仓库规矩取最差帧的 2 倍：0.9805%（6426/655360）→ 1.9610%。
    maxRatio: 0.019610,
    why:
      '状态栏那九行字 + 药品菜单的存货与介绍文字（同一笔字形账，这条上界现在由它定）；' +
      '背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0）',
    // **两笔账两张票**：字形归 xl-9bd.17（已裁定不打包字体），背景动画的有损
    // 重编码归 xl-7ip。提示图那张（xl-ttu）已经关了，所以不再列在这里 ——
    // 它的账在上面的逐帧表里留了痕。上界现在由字形那笔定着。
    issue: 'xl-9bd.17 / xl-7ip',
    // **逐像素相等区**（xl-aq0）：提示图那一层的目标矩形，每一帧从真值的
    // `reminder.dx1..dy2` 现读，**区里一个超容差的像素都不许有**。守的是
    // xl-ttu 那个改动 —— 缩放采样按原版的定点规律在 CPU 上做，而不是交给 GPU
    // 的最近邻。把渲染器换回改前那一版，`pnpm test` 全绿、整屏的 maxRatio 也
    // 照样过（111 个像素只占 0.017%），只有这条会红。理由与另外两条被否掉的
    // 路见 `exactRegions.ts` 的文件头。
    //
    // 实测（`tools/compare-frames.sh battle-menus`，`--every 25`，容差 8）：
    // 采样到的 19 帧里 **3 帧**画着提示图，t=125 的 80×16 是 0/1280、
    // t=200 的 20×4 是 0/80、t=300 的 30×6 是 0/180 —— 与 xl-ttu 记在上面那段
    // 里的读数逐字相同（那一趟是人工数的，这一趟是流水线数的）。
    exact: [
      {
        name: 'reminder',
        source: 'reminder',
        // 整条真值里提示图画着且矩形非空的拍数，`--every` 调成什么都不影响它。
        drawnTicks: 63,
        why: '提示图的缩放采样按原版的定点规律在 CPU 上做（scaledBlit.ts），不走 GPU 的最近邻',
        issue: 'xl-ttu / xl-aq0',
      },
    ],
  },
  // ===== xl-rh9.14 的六条：剩下那些技能与秘术 =====
  //
  // 六条原先都是 `unpainted`（技能菜单与状态图标归 xl-rh9.12、小精灵归
  // xl-rh9.15）。xl-rh9.18 把 xl-rh9.12 合进来之后**五条比得成了**，
  // xl-rh9.15 把第 11 层小精灵画出来之后**第六条也比得成了** —— 六条各自换成
  // 下面这些**真量出来的**表态，这一批里再没有 `unpainted`。
  //
  // 五条的账合起来只有两笔，逐帧数出来的（`tools/compare-frames.sh`，
  // `--every 25`，容差 8）：
  //
  // 1. **状态栏那几行字**，与 battle-min / battle-menus 同一笔（xl-9bd.17）。
  // 2. **背景动画的有损重编码**（xl-7ip）。这一笔只在**背景动画正在放**的那些
  //    帧上出现，而且是满屏的。
  //
  // 第 2 笔与"哪几帧"的对应关系是核过的，不是看出来的：三条技能剧本一共 111
  // 个取样帧，其中**状态栏之外还有差异的恰好 29 帧**（xl-x6w 之后 2026-09-08
  // 重量；xl-7ip 那一趟是 29 帧，xl-9do 那一趟 28 帧），这 29 帧的真值里
  // `anim.bgDrawn` **全是 true**（逐帧从 `tools/traces/out/*.trace.json` 现读核
  // 的，不是看出来的）。反过来有**一个**例外 —— `battle-lu-skills` 的 t=450
  // （`劈风追月` 第 88 帧）：背景动画在放，可状态栏之外一个像素都不差。所以这条
  // 对应是**单向**的：区外有差异 ⇒ 那一拍背景动画在放。
  //
  // ⚠️ 例外的个数在这三张票里来回过一趟，值得记一笔：xl-9do 把伏虎冲天那 74 张
  // （1240×744）裁到画布，`battle-yu-skills` 的 t=75 区外从 3 个降到 **0**，成了
  // 第二个例外；xl-x6w 加 `-sns 0` 之后它又回到 **2** 个，于是掉回名单里、例外
  // 回到一个。**同一件事读三遍读出三个数，那正是「区外像素数是编码抽签的结果」
  // 这句话的现场证据** —— 例外的个数不是一条性质，是一个读数。
  //
  // ⚠️ **xl-7ip 之后（2026-09-07 重量），第 2 笔不再定这三条的上界了。**
  // 背景动画那 753 张 JPG 从 `cwebp -q 80` 换到 `-q 95`（`scripts/bake.ts` 的
  // `BATTLE_LOSSY_QUALITY`，档位取舍的全量实测在那条常量的头注里），区外的
  // 像素数缩小了 30~60 倍：三条剧本最差帧的区外从 30118 / 51884 / 50953 降到
  // 220 / 1000 / 548。**上界现在由第 1 笔（字形）定着**，也就是 xl-9bd.17 那张
  // 票——这正是 xl-7ip 要的结果，因为字形那一笔是有人认领的，而素材编码那一笔
  // 跟"这条剧本画对了没有"毫无关系。
  //
  // xl-7ip 那一趟**那 29 帧的名单一帧没变**（幅度掉了两个数量级而落点分毫未动，
  // 那本身就是"改的是编码、不是渲染"的现成判据）。xl-9do 这一趟名单**动了一帧**
  // ——`battle-yu-skills` 的 t=75 掉出去了 —— 而这一次动是**对的**：xl-7ip 改的是
  // 全部 753 张的档位，落点本就不该变；xl-9do 只裁 1240×744 那 115 张（伏虎冲天
  // 74 张 + 追星破月 41 张），受影响的帧必须、也只该是放这两个动画的那几帧。
  // 逐帧核过：变的恰好是 yu 的 t=50/75/100（伏虎冲天，153/3/6 → 148/0/2）与
  // menus 的 t=200/225（追星破月，13/62 → 11/104），**其余 24 帧一个数都没动**。
  //
  // ⚠️ **xl-x6w 这一趟（2026-09-08）与前两趟都不一样：名单没动，但区外的数几乎
  // 全动了，而且有两帧大幅变差。** 战斗有损那条路加了 `-sns 0`（关掉 cwebp 对
  // 整张图的分段分析），同时把 xl-9do 那条面积门槛删掉、改回「越界就裁」——
  // 753 张全裁。`-sns 0` 是逐张抽签：753 张源图里 **220 张变差、430 张变好、
  // 103 张持平**，合计偏离 153588 → 142986（−6.9%）、体积 32.70 → 31.93 MiB。
  // 全量实测见 `scripts/bake.ts` 的 `BATTLE_LOSSY_SNS` 头注。
  //
  // 落到这三条剧本上，**合计不是全赢**：区外合计 zhang 953 → 1307、
  // yu 1282 → 2522（都变差），lu 1403 → 1284（变好）。两个坏运气都指名道姓：
  // `龙翔九天/15`（16 → 671，顶起 zhang 的新最差帧 #350）与 `苍龙盖天/22`
  // （30 → 1511，顶起 yu 的新最差帧 #175）。两张都是 1066×639，**不是裁出来的**
  // ——保留门槛只裁 1240×744 的话它们是 672 / 1451，一样坏。是 `-sns 0` 本身在
  // 这两张图上掷坏了。
  //
  // **上界因此一条都没有放宽**（xl-x6w 票面的验收标准）：zhang 与 yu 的最差帧
  // 变差了，按仓库「最差帧 ×2」的规矩算出来会是 0.7178% / 1.4316%，比现在的
  // 0.6665% / 1.3480% **松**，所以不改 —— 它们现在的余量是 1.86× / 1.88×，
  // 不是 2×，那是有意的。lu 的最差帧变好了，上界跟着**收紧**。
  //
  // 两条秘术剧本一帧都没有第 2 笔（它们不放背景动画 —— `heroMishu` 一句
  // `backgroundAnimation.set` 都没有），于是它们能写**分区表态**：状态栏那一块
  // 之外**一个像素都不许差**。
  'battle-zhang-skills': {
    status: 'gap',
    // 张小凡的五招（xl-rh9.14，500 步）。实测 20 帧，**xl-x6w 之后 2026-09-08
    // 重量**（q80 那一列是 xl-7ip 之前的，留着看这一笔总共缩了多少）：
    //
    //   t=0/25/50/100..150/225..325/400/425   状态栏之外 **0** 个
    //          整屏(今天)  区外(今天)  区外(xl-x6w 前)  区外(q80)
    //   t=75    0.3275%       184          220           30118  横剑摆渡 第 14 帧
    //   t=175   0.3020%        75           82           20897  银鹰掠地 第 14 帧
    //   t=200   0.2945%         4            8           13762  银鹰掠地 第 39 帧
    //   t=350   0.3589%       524           80            8646  龙翔九天 第 15 帧 ← 最差帧
    //   t=375   0.2707%         1            4            3852  龙翔九天 第 40 帧
    //   t=450   0.3252%       459          469           19300  神剑傲州 第 6 帧
    //   t=475   0.2650%        60           90           28711  神剑傲州 第 31 帧
    //
    // **最差帧从 #75 换成了 #350，而且是变差换过去的**（0.3333% → 0.3589%）：
    // 七帧里六帧都变好了，只有 t=350 从 80 涨到 524 —— `龙翔九天/15` 这一张源图
    // 相对源 JPEG 的超容差像素从 16 涨到 671，`-sns 0` 在它身上掷坏了。**上界
    // 不跟着放宽**（见上面那段的理由），仍是 0.6665%，余量 1.86×。
    //
    // 也就是说：**不放背景动画的每一帧，状态栏之外逐像素相等** —— 技能菜单的
    // 背板与按钮、技能说明图、五套技能动画的每一帧、被击动画、伤害数字，全对。
    //
    // 最差帧仍**不是被背景动画顶上去的**：#350 那一帧超容差的 2352 个像素里，
    // 区外占 524 个，其余 1828 个是状态栏那几行字 —— 字形那一笔仍是大头。
    //
    // ⚠️ **xl-9do 之后重量过，这条剧本上面七个数一个都没动。** 这不是"没查"，
    // 是查过之后的结论，而且它本身是那张票的一条判据：xl-9do 只裁 1240×744
    // 那一档（伏虎冲天 74 张 + 追星破月 41 张），而这条剧本的五套背景动画
    // 横剑摆渡 / 银鹰掠地 / 龙翔九天 / 神剑傲州 全是 1066×639，一张都不该被裁。
    // **它还是那张票收窄范围的直接原因**：先前有一版把 753 张全裁了，1066×639
    // 那档重掷量化骰子掷坏了 —— `神剑傲州/31` 相对源 JPEG 的超容差像素从 107
    // 涨到 18139，这条剧本的最差帧从 #475 0.2693% 顶到 **2.5040%**（超上界
    // 3.76 倍，`tools/compare-frames.sh` 退出 1）。也就是说这条上界**真的拦下过
    // 一次素材层面的回归**，它不是装饰。
    //
    // 上界仍是 xl-7ip 那一趟按「最差帧 ×2」定的 0.3333%（2184/655360）→ 0.6665%。
    // xl-x6w 之后最差帧是 0.3589%（2352/655360），照规矩算会是 0.7178% —— **更松，
    // 所以不采纳**（xl-x6w 票面：上界只许变紧不许放宽）。
    maxRatio: 0.006665,
    why: '状态栏那几行字（字形，这条上界现在由它定）；背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0），只在放背景动画的 7 帧上、七帧合计 1307 个像素',
    issue: 'xl-9bd.17 / xl-7ip / xl-x6w',
    // **逐像素相等区**（xl-aq0）：提示图那一层的目标矩形，每一帧从真值的
    // `reminder.dx1..dy2` 现读，**区里一个超容差的像素都不许有**。守的是
    // xl-ttu 那个改动 —— 缩放采样按原版的定点规律在 CPU 上做，而不是交给 GPU
    // 的最近邻。把渲染器换回改前那一版，`pnpm test` 全绿、整屏的 maxRatio 也
    // 照样过（111 个像素只占 0.017%），只有这条会红。理由与另外两条被否掉的
    // 路见 `exactRegions.ts` 的文件头。
    //
    // 实测（`--every 25`，容差 8）：20 帧里 **4 帧**画着提示图，
    // t=75 / t=175 / t=350 各是 120×24 的 0/2880、t=450 是 60×12 的 0/720。
    // 前三帧背景动画正放着（横剑摆渡 / 银鹰掠地 / 龙翔九天），那笔有损重编码的
    // 账在区外照旧记着，**区内一个像素都不差** —— 这条不会被 xl-7ip 那笔误伤。
    exact: [
      {
        name: 'reminder',
        source: 'reminder',
        drawnTicks: 84,
        why: '提示图的缩放采样按原版的定点规律在 CPU 上做（scaledBlit.ts），不走 GPU 的最近邻',
        issue: 'xl-ttu / xl-aq0',
      },
    ],
  },
  'battle-yu-skills': {
    status: 'gap',
    // 文敏的五招（xl-rh9.14，1175 步）。实测 47 帧，状态栏之外有差异的 8 帧
    // 全是背景动画在放。**xl-x6w 之后 2026-09-08 重量**：
    //
    //          整屏占比(今天)  区外(今天)  区外(xl-x6w 前)  整屏占比(q80，xl-7ip 前)
    //   t=50      0.5556%        142           148           3.96%   伏虎冲天 第 11 帧
    //   t=75      0.5344%          2             0           2.86%   伏虎冲天 第 36 帧
    //   t=100     0.5344%          4             2           5.17%   伏虎冲天 第 61 帧
    //   t=175     0.7158%       1286            24           3.54%   苍龙盖天 第 22 帧 ← 最差帧
    //   t=200     0.5205%         10             4           1.68%   苍龙盖天 第 47 帧
    //   t=800     0.5310%         67            65           1.31%   妙手回春 第 17 帧
    //   t=850     0.5157%         37            39           1.03%   蝶影神灵 第 4 帧
    //   t=875     0.6697%        974          1000           6.5454% 蝶影神灵 第 29 帧
    //   其余 39 帧 0.31..0.53%     0             0            同左    状态栏之外 **0** 个
    //
    // **最差帧从 #875 换成了 #175，而且是变差换过去的**（0.6740% → 0.7158%）：
    // `苍龙盖天/22` 这一张源图相对源 JPEG 的超容差像素从 30 涨到 1511，`-sns 0`
    // 在它身上掷坏了（1066×639，不是裁出来的：保留门槛也是 1451）。**上界不跟着
    // 放宽**，仍是 1.3480%，余量 1.88×。
    //
    // **t=75 回到了「区外有差异」那份名单**（xl-9do 让它掉出去过），这条剧本从
    // 7 帧回到 8 帧、合计从 1282 涨到 2522 —— 这 +1240 是这么来的：t=175 一帧
    // 就涨了 +1262，其余七帧加起来净减 22。
    //
    // 最右那一列是 xl-rh9.18 当时记下的整屏占比，**不是区外像素数** —— 那一趟
    // 只留了占比。区外像素数只有 q95 这一列是量出来的。
    //
    // 妙手回春那一帧值得单说：文敏那一招一发伤害都不打，只给全员回血并复活，
    // 而这一帧状态栏之外也只有背景动画那一笔。
    //
    // 新的最差帧 t=175：那一帧超容差的 4691 个像素里，区外 1286 个、状态栏那几行
    // 字 3405 个 —— **上界仍由字形定着**，素材编码那一笔虽然涨了，还没夺回来。
    //
    // 上界仍是 xl-7ip 那一趟按「最差帧 ×2」定的 0.6740%（4417/655360）→ 1.3480%。
    // xl-x6w 之后最差帧是 0.7158%（4691/655360），照规矩算会是 1.4316% —— **更松，
    // 所以不采纳**（xl-x6w 票面：上界只许变紧不许放宽）。
    maxRatio: 0.013480,
    why: '状态栏那几行字（字形，这条上界现在由它定）；背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0），只在放背景动画的 8 帧上、八帧合计 2522 个像素',
    issue: 'xl-9bd.17 / xl-7ip / xl-x6w',
    // **逐像素相等区**（xl-aq0）：提示图那一层的目标矩形，每一帧从真值的
    // `reminder.dx1..dy2` 现读，**区里一个超容差的像素都不许有**。守的是
    // xl-ttu 那个改动 —— 缩放采样按原版的定点规律在 CPU 上做，而不是交给 GPU
    // 的最近邻。把渲染器换回改前那一版，`pnpm test` 全绿、整屏的 maxRatio 也
    // 照样过（111 个像素只占 0.017%），只有这条会红。理由与另外两条被否掉的
    // 路见 `exactRegions.ts` 的文件头。
    //
    // 实测（`--every 25`，容差 8）：47 帧里 **3 帧**画着提示图，
    // t=50 是 110×22 的 0/2420、t=800 是 120×24 的 0/2880、t=850 是 40×8 的 0/320。
    // 三帧背景动画都放着（伏虎冲天 / 妙手回春 / 蝶影神灵），区内照样一个不差。
    exact: [
      {
        name: 'reminder',
        source: 'reminder',
        drawnTicks: 84,
        why: '提示图的缩放采样按原版的定点规律在 CPU 上做（scaledBlit.ts），不走 GPU 的最近邻',
        issue: 'xl-ttu / xl-aq0',
      },
    ],
  },
  'battle-lu-skills': {
    status: 'gap',
    // 陆雪琪的五招（xl-rh9.14，1100 步）。实测 44 帧，状态栏之外有差异的 14 帧
    // 全是背景动画在放。**xl-x6w 之后 2026-09-08 重量**：
    //
    //          整屏占比(今天)  区外(今天)  区外(xl-x6w 前)
    //   t=50      0.6290%        415          427   灵凤吐珠 第 17 帧 ← 最差帧
    //   t=100     0.5838%         58           60   踏月无痕 第 3 帧
    //   t=125     0.5765%         12           13   踏月无痕 第 28 帧
    //   t=150     0.5739%          1            3   踏月无痕 第 53 帧
    //   t=250     0.5682%          3            4   星火乾坤圈 第 24 帧
    //   t=300     0.5684%         54           55   亟电崩离 第 2 帧
    //   t=325     0.5673%         47           49   亟电崩离 第 27 帧
    //   t=375     0.5727%        110          103   劈风追月 第 13 帧
    //   t=400     0.5524%         23           18   劈风追月 第 38 帧
    //   t=425     0.6224%        460          548   劈风追月 第 63 帧
    //   t=600     0.5614%         11           15   劈风追月 第 2 帧
    //   t=625     0.5501%         16           20   劈风追月 第 27 帧
    //   t=650     0.5603%          3            1   劈风追月 第 52 帧
    //   t=675     0.5582%         71           87   劈风追月 第 77 帧
    //   其余 30 帧 0.15..0.60%      0            0
    //
    // **这条剧本是四条里唯一全面变好的**：区外合计 1403 → 1284，最差帧从 #425
    // 换成 #50 并且**降到 0.6290%**（#425 自己从 0.6354% 降到 0.6224%，让位是因为
    // 它降得比 #50 多）。上界因此**收紧**。十四帧里十一帧变好、三帧变差
    // （t=375/400/650），最大的一笔变差是 +7 个像素。
    //
    // xl-7ip 之前（q80）这条剧本最差 **#425 6.9888%**、次差 #625 6.0075%；现在
    // 最差的 #50 那 4122 个超容差像素里区外只占 415 个，其余 3707 个是状态栏那几行
    // 字 —— **上界由字形定着**。#625 也从次差掉进了普通帧（0.5501%，区外 16）。
    //
    // ⚠️ 反过来不成立，这里有**唯一一个反例**：t=450 背景动画照放（劈风追月
    // 第 88 帧），可状态栏之外一个像素都不差。所以这条剧本的注释里那句对应
    // 只能写单向的："区外有差异 ⇒ 背景动画在放"。**xl-x6w 重量之后它还是唯一
    // 一个**（15 帧 bgDrawn=true，区外为 0 的仍只有 t=450 —— 逐帧从真值现读核的）。
    //
    // 上界按仓库规矩取最差帧的 2 倍：0.6290%（4122/655360）→ **1.2579%**
    // （xl-x6w 收紧，此前是 0.6354% → 1.2708%）。
    maxRatio: 0.012579,
    why: '状态栏那几行字（字形，这条上界现在由它定）；背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0），只在放背景动画的 14 帧上、十四帧合计 1284 个像素',
    issue: 'xl-9bd.17 / xl-7ip / xl-x6w',
    // **逐像素相等区**（xl-aq0）：提示图那一层的目标矩形，每一帧从真值的
    // `reminder.dx1..dy2` 现读，**区里一个超容差的像素都不许有**。守的是
    // xl-ttu 那个改动 —— 缩放采样按原版的定点规律在 CPU 上做，而不是交给 GPU
    // 的最近邻。把渲染器换回改前那一版，`pnpm test` 全绿、整屏的 maxRatio 也
    // 照样过（111 个像素只占 0.017%），只有这条会红。理由与另外两条被否掉的
    // 路见 `exactRegions.ts` 的文件头。
    //
    // 实测（`--every 25`，容差 8）：44 帧里 **5 帧**画着提示图，
    // t=50 与 t=375 是 120×24 的 0/2880、t=100 是 30×6 的 0/180、
    // t=300 与 t=600 是 20×4 的 0/80。
    exact: [
      {
        name: 'reminder',
        source: 'reminder',
        drawnTicks: 126,
        why: '提示图的缩放采样按原版的定点规律在 CPU 上做（scaledBlit.ts），不走 GPU 的最近邻',
        issue: 'xl-ttu / xl-aq0',
      },
    ],
  },
  'battle-mishu-zhang': {
    status: 'gap',
    // 张小凡的秘术金钟罩（xl-rh9.14，600 步）。**分区表态** —— 实测 24 帧，
    // 每一帧的每一个超容差像素都落在张小凡那一格的文字里，最差帧 2128 个；
    // 那块之外，24 帧**一个像素都不差**（末帧连那块也是 0）。
    //
    // 这条剧本只有张小凡出战，所以只有一个缺口区（另外两格根本不画）。
    // 它比整屏上界硬：金钟罩那张状态图标、秘术动画 18 帧、怒气槽清零、
    // 控制台四颗按钮的贴图，全在硬比区里逐像素相等。
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 2128 个，上界取 2 倍。
        maxPixels: 4256,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '状态栏第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩状态栏那三行字的字形；这条剧本不放背景动画，所以没有第二笔账',
    issue: 'xl-9bd.17',
  },
  // ⚠️ **上面这条是 xl-7ip 的反向判据，2026-09-07 真跑过。** 背景动画的编码从
  // q80 换到 q95 之后重跑 `tools/compare-frames.sh battle-mishu-zhang`：硬比区
  // 仍是 24 帧逐像素相等、`panel-text-zhang` 合计 44598、最差 2128/4256 ——
  // 与换编码之前 xl-rh9.18 记下的数**逐字相同**。它本就不该动（`heroMishu`
  // 一句 `backgroundAnimation.set` 都没有），所以「它动了」才是信号：那说明
  // 改到的不只是背景动画那 753 张。下面 `battle-mishu-yu` 同理。
  //
  // **xl-x6w（2026-09-08）第三次跑了同一条反向判据，仍逐字相同**：加 `-sns 0`
  // 并把 753 张全裁之后，硬比区仍是 24 帧逐像素相等、`panel-text-zhang` 合计
  // 仍是 44598、最差仍是 2128/4256。这一趟它尤其该跑：编码设置与裁剪范围**同时**
  // 动了，「只波及背景动画」这句话比 xl-7ip 那趟更需要一个反证。
  // 配套的正面读数是改动面本身 —— 那两个提交动的 753 个 `.webp` **全部**在
  // `web/public/battle-anim/背景动画/` 下，技能动画那 1017 张与其余有损路径
  // 一个字节没动（`git diff --name-only -z master...HEAD` 现数的）。
  'battle-mishu-yu': {
    status: 'gap',
    // 文敏的秘术潜能爆发（xl-rh9.14，775 步）。同样是**分区表态**：实测 31 帧，
    // 超容差像素全部落在文敏那一格的文字里，最差帧 2202 个，区外恒 0。
    //
    // 这条剧本还顺带钉住了一个原版缺陷的画面：潜能爆发退回去那一下属性折半、
    // `refreshValue()` 把 hp 夹回新上限，那几帧的血条宽度也在硬比区里。
    //
    // xl-7ip 的反向判据（2026-09-07 重跑）：换 q95 之后硬比区仍是 31 帧逐像素
    // 相等、`panel-text-yu` 合计 64463、最差 2202/4404，与换之前逐字相同。
    // **xl-x6w（2026-09-08）第三次重跑，三个数仍逐字相同**：加 `-sns 0` 且
    // 753 张全裁之后，硬比区 31 帧逐像素相等、合计 64463、最差 2202/4404。
    gaps: [
      {
        name: 'panel-text-yu',
        // 实测最差帧 2202 个，上界取 2 倍。
        maxPixels: 4404,
        rect: { x0: 410, y0: 545, x1: 652, y1: 614 },
        why: '状态栏第二格（文敏）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩状态栏那三行字的字形；这条剧本不放背景动画，所以没有第二笔账',
    issue: 'xl-9bd.17',
  },
  'battle-mishu-lu': {
    status: 'gap',
    // 陆雪琪的秘术召出小精灵那一条（xl-rh9.14，1080 步）。第 11 层由 xl-rh9.15
    // 画出来之后，它从 `unpainted` 换成了这条**真量出来的**分区表态。
    //
    // 实测（`tools/compare-frames.sh battle-mishu-lu --every 25`，44 帧，
    // 容差 8）：
    //
    //   硬比区 44 帧**逐像素相等**（三个文字缺口区之外，超容差的像素合计 **0** 个）
    //   panel-text-zhang  合计 65974、最差 1637（#325）
    //   panel-text-yu     合计 64491、最差 1637（#150）
    //   panel-text-lu     合计 87794、最差 2140（#700）
    //
    // 那个 **0** 就是这张票的判据，逐样点名它盖住了什么：
    //
    // - **小精灵本体**画在 `(pet.x, pet.y)`，而 `pet.x/y/isDraw` 三个字段
    //   **一个都不在行为真值里**（导出器的 `snapshotState` 没取 `bp.pet`）。
    //   所以位置对不对、`Pet.update()` 那个浮动的相位对不对，逐步的 `toEqual`
    //   一个字都验不到 —— 只有这 44 帧的像素验得到。**浮动一轮是 9 拍**
    //   （原版注释说的"上浮五拍、下沉五拍"是错的，来历只在 `battle/step.ts`
    //   的 `updatePet` 上写一份），而取样点是 25 的倍数 —— `gcd(25, 9) = 1`，
    //   所以召出来（第 855 拍）之后的 9 个取样点**把 9 个相位一人一次全采到
    //   了**：875/900/925/950/975/1000/1025/1050/1075 的相位依次是
    //   2/0/7/5/3/1/8/6/4，实测 y 依次是 398/400/398/(没画)/397/399/399/
    //   397/396。那个"没画"正是下一条说的出手那一段。
    // - **行动条上那一颗小头像**：真值里有 `bar.pet`（petX），可"该不该画
    //   这一颗"不在真值里。原版判的是 `bp.pet!=null` 而**不是** `pet.isDraw`
    //   —— 小精灵出手那几拍本体消失、这一颗还在，那几拍也在这 0 里。
    // - **`小精灵攻击` 那 22 帧动画**与它打出去的伤害数字。
    //
    // 这条剧本**不放背景动画**（秘术与小精灵攻击都不走 `skillAttack`，
    // `backgroundAnimation.isDraw` 全程 false），所以没有 xl-7ip 那笔账，
    // 剩下的差异只有状态栏那九行字。
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 1637 个，上界取 2 倍。
        maxPixels: 3274,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '状态栏第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-yu',
        // 实测最差帧 1637 个，上界取 2 倍。
        maxPixels: 3274,
        rect: { x0: 410, y0: 545, x1: 652, y1: 614 },
        why: '状态栏第二格（文敏）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-lu',
        // 实测最差帧 2140 个，上界取 2 倍。陆雪琪那一格最大，因为她放秘术
        // 掉灵力、字数比另外两格多变几次。
        maxPixels: 4280,
        rect: { x0: 732, y0: 545, x1: 974, y1: 614 },
        why: '状态栏第三格（陆雪琪）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩状态栏那九行字的字形；小精灵那三层（本体 / 行动条上那一颗 / 攻击动画）在硬比区里逐像素相等',
    issue: 'xl-9bd.17',
  },
  'battle-victory': {
    status: 'gap',
    // **第一条走到结算之后的剧本**（xl-rh9.13，837 步，一路记到原版自己
    // `switchTo("scene")`）。第 22 层画出来之后实测
    // （`tools/compare-frames.sh battle-victory`，`--every 25`，34 帧，容差 8）：
    //
    //   硬比区 34 帧**逐像素相等**（区外超容差的像素合计 **0** 个）
    //   panel-text-zhang   合计 65480、最差 2047
    //   panel-text-yu      合计 65727、最差 2045
    //   panel-text-lu      合计 49144、最差 1612
    //   victory-attr-nums  合计   453、最差  453
    //   victory-exp-nums   合计  2741、最差 1683
    //   victory-loot       合计  4132、最差 1033
    //
    // 也就是说：卷轴从 y=80 往下拉开的每一格、物品框从中心对开的每一格、
    // 第一页三张人物图、第二页那张（只有升了级的陆雪琪）、升级小图、
    // 「获得物品」图 —— 全部逐像素相等。剩下的差异**只有字**：状态栏那九行，
    // 加上结算画面上的三栏数字。
    //
    // 六个矩形是量出来的，不是画出来的：34 帧合起来，超容差的像素落在十二段
    // 行区间上，每一段的列区间都干净地分成几簇 ——
    //
    //   行 137–211 列 695–764              掉落物三行 + 「金钱 N」（x=690）
    //   行 177–440 列 532–580              两栏经验数字（x=532）
    //   行 368–440 列 502–521              陆雪琪那四行属性（x=502）
    //   行 547–612 列 92–178 / 240–327 …   状态栏，与 battle-min 同一块地方
    //
    // 这笔账与 battle-min / dorm-intro 是**同一笔**：原版 `文鼎粗钢笔行楷`
    // 未交付，两端各退到自己的后备链（xl-9bd.17 已裁定不打包字体）。
    // 结算画面上没有一处是「画错了」的 —— 位置全对，笔画不同。
    why: '状态栏九行字 + 结算画面三栏数字的字形（原版 文鼎粗钢笔行楷 未交付）',
    issue: 'xl-9bd.17',
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 2047 个（2026-09-07，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 4094,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-yu',
        // 实测最差帧 2045 个，上界取 2 倍。
        maxPixels: 4090,
        rect: { x0: 410, y0: 545, x1: 652, y1: 614 },
        why: '第二格（文敏）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-lu',
        // 实测最差帧 1612 个，上界取 2 倍。
        maxPixels: 3224,
        rect: { x0: 732, y0: 545, x1: 974, y1: 614 },
        why: '第三格（陆雪琪）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'victory-attr-nums',
        // 实测最差帧 453 个（合计也是 453 —— 34 帧里只有一帧采样到了第二页），
        // 上界取 2 倍。
        maxPixels: 906,
        rect: { x0: 498, y0: 364, x1: 526, y1: 444 },
        why: '第二页那四行属性数字的字形（这一场只有陆雪琪升级，所以只有第三行）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'victory-exp-nums',
        // 实测最差帧 1683 个，上界取 2 倍。
        maxPixels: 3366,
        rect: { x0: 528, y0: 173, x1: 584, y1: 444 },
        why: '第一页每人两行经验数字（这一场获得多少 / 还差多少）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'victory-loot',
        // 实测最差帧 1033 个，上界取 2 倍。
        maxPixels: 2066,
        rect: { x0: 691, y0: 133, x1: 768, y1: 215 },
        why: '掉落物三行与「金钱 N」那一行的字形',
        issue: 'xl-9bd.17',
      },
    ],
  },
  'battle-em3-box': {
    status: 'gap',
    // em3 命中框那一场（xl-rh9.8 补齐状态层，xl-rh9.9 接上画面）。实测
    // （`--every 25`，30 帧，容差 8）最差帧 #50 **0.9467%**，包围盒
    // (92,547)-(971,612) —— 与 battle-min 同一块地方，也就是同一笔字形账。
    // 上界取 2 倍 = 1.89%。
    //
    // 它比 battle-min 宽 20 px，因为这一场三个人的等级是 10 级，血量数字
    // 多了一位（`1960/1960`）。
    //
    // **为什么这一条是整屏表态而不是分区**：票面要的是 battle-min 进分区、
    // 其余四条写量出来的 gap。顺带记一个实测事实给下一个人：这四条的偏离
    // 也全落在状态栏那三块文字上，硬比区照样是干净的 —— 也就是说它们随时
    // 可以照 battle-min 的样子改成分区表态，只差把三个矩形按各自的数字宽度
    // 重量一遍。
    maxRatio: 0.018934,
    why: '状态栏那九行字的字形（同 battle-min，10 级队伍的数字多一位）',
    issue: 'xl-9bd.17',
  },
  'battle-defeat-scene': {
    status: 'gap',
    // 打输回地图那条出口（xl-rh9.3 的真值，xl-rh9.8 的状态层）。实测
    // （`--every 25`，11 帧）最差帧 #50 **0.8916%**，包围盒 (92,547)-(971,612)，
    // 又是状态栏那三块字。上界取 2 倍 = 1.78%。
    //
    // **11 帧里有 1 帧差 0 个像素**：末帧 t=250，全灭图已经从两侧对开到合拢
    // （`lsx2` 每拍 +8，到 512 就盖满），整屏被那两张图盖住，连状态栏的字都
    // 盖掉了。也就是第 24 层「全灭图」那两个 1:1 的源矩形是**逐像素对上的** ——
    // 这一帧是它唯一的正面判据，三条打输剧本各有一帧。
    maxRatio: 0.017832,
    why: '状态栏那九行字的字形（同 battle-min）；末帧全灭图盖满，差 0 个像素',
    issue: 'xl-9bd.17',
  },
  'battle-defeat-start': {
    status: 'gap',
    // 打输回标题那条出口。实测（`--every 25`，8 帧）最差帧 #25 **0.4640%**，
    // 包围盒 (105,547)-(629,612)。上界取 2 倍 = 0.93%。
    //
    // 包围盒只到 x=629，因为这一场**只有两个人出战**（张小凡 + 文敏），
    // 第三格整格空着 —— 原版那里是 `drawImage(null,…)`，什么都不画，而 web
    // 侧也什么都不画，于是那一格逐像素相等。这条顺带证明了状态栏是**按格位**
    // 画的、缺席的人不会让后面的人往前挪。
    //
    // 末帧（t=175）同样是全灭图盖满、差 0 个像素。
    maxRatio: 0.00928,
    why: '状态栏那六行字的字形（这一场只有两人出战，第三格整格空着）',
    issue: 'xl-9bd.17',
  },
  'battle-defeat-slot2': {
    status: 'gap',
    // 罹年居士在第 2 槽、照样回标题（xl-rh9.6 的真值）。实测（`--every 25`，
    // 5 帧）最差帧 #25 **0.5193%**，包围盒 (105,547)-(629,612) —— 与
    // battle-defeat-start 同形（也是两人出战）。上界取 2 倍 = 1.04%。
    // 末帧（t=100）全灭图盖满，差 0 个像素。
    maxRatio: 0.010386,
    why: '状态栏那六行字的字形（同 battle-defeat-start）',
    issue: 'xl-9bd.17',
  },
  'dorm-walk': {
    status: 'gap',
    // 地图底图已逐像素对齐（xl-9bd.16），NPC 已实现并逐 tick 对齐（xl-9bd.9），
    // 对话框已实现（xl-9bd.10）。
    //
    // 对话框接进来之后最差帧从 12.73% 降到 1.01%（实测 --every 100），剩下的
    // 那一块**只有正文的字形**：差异图上对话框的边框、名字牌与头像整个是暗的
    // （= 逐像素相同），亮起来的只有字。原因是原版用的 `文鼎粗钢笔行楷` 绝大
    // 多数机器上没有，Java2D 的基线与 DOM 的行盒也不是一回事。
    //
    // xl-9bd.17 已裁定：**不打包字体**，把 web 的后备链换成与基准同一个落点
    // （本机默认无衬线，见 `src/textFont.ts`）。同一条命令的最差帧 #500 因此
    // 从 1.0056% 降到 0.7401%，包围盒不变。剩下的字形差是接受的账。
    why: '右下角的金币 HUD + 对话正文的字形（字体不在，基线也不同）',
    issue: 'xl-yg6.1 / xl-9bd.17',
    // 这条是**第一条分区表态**（xl-1vu.3）。整屏一个状态时它只说得出"红着"，
    // 而逐帧把差异按区拆开之后，22 帧（`--every 25`，容差 8）里剩下的偏离
    // 全落在下面这三块，**三块之外一个像素都不差**：
    //
    //   t=0..300、400、525       缺口区之外 0 个（走动、跑动、对话收掉之后）
    //   t=425                    speech-name 里 1397 个 —— 名字牌上的三个字
    //   t=450..500               speech-name + speech-text 合计
    //                            1588/3098/4323 个 —— 名字与正文的字形
    //   全部 22 帧               coin-hud 里恒 527 个
    //
    // 也就是说：地图、家具、NPC、主角的走动帧**与跑动帧**、对话框的边框与
    // 名字牌本身，在这条剧本的每一帧上都是**逐像素相等**的。这是分区判据买到的
    // 东西 —— 以前这句话只能靠读注释相信，现在少一个像素就红。
    //
    // 跑动帧原先在这里挂着一个 `hero-run` 缺口区（412/360/368 个像素）。
    // xl-u39 把它还掉了：起因是 `roles/zhangxiaofanRun/15.png`、`16.png` 是
    // 42×65 而原版 `drawHero` 的源矩形只取 42×64，Web 侧却是 `setSize(42,64)`
    // 缩下来 —— 裁与缩的区别。烘焙器改成按源矩形裁之后这三帧归零，而**这张表
    // 没改就红了**（"缺口区一帧都不差了"），正是分区判据该有的样子。
    gaps: [
      {
        name: 'coin-hud',
        // 实测最差帧 527 个（2026-09-06，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 1054,
        rect: { x0: 925, y0: 615, x1: 993, y1: 630 },
        why: '右下角的金币 HUD 还没画，恒差 527 个像素',
        issue: 'xl-yg6.1',
      },
      {
        name: 'speech-name',
        // 实测最差帧 1397 个（2026-09-06，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 2794,
        rect: { x0: 0, y0: 0, x1: 271, y1: 40 },
        // 名字牌（192×32）沿画面顶端从左边滑到 (272,32)，牌子本身逐像素对上，
        // 偏的只有牌子上的三个字。按 x=272 与 speech-text 切开只为不重叠。
        why: '名字牌滑入路径上的名字字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'speech-text',
        // 实测最差帧 4323 个（2026-09-06，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 8646,
        rect: { x0: 272, y0: 0, x1: 752, y1: 192 },
        // 样式 1 的对话框：从 (752,192) 往左上长到 (272,32)，正文原点
        // (boxX+40, boxY+40)。见 `ui/dialogueLayout.ts`。
        why: '样式 1 对话框里的正文字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
    ],
  },
  'bigmap-walk': {
    status: 'gap',
    // 大地图这条剩下的是**素材重编码**，不是渲染：大地图.jpg 烘成 q80 的有损
    // WebP，把烘焙产物解回来按同一个采样公式取，与原版第 0 帧就已经差 37.22%。
    // 幅度上界（xl-l3o）：这一次实测最差帧 #475 **37.1101%**（整屏，34 帧各帧
    // 都在 37% 上下），上界取 2 倍 = 74.22%。全黑图给到 99.96%，在上界之上，会红。
    maxRatio: 0.7422,
    why: '大地图.jpg → 有损 WebP 的重编码差异',
    issue: 'xl-9bd.14',
  },
  'dorm-exit': {
    status: 'gap',
    // 出口切换（xl-9bd.12）。这条剧本横跨三个场景，于是把另外两条各自的缺口
    // 都继承了过来，一笔新账都没添。实测（`tools/compare-frames.sh dorm-exit
    // --every 100`，12 帧，容差 8）逐帧分得干干净净：
    //
    //   宿舍段（t=0..400，5 帧）  固定偏 527 个像素、0.0804%，包围盒恒为
    //                             (925,615)-(993,630) —— 右下角还没画的金币
    //                             HUD（xl-yg6.1），跟 dorm-intro 那条量到的
    //                             是同一个数。
    //   大地图段（t=500..800）    整屏 34.01%–34.07%，包围盒是整张画布 ——
    //                             大地图.jpg → q80 WebP 的重编码差异
    //                             （xl-9bd.14，bigmap-walk 那条量到 37.22%）。
    //   脚本1 段（t=900）         又回到 527 个像素、0.0804%，包围盒一模一样；
    //                             t=1000/1100 是 3970 个像素、0.6058%，包围盒
    //                             (200,503)-(993,630)，正是对话框那一块加
    //                             HUD —— 对话正文的字形（xl-9bd.17）。
    //                             （这一格在 xl-9bd.17 合并前量到的是 5592 个
    //                             像素、0.8533%：换掉楷体后备链之后字形差本身
    //                             缩了，与 dorm-walk 最差帧 1.0056%→0.7401%
    //                             是同一回事。）
    //
    // 进第三个场景的那一刻重新掉回**跟出门前逐像素一样的那 527 个**（t=900、
    // 925 两帧，一个字都还没打），这就是可核的证据：地图、主角、NPC 都落在了
    // 对的地方，换场景没带进任何新的偏离；之后涨上去的全在对话框那个包围盒里。
    // 注意第三段是**脚本1**，不是走回宿舍 —— 路线是 宿舍 → 大地图 → 脚本1。
    // 幅度上界（xl-l3o）：这一次实测最差帧 #875 **34.0767%**（大地图那一段，
    // 整屏），上界取 2 倍 = 68.15%。
    maxRatio: 0.6815,
    why: '大地图的有损 WebP 重编码 + 金币 HUD + 对话正文的字形',
    issue: 'xl-9bd.14 / xl-yg6.1 / xl-9bd.17',
  },
  'milestone': {
    status: 'gap',
    // M1 的里程碑剧本（xl-9bd.13）：原版开场一条龙 —— 脚本1 的旁白与 23 句
    // 主线对话、跟曾书书搭一次话、出宿舍门进大地图夜（脚本2）、那边的旁白与
    // 27 句对话、进大活夜（背景音乐第一次切换）、再出来回到大地图夜（第二次
    // 切换）。它把另外四条机制剧本各自量到的缺口原样继承过来，**没有添一笔
    // 新账**。实测（`tools/compare-frames.sh milestone --every 250`，37 帧，
    // 容差 8）逐段分得干干净净：
    //
    //   脚本1 的旁白段（t=0..750）    第 0 帧只差 15 个像素（旁白背景缩放的
    //                                 取整边界，与 dorm-intro 同一笔），有字的
    //                                 三帧 **0.9096%/1.8335%/2.7330%**，包围盒
    //                                 是旁白正文那一片 —— 只剩字形（xl-9bd.17）。
    //                                 xl-9bd.18 把旁白改成逐字按格摆之前，这三
    //                                 帧是 1.74%/3.71%/5.42%，多出来的是每字约
    //                                 0.37 px 的累积横向漂移。其余各段一个数都
    //                                 没动（那些帧没有旁白），实测重跑
    //                                 `--every 250` 的 37 帧逐帧核对过。
    //   脚本1 的对话段（t=1000..4250）0.28%–0.79%，包围盒 (200,503)-(993,630)
    //                                 = 对话框那一块加 HUD；其中 t=1750 差
    //                                 **0 个像素**（对话框已弹出、一个字还没
    //                                 打），t=2500/3500/4250 回到 527 个像素、
    //                                 0.0804%、包围盒恒为 (925,615)-(993,630)
    //                                 —— 只剩右下角还没画的金币 HUD（xl-yg6.1）。
    //   脚本2 段（t=5250..8250）      整屏 25.85%–28.72%，包围盒是整张画布 ——
    //                                 大地图夜.jpg → q80 WebP 的重编码差异
    //                                 （xl-9bd.14，bigmap-walk 在 大地图.jpg 上
    //                                 量到 37.22%）。
    //   大活夜段（t=8500、8750）      **553 个像素、0.0844%，包围盒又是
    //                                 (925,615)-(993,630)**。大活.png 烘成
    //                                 无损 WebP，于是这一段只剩 HUD。
    //   大地图夜段（t=9000）          28.98%，整屏 —— 又是那张 jpg。
    //
    // 大活夜那两帧是这条剧本最有分量的证据：连过两道门、跨了四个场景之后，
    // 偏离**掉回只剩右下角那一小块**。地图、主角、NPC、对话框、旁白都落在了
    // 对的地方，出口与音乐切换没带进任何新的偏离。
    //
    // 与像素无关的那一半判据在 `src/state/traceReplay.test.ts`（逐 tick 的
    // 主角/NPC/对话/旁白/视口全部对齐）与 `src/state/milestone.test.ts`
    // （这条剧本确实把四个环节都走到了）里，两边都不许有例外。
    // 幅度上界（xl-l3o）：这一次实测最差帧 #8825 **29.1754%**（大地图夜那一段，
    // 整屏），上界取 2 倍 = 58.35%。
    maxRatio: 0.5835,
    why: '大地图夜的有损 WebP 重编码 + 金币 HUD + 旁白与对话正文的字形',
    issue: 'xl-9bd.14 / xl-yg6.1 / xl-9bd.17',
  },
  'dorm-intro': {
    status: 'gap',
    // 旁白（xl-9bd.11）与对话框（xl-9bd.10）都接上之后实测
    // （`tools/compare-frames.sh`，每 25 个 tick 一帧，共 165 帧，容差 8）。
    // 两张票各自的表态都说"欠着对方"，合并之后两边都不欠了，剩下的是这三笔：
    //
    //   1. **字形。** 原版的 `文鼎粗钢笔行楷` 没有随游戏交付，两端各自退到
    //      后备字体，同样的基线坐标画出来的笔画不同。xl-9bd.17 已裁定不打包
    //      字体、只把 web 的后备链换成与基准同一个落点（本机默认无衬线），
    //      理由与三条链的实测表在 `src/textFont.ts`：165 帧的差异像素合计从
    //      1126591（1.0418%）降到 992667（0.9180%），而字最多的那一帧
    //      t=800 反向从 5.5397% 涨到 5.6325% —— 浏览器的粗体比 Java2D 重。
    //      对话段的最差帧仍是 t=3425（包围盒正是对话框那一块），从 3.0641%
    //      降到 2.1432%。**这一笔是接受的账，不是待补的功能。**
    //
    //      xl-9bd.18 又从这一笔里切走了**横向漂移**那一半：旁白原先是
    //      `fillText(整行)`，而浏览器给中文的步进不是原版 `FontMetrics` 那个
    //      整 20 px，一行 43 个字累起来差十几个像素。改成逐字按格摆之后
    //      （同一条命令重量两次，原版侧那 165 张位图完全相同，只重截 web 侧）：
    //      **合计 992667 个像素、0.9180% → 687547 个、0.6358%；最差帧 t=800
    //      从 5.6325% 降到 2.8482%**。t=800 那六行旁白的墨迹右边界
    //      （亮度 > 220，与 textFont.ts 同一条判据）由
    //      433/924/822/313/607/261 变成 **427/908/808/308/597/258**，
    //      原版是 426/908/808/308/597/253 —— 中间四行**逐像素相同**。
    //      第一行差 1 px 是末字的笔画本身，步进已经一样了；第六行
    //      '我们的故事就此展开......' 还差 5 px，是那六个**半角**句点：它们
    //      在两端的字体里步进本来就不同（基准侧 Java 量到 5 px），属于字形
    //      那笔账，不是排版。
    //      可核的证据：采样到的 165 帧里**有 8 帧差 0 个像素**（t=1425、1750、
    //      1925、2050、2950、3075、3175、3950），它们全是"对话框已弹出、头像
    //      正在滑入、一个字都还没打"（真值 printing=false、cursor=0）。也就是
    //      对话框的边框、名字牌、头像与滑入动画是逐像素对上的，偏的只有字。
    //   2. **右下角的金币 HUD 还没画**（xl-yg6.1）：没有对话框盖住它的帧固定
    //      偏 527 个像素、0.0804%，包围盒恒为 (925,615)-(993,630)。
    //   3. **旁白背景的缩放取整。** 第 0 帧（旁白已起、还没有字）只差 15 个
    //      像素，全在第 599 行，最大通道差 41 —— 639×395 拉满 1024×640 时最近
    //      邻的取整边界，两端各按各的规则舍入。占比 0.0023%，在判定阈值之内。
    // 幅度上界（xl-l3o）：这一次实测最差帧 #800 **2.8482%**（旁白字最多的那一帧），
    // 上界取 2 倍 = 5.70%。五条剧本里数它最紧，也正是它给出了 2 倍这个余量 ——
    // 换一条后备字体链，同一帧记录在案的量是 5.6325%，压在上界之下。
    maxRatio: 0.0570,
    why: '旁白与对话正文的字形（原版字体未交付）+ 右下角的金币 HUD',
    issue: 'xl-9bd.17 / xl-yg6.1 / xl-9bd.18',
  },
  'shop-trade': {
    status: 'unassembled',
    // 商店真值（xl-1vu.6，driver = shop）。与 menu-equip 同一个处境：Web 侧
    // 整个商店系统还没做（M4 / xl-knp），取图页拿判别名去装配会直接抛
    // UnknownDriverError（`src/replay/drivers.ts`），整条流水线在这条剧本上
    // 非零退出 —— 而不是静静地比出"零帧差异"。
    //
    // 所以这里既不写 maxRatio 也不写 gaps：一帧都还没比过，写任何数都是编的。
    // 商店在 web 侧画出来之后，这一条要么改成 match，要么带上真量出来的表态 ——
    // **那是 M4 的事，xl-1vu 这个 SPEC 不接线**。
    why: 'Web 侧还没有商店系统，取图页装配不出 driver=shop，一帧都出不来',
    issue: 'xl-knp.1',
  },
  'menu-equip': {
    status: 'unassembled',
    // 第一条**不是场景**的剧本（xl-1vu.5，driver = menu）。Web 侧整个菜单系统
    // 都还没做（M3 / xl-6lo），所以这条剧本现在连一帧都出不来 —— 取图页拿
    // 判别名去装配会直接抛 UnknownDriverError（`src/replay/drivers.ts`），
    // 而那正是想要的：一条装配不出来的剧本不许静静地比出"零帧差异"。
    //
    // 因此这里**不写 gaps 分区**：分区表态的判据是"缺口区之外逐像素相等"，
    // 而这条剧本一个像素都还没画出来过，写分区等于凭空声明一份没量过的账。
    // 菜单在 web 侧画出来之后，这一条要么改成 match，要么带上真量出来的分区 ——
    // **那是 M3 的事，xl-1vu 这个 SPEC 不接线**。
    why: 'Web 侧还没有菜单系统，取图页装配不出 driver=menu，一帧都出不来',
    issue: 'xl-6lo',
  },
  'menu-magic': {
    status: 'unassembled',
    // 奇术页与那条被冻住的 100ms 循环（xl-1vu.9，driver = menu）。与 menu-equip
    // 同一个处境、同一个理由：Web 侧还没有菜单系统（M3 / xl-6lo），取图页装配
    // 不出 driver=menu，一帧都出不来，所以既不写 maxRatio 也不写 gaps。
    //
    // 这一条对 M3 的意义不在像素上：它是**唯一**一条把 `mouse.code/frame` 与
    // `magic.animation.code` 逐帧推进记下来的真值。菜单的鼠标图标与技能动画
    // 在 web 侧做出来之后，逐帧对齐的对象是它，不是 menu-equip（那一条整条
    // 都停在第 0 帧）。
    //
    // **接线时记得把取帧密度调到 `--every 1`。** `tools/compare-frames.sh` 缺省
    // 25 个 tick 取一帧，这条剧本 47 步只出 2 帧 —— 而它的全部意义在逐帧上，
    // 按缺省采样等于两帧里什么动画都没采到，却照样"比过了"。
    why: 'Web 侧还没有菜单系统，取图页装配不出 driver=menu，一帧都出不来',
    issue: 'xl-6lo',
  },
}

/**
 * 磁盘上有哪些剧本。**分母从源头数**，不抄一份名单：别人往
 * `tools/traces/scripts/` 里加一条剧本，这里立刻就知道，而它没在 `EXPECTED`
 * 里表态就会红。名单抄两份，迟早对不上。
 */
export function scriptNames(): string[] {
  return readdirSync(repoPath('tools/traces/scripts'))
    .filter((f) => f.endsWith('.json'))
    .map((f) => f.replace(/\.json$/, ''))
    .sort()
}

export function expectationOf(name: string): Expectation {
  const e = EXPECTED[name]
  if (!e) {
    throw new Error(
      `剧本 ${name} 没有在 src/compare/expected.ts 里表态。` +
        `新剧本必须先说清楚它现在应该全过（match）还是应该差（gap，并写明差在哪、归哪张票）。`,
    )
  }
  return e
}
