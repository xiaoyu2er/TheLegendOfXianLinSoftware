# 跨端逐帧比对与时间加速

> 一条命令：同一份剧本在原版与 Web 版各跑一遍，各出 N 帧，逐帧算差异，
> 超过阈值即判为回归，并报告**第一个偏离的帧号**。
>
> 实现：`tools/compare-frames.sh`（编排）、`tools/src/devtools/ExportTrace.java`
> 的 `--frames`（原版侧出图）、`web/scripts/compare.ts`（Web 侧出图 + 报告）、
> `web/src/compare/`（判据、PNG 编解码、期望表）。剧本格式见
> `docs/trace-format.md`。

## 命令

```bash
tools/compare-frames.sh                          # 全部剧本
tools/compare-frames.sh dorm-walk                # 只跑一份
tools/compare-frames.sh --every 50               # 每 50 个 tick 取一帧（压掉剧本自报值）
tools/compare-frames.sh --threshold 0.001        # 一帧里超过千分之一的像素偏了才算偏离
tools/compare-frames.sh dorm-walk --self-check   # 故意改坏一处渲染，验流水线响不响
tools/speed-probe.sh                             # 两端时间加速的实测线性度
```

**不带 `--every` 时密度由剧本自报**（xl-6lo.3）：剧本 JSON 里可选的 `every`
字段说了算，剧本没写才落到 25。菜单那两份剧本自报的是 1 —— 它们是事件驱动的，
一步就是一次输入事件，按 25 采样等于 47 步只出 2 帧，**却照样打印"比过了"**。
字段的定义见 `docs/trace-format.md` 的剧本字段表。

需要 Java（`brew install openjdk@17`）与本机的 Chrome（`CHROME=…` 可指定路径）。
两样都不在 CI 里，所以这条流水线不在 CI 里；进 CI 的是它的**判据**
（`web/src/compare/*.test.ts`，`pnpm test`）。

产物在 `tools/traces/compare/<剧本>/`，**不入库**（`tools/.gitignore`）：

| 目录 | 内容 |
|---|---|
| `java/` | 原版真的画出来的位图（`ScenePanel.backImage`），加 `frames.json` 清单与本次导出的 trace |
| `web/` | 真浏览器里 Pixi 真的画出来的位图 |
| `web-broken/` | 只有 `--self-check` 才有：故意改坏渲染那一遍 |
| `diff/` | 差异图，洋红 = 超出容差 |
| `../report.json` | 机器可读的报告 |

## 两端是怎么跑同一份剧本的

原版侧就是 trace 导出器多存了几张图：它每个 tick 真的调一次 `paint()`，
`--frames` 只是把那张已经画好的 1024×640 位图每 n 个 tick 写成 PNG，并记一份
**帧清单**。**每步都画，两条独立的理由**：场景侧 `ScenePanel.paint()` 有副作用
（`calOffset()` 算视口，`@` / `$` 会写 `dialogueFight` / `gameOver`），省不得；
战斗侧 `paint()` 实测**没有**副作用（2026-09-06 推翻旧结论，见
`docs/trace-format.md`），但位图本身就是这份真值的交付物，而"省一次绘制"正是
那种省对了和省错了长得一样的优化。

Web 侧是一个 dev-only 的取图页（`web/replay.html` + `src/replay/main.ts`），
在**真的无头 Chrome** 里跑真的 Pixi 渲染器，按 trace 里那一 tick 实际喂给原版的
按键回放（与 `src/state/traceReplay.test.ts` 同一套），推进到清单里的每个 tick
再截一张图。

三处关键决定：

- **比哪些帧只有一个来源**：清单由原版侧生成，Web 侧照着走。两端各算各的
  采样点，是错位的现成入口；而错位之后每一帧都不一样，报告只会说"全红"。
- **回放的是按键，不是坐标**。喂坐标等于把两端的分歧提前抹平：图会一直一致，
  而游戏是错的。
- **必须是真浏览器**。在 Node 里拿一个软件光栅器重画一遍，比的就是那个光栅器，
  不是被移植的渲染代码。CDP 客户端是自己写的一百来行（`web/scripts/cdp.ts`），
  Node 内建的 `WebSocket` 与 `fetch` 就够，不引 puppeteer。

## 判据：为什么不是哈希

本项目已经为这件事付过一次学费：取景器截了 4800 帧战斗，用 MD5 判"唯一帧"，
报出"60/60 唯一、没问题"，而实际上 16 场里 14 场两秒后完全静止、九成的帧是废的
——行动条挪一个像素就算"不同"，这个判据没有诊断力。

所以量的是**有多少像素差了多少**，并且把差异的**位置**一起报出来
（`web/src/compare/diff.ts`）：

| 量 | 作用 |
|---|---|
| `tolerance`（默认 8） | 单通道容差。地图资产在 Web 侧走 WebP，JPG 源那批是有损的，逐字节相等做不到也不该要求 |
| `ratio` | 超出容差的像素占比，判"这一帧算不算偏了" |
| `threshold`（默认 0.0002） | `ratio` 超过它就算偏离 |
| `maxDelta` / `box` | 坏了要长得不一样：整屏差一点点（资产编码）与一小块差很多（画错了一个精灵）必须分得出来 |

分母是**剧本条数 × 各自的帧数**，开跑前就数得出来，报告里逐条印出来。
空的帧序列、清单里有而磁盘上没有的帧、两端尺寸不同，一律硬失败——
"一帧都没比"和"比了全过"在任何只看退出码的检查里长得一模一样。

## 落地时的实测（2026-09-06，3 条剧本 / 221 帧 / 每 25 个 tick 取一帧）

```
跨端逐帧比对：3 条剧本 × 各自的帧数 = 221 帧（阈值 0.0200% 的像素，单通道容差 8）

  通过  bigmap-walk    34 帧 · 首个偏离帧 #0 · 偏离 34/34 · 最差 #625 95.9496% @ (0,0)-(1023,639)
        已知缺口（地图拉伸 + 镜头跟随 + 大地图的 13 个 NPC），第 0 帧起偏离
  通过  dorm-intro    165 帧 · 首个偏离帧 #0 · 偏离 165/165 · 最差 #625 99.9706% @ (0,0)-(1023,639)
        已知缺口（地图拉伸 + 旁白与主线对话框），第 0 帧起偏离
  通过  dorm-walk      22 帧 · 首个偏离帧 #0 · 偏离 22/22 · 最差 #425 85.2089% @ (0,0)-(1023,639)
        已知缺口（地图拉伸（原版把 1016×632 的源区拉到 1024×640）+ 宿舍的 2 个 NPC），第 0 帧起偏离

3/3 条剧本符合预期。

流水线自检：故意改坏一处渲染
  通过  bigmap-walk  34 帧 · 注入点 #425 · 首个变化帧 #425 · 变了 15/34 帧
  通过  dorm-intro   165 帧 · 注入点 #2050 · 首个变化帧 #2050 · 变了 83/165 帧
  通过  dorm-walk    22 帧 · 注入点 #275 · 首个变化帧 #275 · 变了 11/22 帧
```

整轮（含自检那一遍取图）约 3 分钟，其中原版侧出图与 Web 侧出图各占一半。
"通过"在这里的意思是**符合期望表**，不是"两端一致"——三条现在全都差得很远，
差在哪由下一节的期望表逐条钉住。

把某条剧本的期望从 `gap` 改成 `match` 再跑，它会红成
`失败 … 回归：第 0 帧起偏离`，退出码 1（已实测）。

## 期望表：为什么"现在必然红"不等于没用

Web 版还没画 NPC、没做旁白与对话框，跨端比对现在必然红。一条恒红的检查
第二天就变成噪声；而把阈值放松到能过，就变成了这个项目最贵的那种检查——
通过条件是"没找到问题"。

`web/src/compare/expected.ts` 因此让每条剧本**显式表态**，且**两头都会红**
（`src/assets/knownMissing.ts` 是同一个套路）：

- `match` 的剧本出现偏离帧 → 红，报告指出第一个偏离的帧号；
- `gap` 的剧本一帧都不偏了 → **也红**，说明缺口补上了、这张表过期了；
- `gap` 的剧本偏得比声明的多 → **也红**（下一节）；
- 磁盘上有而表里没表态的剧本 → 红。名单从 `tools/traces/scripts/` 现扫，
  不抄第二份。

## 幅度上界：偏多少也算数

上面那两条只管"有没有偏离"，不管偏了多少——而**灾难性的错误同样能提供偏离**。
实测（2026-09-06，xl-l3o）：把基准侧 `ExportTrace.dumpFrame` 里那张位图换成
全黑的 1024×640，`dorm-walk` 的最差帧从 0.7401% 跳到 99.9565%，流水线退出 0、
报"1/1 条剧本符合预期"。这正是本仓库那句"失败的样子和成功一模一样"。

所以每笔账都有上界（判据在 `web/src/compare/verdict.ts` 与 `regions.ts`）：

- 整屏表态写 `maxRatio`——**每一帧**的差异占比都必须 ≤ 它；
- 分区表态给每个缺口区写 `maxPixels`——**每一帧**里那个区超容差的像素数上限。

上界是**每帧**而不是所有帧之和：采样密度由 `--every` 定，帧数一变和值跟着变，
一张按 `--every 25` 量出来的和值表换个采样就是错的，而且是往松了错。单帧的量
与采样密度无关——多采几帧只可能采到更差的帧。

**取值一律实测，余量一律 2 倍。** 上界要挡住"什么都没画出来"，又不能让另一台
机器的正常抖动变红。这条流水线上最会抖的是**后备字体**：本仓库记录的实测
（xl-9bd.17 / xl-9bd.18）里，`dorm-intro` 最会写字的那一帧 t=800 在
5.5397% → 5.6325% → 2.8482% 之间来回，最宽一档 1.98 倍；这一次重量到的正是
2.8482%。2 倍盖得住"这台机器挑了另一条后备链"，盖不住"这一帧没画出来"
（全黑 ≈ 100%）。重编码那几条（两端解码器都定死）其实稳得多，2 倍对它们是宽的
——宁可规则统一、余量说得清，也不要每条剧本各拍一个数。

> ⚠️ **这条规矩今天有两处例外，都在「变紧」的方向上**（xl-x6w，2026-09-08）：
> `battle-zhang-skills` 与 `battle-yu-skills` 的最差帧因为改了素材编码而变差，
> 照「×2」重算会**放宽**上界，用户裁定保持原值不动，于是它们的余量是
> **1.86× / 1.88×**。理由与实测在下面「背景动画的有损重编码」那一节末尾。
> **例外只许往紧的方向开**：往松的方向开就等于让被守的东西自己给自己签字。

这一次量到的五条（`--every 25`，容差 8，共 636 帧）与它们的上界：

| 剧本 | 最差帧 | 上界 | 篡改成全黑之后 |
|---|---|---|---|
| bigmap-walk | #475 37.1101% | 74.22% | #0 99.9751%（1.35 倍） |
| dorm-exit | #875 34.0767% | 68.15% | #525 99.9867%（1.47 倍） |
| dorm-intro | #800 2.8482% | 5.70% | #3725 99.9649%（17.54 倍） |
| milestone | #8825 29.1754% | 58.35% | #8800 99.9921%（1.71 倍） |
| dorm-walk（分区） | coin-hud 527 / speech-name 1397 / speech-text 4323 | 1054 / 2794 / 8646 | 1104、11152、92833，三个区**全部**超界，硬比区同时破 |

全黑那一轮 `tools/compare-frames.sh`（不带剧本名，五条全跑）退出码 1，报
"5/5 条剧本不符合预期"，并逐条点名超了多少倍。

**另一头没被挤掉。** 同一天的第二次篡改：把 `dorm-intro` 与 `dorm-walk` 的 Web
侧 187 张帧直接抄成基准侧的（差异归零，等于"缺口全补上了"），退出码仍是 1 ——
整屏那条报"缺口没了…把 expected.ts 里这条改成 match"，分区那条报"缺口区
coin-hud、speech-name、speech-text、hero-run 一帧都不差了"。上界与下界各管一头。
（那一轮里 `hero-run` 还在表上；它已经在 xl-u39 里还掉了，见下面那一节。）

上界还必须**够得着**：`expected.test.ts` 里有一条检查，缺口区的 `maxPixels`
不许大于这块区自己的面积——破不了的检查和没有检查是同一件事，而两者都安安静静
地通过。`coin-hud` 那一格就是这条检查的现实意义：它的区里一共 1104 个像素，
上界 1054，全黑时刚好超 50 个。

## 分区表态：硬比区逐像素，缺口区各自记账

整屏一个状态的表态只说得出"这条剧本红着"，红在**哪儿**由注释里的一段散文钉住
——而散文不会在缺口挪了位置的时候变红。缺口挪到别处、或者多长出一块新的偏离，
`status: gap` 照样通过。

`Expectation` 因此有一个可选的 `gaps`（xl-1vu.3），判据在
`web/src/compare/regions.ts`：

- 声明出来的矩形是**缺口区**，照旧记账；
- 矩形**之外**全是**硬比区**，每一个像素都必须在容差内相等。不是"超过阈值才
  算偏"，是**一个都不许有**；
- **每个缺口区各自双向红**：某个区一帧都不差了就红。整屏版的双向红只有一个
  分母，三个缺口里补上一个根本看不出来。

写了 `gaps` 的剧本走这一套，没写的照旧。表那一层还有三条约束
（`expected.test.ts`）：区之间**不许重叠**（重叠会让"这个区归零了"不再可判）、
每个区要写 `why` 与 `xl-` 票号、**所有区加起来不许超过画布的 40%**——"把区画大
一点让它过"是这套判据唯一的作弊路子。

### dorm-walk：第一条分区表态

实测（`tools/compare-frames.sh dorm-walk --every 25`，22 帧，容差 8）：

```
  通过  dorm-walk      22 帧 · 首个偏离帧 #0 · 偏离 22/22 · 最差 #500 0.7401%
        硬比区 22 帧逐像素相等；coin-hud 合计 11594、最差 527/1054 ·
        speech-name 合计 1397、最差 1397/2794 · speech-text 合计 9009、
        最差 4323/8646
```

（`最差 527/1054` 是"这个区最差的一帧差了 527 个像素，上界 1054"，见上一节。）

11594 = 527 × 22，正是右下角那块还没画的金币 HUD。也就是说：地图、家具、NPC、
主角的走动帧**与跑动帧**、对话框的边框与名字牌本身，在这条剧本的每一帧上都逐
像素相等——这句话以前只能靠读注释相信。（跑动帧原先是个例外，见下一节。）

### 分区量出来的那一笔账，以及它是怎么还掉的（xl-u39）

分区判据立起来的当天顺手量出了一笔没人记过的账：`hero-run`。`runTo` 那一段的
三帧（t=225/250/275）在 HUD 之外各差 412/360/368 个像素，包围盒都落在跑步精灵
那 42×64 的框里，而**走动**的帧在 HUD 之外差 0 个。整体平移 dx∈[-3,3]、
dy∈[-1,1] 再比，(0,0) 是唯一最优（412 对次优 5114），所以不是错位，是内容不同。

**结论不是它猜的那个。** 票上怀疑跑步帧号（`Role.count2` / web 的 `runFrame`）
两端推进不一致，而 `count2` 当时不在真值里。把它补进 scene 真值之后逐 tick 比对
全绿——两端的推进**一模一样**，起点与步长都不差。真正的原因在素材尺寸：

`roles/zhangxiaofanRun/15.png` 与 `16.png` 是 42×65，其余 14 张 42×64。原版
`Role.drawHero` 的 `drawImage` 带**源矩形** `(0,0)-(42,64)`，多出来的那一行是被
**裁**掉的；而 Web 侧 `sceneRenderer` 走 `setSize(42, 64)`，那是**缩**——65 行
重采样成 64 行。这两张正好是朝右的第 3、4 帧，也正是那三帧画到的。烘焙器改成
按源矩形裁之后，这三帧归零。

**这笔账是被判据自己催着还的**：修好之后 `expected.ts` 还没改，跑一次比对就得到

```
  失败  dorm-walk      22 帧 · …
        缺口区 hero-run 一帧都不差了 —— 账还完了而表没改，把这些区从
        expected.ts 的 gaps 里删掉（删干净之后整条就该是 match）。
```

也就是下面那张篡改表里第三行同一条路径，只是这一次不是篡改，是真的补上了。

### 三次篡改（2026-09-06 实测，都退出码 1）

| 改了什么 | 结果 |
|---|---|
| 硬比区里一帧的一个像素取反色 | `硬比区破了：第 275 帧起…22 帧合计 1 个…@ (500,300)-(500,300)` |
| 四个缺口区全抄成原版（2781790 个像素） | `缺口区 coin-hud、speech-name、speech-text、hero-run 一帧都不差了` |
| 只把 `hero-run` 抄成原版（469832 个像素） | `缺口区 hero-run 一帧都不差了` |

第三次是分区相对整屏多买到的那点分辨力：那一次整屏判据会说**通过**——报告那
一行仍然是"偏离 22/22"。

⚠️ 这三次是 xl-1vu.3 当天的实测，那时 `hero-run` 还在表上；它已经在 xl-u39 里
删掉了，所以后两行**照原样复现不出来**（区不存在，抄它没有意义）。要重跑就换成
今天表里还在的那三个区之一，路径与结论一样。第三行那条路径本身没有过期——
xl-u39 真的走了一遍，见上一节。

## 逐像素相等区：矩形从真值现读（xl-aq0）

上面那套分区表态是**整屏切分**：列出来的是缺口，其余全是硬比区。战斗那几条
剧本用不上它 —— 它们还欠着背景动画的有损重编码（xl-7ip），那笔账是**满屏**的，
声明不成矩形，所以它们走的是整屏 `maxRatio`。

而整屏上界挡不住**小面积**的回归。现实例子：xl-ttu 把提示图的缩放采样从 GPU
的最近邻换成原版的定点采样（`web/src/battle/render/scaledBlit.ts`），
`battle-menus` 的 t=125 那块 80×16 从差 111 个像素变成 0。可**把渲染器换回改前
那一版，当时全套检查一条都不红**：`pnpm test` 全绿（渲染器按设计没有测试缝，
见 `docs/adr/0002`），整屏上界也照过 —— 111/655360 = 0.017%，而上界是 1.9610%。

`Expectation` 因此还有第三个可选字段 `exact`，判据在
`web/src/compare/exactRegions.ts`：

- 声明的是**逐像素相等区**，区里超容差的像素必须是 **0**，**没有上界** ——
  这一块本来就该完全相等，给它上界等于给回归留位置；
- 「相等」是**容差之内**相等（容差沿用整屏那条口径，跑的是 8），与分区表态的
  硬比区同一句话。**这不是留余地，是这一层做不到字面零差**：同一批帧把容差调
  到 0 现数，15 帧 22620 个像素里有 **7526 个**通道差 1..8（提示图带透明，压在
  有损重编码过的背景动画上，两端合成取整差一档）。那个数本身也是判据 —— 哪天
  它归零，说明合成那一路也对齐了，那时才该收紧容差；
- **矩形不写在表里**，每一帧从行为真值现读（提示图读的是
  `reminder.dx1..dy2`，与 `drawList.ts` 的 `reminderOps` 同一组字段）。提示图
  每拍朝两边张开，一条剧本走过十二档尺寸（10×2 … 120×24）；写死坐标就会守着
  一块空地，而空地上两端本来就相等 —— **判据会安安静静地恒真**；
- 它**叠在**整屏（或分区）判据之上，不是替代。任一条不过，整条剧本不过。

### 两个分母

"找不到东西"不许成为通过条件，所以这套判据有两个分母：

1. **真值层**（`ExactRegionSpec.drawnTicks`）：整条真值里这一层画着、且矩形非空
   的 tick 数。它是**人签的一份登记**，不许现扫 —— 现扫等于让被守的东西自己
   给自己签字。它与取帧密度无关，所以写得死：`--every` 怎么调都不影响它。
   `expected.test.ts` 拿 `tools/traces/out/` 里的入库真值现扫一遍去撞它，
   **三个方向都会红**：真值里有拍画着而表里没声明、表里声明了而真值一拍都没有、
   两边都有但数对不上。那条判据在 CI 里跑，不要 Java 也不要 Chrome。
2. **本次运行层**：这一趟采样到的帧里至少有一帧命中矩形。一帧都没命中是硬失败
   （报"换 `--every` 再跑"），不是通过 —— 那说明这一趟根本没验到这块。

### 实测（2026-09-08，`--every 25`，容差 8）

四条剧本画提示图，15 帧命中，合计 22620 个像素，**超容差 0 个**：

| 剧本 | 登记拍数 | 命中帧 | 每帧 |
|---|---|---|---|
| battle-menus | 63 | 3 | t=125 0/1280、t=200 0/80、t=300 0/180 |
| battle-zhang-skills | 84 | 4 | t=75/175/350 各 0/2880、t=450 0/720 |
| battle-yu-skills | 84 | 3 | t=50 0/2420、t=800 0/2880、t=850 0/320 |
| battle-lu-skills | 126 | 5 | t=50/375 各 0/2880、t=100 0/180、t=300/600 各 0/80 |

`battle-zhang-skills` 的 t=75/175/350 与 `battle-yu-skills` 的三帧背景动画正
放着，区外照旧记着 xl-7ip 那笔有损重编码的账，而**区内一个像素都不差** ——
这套判据不会被那笔误伤。

### 篡改验证（2026-09-08 实测，十次，每一次都先确认篡改真的写进去了）

第一条就是这张票的来由，其余八条验的是判据自己的每一道门：

| # | 篡改 | 结果 |
|---|---|---|
| 1 | `battleRenderer.ts` 换回 GPU 采样那一版（`git show 03bb5fd~1:…`） | **`pnpm test` 仍然 1112 条全绿**；比对流水线**退出码 1，四条剧本一起红** —— menus 第 125 帧起 3 帧合计 174（最坏 111/1280）、zhang 4 帧 444（最坏 238/720）、yu 3 帧 236（最坏 138/2420）、lu 5 帧 209（最坏 69/2880）。而**整屏上界四条全部照过**（menus 0.9975% ≤ 1.9610%）—— 这一条就是那个洞 |
| 2 | 删掉 `battle-zhang-skills` 的整块 `exact` | `真值里有 84 拍画着提示图，却没声明逐像素相等区` |
| 3 | `battle-menus` 的 `drawnTicks` 63 → 62 | `登记拍数与真值对不上：expected 62 to be 63` |
| 4 | 给一拍都不出提示图的 `battle-min` 声明一块 | `真值里一拍都没画提示图，却声明了逐像素相等区` |
| 5 | `exactRectsOf` 不再过滤"已停住"的拍 | 单测红 + 登记对撞红 |
| 6 | `exactRectsOf` 不再过滤零宽/零高的矩形 | 单测红 + 登记对撞红（空矩形会变成一块永远相等的假区） |
| 7 | `rectDiffering` 的容差判定永远不成立 | `矩形里的像素账` 两条红 |
| 8 | `judgeExact` 给这块区开一个 1000 像素的上界 | `差一个就红` 那条红 |
| 9 | `judgeExact` 把"一帧都没命中"当成通过 | `一帧都没命中不算通过` 那条红 |
| 10 | `expected.test.ts` 的 `SCANNED_SOURCES` 清空 | `来源 reminder 还没有人扫`（挡的是"加了新来源而对撞悄悄不核它"） |

第 1 条改回来之后重跑，四条剧本全部退回"通过"，退出码 0。

### 为什么不是另外两条路

票面（xl-aq0）列了三条，另外两条的代价：

- **让渲染器把"这一帧走了哪条采样路径"记到某个可读的地方**：等于给渲染器开一个
  只为测试存在的出口，而"渲染器不开测试缝"是 M2 定的（`docs/adr/0002`）；而且
  它验的是"我调用了自己"，不是"画出来的像素对"。
- **把分支判定抽成纯函数再钉住它**：只守得住判定逻辑对不对，守不住"渲染器真的
  调了它" —— 换回 GPU 采样那一版照样绿，洞原封不动。

立硬比区守的是**画出来的像素**，跟渲染器内部长什么样无关。

## 装配不出来的驱动器：未实现的 web 侧必须响亮（xl-1vu.7）

驱动器的真值（有哪几支见 `docs/trace-format.md` 的驱动器一览，M6 起多了第五支
`saveload`），**取图页只装配得出 `web/src/replay/implemented.ts` 里
`IMPLEMENTED_DRIVERS` 列的那几支**——名单只有那一份，这里不抄第二份（**别照抄
任何写在散文里的读数**：这一处的读数已经过期过两次，2026-09-07 写的是 `scene`
与 `battle`）。没在名单上的那几支在这条流水线里
必须响亮地失败：一条没被装配的剧本比出来是「零帧差异」，跟「两端完全一致」长得
一模一样。

⚠️ **xl-knp.10（M4 收口）把最后一支 `shop` 接上之后，磁盘上一条装配不出来的
剧本都不剩了。** 这一节讲的判据一条都没变，但它守的东西现在**只对未来的新
驱动器有效** —— 于是它原先靠"磁盘上总还剩几条没接线的"当素材的那两处判据
当场变成恒真或恒红，两处都改掉了，见下面「一条都不剩之后」。

流水线因此**在开浏览器之前先分流**。分流的两份记录互不抄袭：

| 记录 | 说的是 | 在哪 |
|---|---|---|
| 名单 | 取图页**能**装配哪些判别名 | `web/src/replay/implemented.ts` |
| 表态 | 每条剧本**声称**自己现在什么处境 | `web/src/compare/expected.ts` 的 `status` |

`web/src/compare/unassembled.ts` 拿这两份对撞，**两个方向都是硬失败**：表说
`match`/`gap` 而页面装不出（那笔账是编的，一帧都没比过）；页面已经装得出而表
还写 `unassembled`（面板做好了而表没改，这条剧本会永远挂在"比不了"上）。
判据在 `web/src/compare/unassembled.test.ts`，跑在 `pnpm test` 里 —— 不需要 Java
与 Chrome，所以它进 CI，而整条流水线不进。

名单为什么不能抄两份：`web/src/replay/main.ts` 的装配表声明成
`Record<ImplementedDriver, Assembly>`，**少一个键或多一个键都是编译错** ——
这道是常开的，`pnpm typecheck` 每次都过。跑起来之后页面还把
`Object.keys(ASSEMBLIES)` 挂在 `window.__xlDrivers` 上，比对器进门核一次
（`assertPageAgrees`），核不上是硬失败，不许降级成"那就信本地这份"；不过**这第二道
只在这一轮真的要取图时才跑** —— 一轮里全是装配不出来的剧本时压根不开浏览器
（`tools/compare-frames.sh menu-equip` 就是这种），那一轮守着的只有类型那道。

**为什么要提前分流而不是撞上去。** 从前是撞上去的：取图页对未实现的驱动器抛，
异常转成 Node 侧的 Error，整轮**中断**。于是默认全跑时 —— 按字典序第一条正好是
`battle-em3-box` —— 五条能比的场景剧本一帧都比不成，`--self-check` 也跟着不跑。
一条把自己噎死的流水线与一条恒绿的流水线，实用价值是一样的。

实测（**2026-09-06 的历史读数**，当时名单上只有 `scene`，战斗还没接；
`tools/compare-frames.sh --self-check`，9 条剧本，退出码 **1**）：

（逐字照抄，只删掉五条剧本各自的第二行判词与取图进度行；其余一字未改）

```
跨端逐帧比对：5 条剧本 × 各自的帧数 = 636 帧（阈值 0.0200% 的像素，单通道容差 8）

  通过  bigmap-walk    34 帧 · 首个偏离帧 #0 · 偏离 34/34 · 最差 #475 37.1101% @ (0,0)-(1023,639)
  通过  dorm-exit      48 帧 · 首个偏离帧 #0 · 偏离 48/48 · 最差 #875 34.0767% @ (0,0)-(1023,639)
  通过  dorm-intro    165 帧 · 首个偏离帧 #25 · 偏离 155/165 · 最差 #800 2.8482% @ (22,43)-(1018,599)
  通过  dorm-walk      22 帧 · 首个偏离帧 #0 · 偏离 22/22 · 最差 #500 0.7401% @ (273,3)-(993,630)
  通过  milestone     367 帧 · 首个偏离帧 #25 · 偏离 357/367 · 最差 #8825 29.1754% @ (0,0)-(1023,639)

5/5 条剧本符合预期。

web 侧还装配不出来的剧本 4 条 —— 这一趟它们一帧都没比过：
  装不出  battle-em3-box  driver=battle  web 侧还没有战斗面板，取图页装配不出 battle，整条流水线在这条剧本上硬失败 · 归 xl-82c
  装不出  battle-min      driver=battle  web 侧还没有战斗面板，取图页装配不出 battle，整条流水线在这条剧本上硬失败 · 归 xl-82c
  装不出  menu-equip      driver=menu    Web 侧还没有菜单系统，取图页装配不出 driver=menu，一帧都出不来 · 归 xl-6lo
  装不出  shop-trade      driver=shop    Web 侧还没有商店系统，取图页装配不出 driver=shop，一帧都出不来 · 归 xl-knp.1
  取图页现在实现了：scene。等上面那几张票把 web 侧的面板建起来，再回 src/compare/expected.ts 把表态换掉。
  （非零退出。一条没被装配的剧本比出来是"零帧差异"，跟"两端完全一致"长得一模一样。）

流水线自检：故意改坏一处渲染
  通过  bigmap-walk  34 帧 · 注入点 #425 · 首个变化帧 #425 · 变了 17/34 帧
  通过  dorm-exit    48 帧 · 注入点 #600 · 首个变化帧 #600 · 变了 24/48 帧
  通过  dorm-intro   165 帧 · 注入点 #2050 · 首个变化帧 #2050 · 变了 83/165 帧
  通过  dorm-walk    22 帧 · 注入点 #275 · 首个变化帧 #275 · 变了 11/22 帧
  通过  milestone    367 帧 · 注入点 #4575 · 首个变化帧 #4575 · 变了 174/367 帧
```

也就是说：**能比的照常比、照常自检，比不了的逐条点名并把退出码染红。**

### 篡改验证（2026-09-06 实测，每一条都真跑过）

⚠️ **下面这张表拿 menu 当"还没实现的驱动器"举例，而 xl-6lo.14 之后 menu 已经
实现了 —— 表里前两行与最后一行照原样重跑是复现不出来的**（注入点变成了仓库的
真实状态）。xl-knp.10 之后**换 `shop` 举例也复现不出来了**：四支驱动器全接上，
一条装配不出来的剧本都没有。判据本身没变，重跑要**反着造**（把某一支从
`IMPLEMENTED_DRIVERS` 里摘掉），2026-09-09 逐条真跑过的读数见下面「一条都不剩
之后」那张表。两个方向在 xl-6lo.14 那天各自真跑过一次，读数附在每行后面。

| 伪造的假象 | 怎么造 | 结果 |
|---|---|---|
| 「商店已经实现了」（xl-6lo.14 实测） | 往 `IMPLEMENTED_DRIVERS` 里加 `'shop'` | `pnpm typecheck` 报 **TS2741**（`Property 'shop' is missing in type '{ scene; battle; menu }'`）；`unassembled.test.ts` 报「shop-trade 的表态还写着 unassembled（一帧都比不了），可取图页已经装得出 driver=shop 了」 |
| 「菜单其实还没实现」（xl-6lo.14 实测的反向） | 把 `'menu'` 从 `IMPLEMENTED_DRIVERS` 里删掉 | `pnpm typecheck` **3 条 TS 错**；`unassembled.test.ts` 红，报「menu-equip 的表态是 gap，但取图页装配不出 driver=menu（取图页实现了：battle、scene）」 |
| 「表态忘了跟着改」（xl-6lo.14 实测） | 把 `expected.ts` 里 `menu-func` 的表态改回 `unassembled` | `unassembled.test.ts` 红两条，报「menu-func 的表态还写着 unassembled（一帧都比不了），可取图页已经装得出 driver=menu 了」 |
| 「这份真值是场景导出的」 | 把 `tools/traces/compare/shop-trade/java/frames.json` 的判别字段改成 `"scene"` | 退出码 **2**，表态与判别名对不上 |
| 同上，改的是 trace 那一头 | 把 `…/java/trace.json` 的判别字段改成 `"battle"` | 退出码 **2**，`帧清单说 driver=…，旁边那份 trace.json 说 driver=battle` |

**改判别字段必须绕开 `tools/compare-frames.sh` 直接跑
`pnpm exec vite-node scripts/compare.ts -- <剧本> --skip-capture`** —— 那个外壳
会重跑 Java 导出，把手改的东西覆盖掉（这条坑本身记在 `docs/agents/dispatch.md`）。

顺带三处"失败长得像成功"，都在这一趟里改掉了：只跑装配不出来的剧本时原先印
「0/0 条剧本符合预期」（读起来跟全过一样，现在印「一条剧本都没比成」）；
`--self-check` 在没有可比剧本时原先空循环全过（现在硬失败）；`frames.json` 缺
`driver` 字段时原先一路走到装配才炸（现在读清单时就报）。

### 接线不在 xl-1vu 那张票里，四支现在都接上了（xl-rh9.9 / xl-6lo.14 / xl-knp.10）

**非场景驱动器的跨端逐帧比对，要等各自的里程碑在 web 侧把面板建起来才接得上：**
战斗 **M2 / xl-rh9.9（已接）**、菜单 **M3 / xl-6lo.14（已接）**、商店
**M4 / xl-knp.10（已接，2026-09-09）**。
`xl-1vu` 这个 SPEC **不做接线** —— 它交付的是真值与"装配不出来必须响亮"这条
判据。面板做好之后要动的是两处：`implemented.ts` 里加判别名（装配表跟着补，
不补就编译不过），以及 `expected.ts` 里那条剧本的表态换成 `match` 或**真量出来的**
`gap`；只改一处的话上面那套对撞会红。

上面那一段实测输出是 xl-1vu.7 当天的（2026-09-06），**战斗与菜单那几行都已经
过期**：`battle-min` / `battle-em3-box` / 三条打输剧本、以及五条 `menu-*`
现在都真的在比。**别照抄"还剩几条装不出来"这个数** —— 它已经过期过两次；
照原样重跑 `tools/compare-frames.sh --self-check`，报告最后那一段自己会数
（**别照抄下面这个数** —— 它是 2026-09-09 的一次读数，当天就变过一回：
`shop` 那一支从 1 条变成 3 条。照原样重跑 `docs/trace-format.md` 里那条
`for f in tools/traces/scripts/*.json` 现数，装不出来的就是 `driver` 不在
`IMPLEMENTED_DRIVERS` 里的那几条。）

### 一条都不剩之后：两处判据换了素材（2026-09-09，xl-knp.10）

四支全接上，`status: 'unassembled'` 这个取值**在仓库里一条都不剩**。它不是废了 ——
新驱动器落了真值而还没接线时照旧要走它 —— 但有两处判据原先拿"磁盘上还剩几条"
当素材，那两处必须换，否则一处恒红、一处**恒真**：

| 在哪 | 原先 | 现在 |
|---|---|---|
| `expected.test.ts`「unassembled 不许带量出来的数」 | `expect(unassembled.length).toBeGreaterThan(0)` —— 从此恒红 | 手签的登记 `toEqual([])`，与旁边那条 `unpainted` 同一个套路，两头都会红 |
| `unassembled.test.ts` 那一族篡改 | 从磁盘上 `find` 一条没接线的剧本当素材 | **合成素材**：`ghost` 驱动器 + 临时注册进 `EXPECTED` 的 `ghost-script`，分母写死 |

第二处不换的后果是**最容易被读错的那一种**：`find` 挑不到时返回 `undefined`，
四条篡改会一起变成对 `undefined` 的断言——两条报「剧本 undefined 没有表态」、
两条直接 ENOENT。它既不是判据在验，也不长得像"判据失灵"。合成素材那一族还多带
一条自检（`ghost` 万一哪天成了真驱动器名就红），否则它自己也会静静地失去分辨力。

⚠️ 「装不出来的每一条都点得出驱动器与归属票号」那一条同理：那个 `filter` 今天是
**零轮**的，所以分母改成了一条合成的处境，磁盘上那个循环退成附加项。

### 篡改验证（2026-09-09，xl-knp.10，每一条都真跑过）

| 伪造的假象 | 怎么造 | 结果 |
|---|---|---|
| 「商店其实还没实现」 | `IMPLEMENTED_DRIVERS` 里摘掉 `'shop'` | `pnpm typecheck` **3 条 TS 错**；`unassembled.test.ts` 红，报「shop-categories 的表态是 gap，但取图页装配不出 driver=shop（取图页实现了：battle、menu、scene）」 |
| 「表态忘了跟着改」 | `shop-trade` 的表态改回 `unassembled` | 红**两条**：`unassembled.test.ts` 报「表态还写着 unassembled，可取图页已经装得出 driver=shop 了」，`expected.test.ts` 那份手签登记报「expected [ 'shop-trade' ] to deeply equal []」 |
| 「这块没人声明过的差异藏在缺口区底下」 | 删掉 `coins` 那个缺口区 | 退出码 1，报「硬比区破了：第 0 帧起有像素超容差，40 帧合计 21847 个；最坏的是第 0 帧 650 个 @ (905,4)-(971,19)」 |
| 「上界随便填一个」 | `coins` 的上界 1300 → 600 | 退出码 1，报「缺口区超了上界：coins 第 0 帧 650 个 > 上界 600（超了 50 个，1.08 倍）」 |
| 「账还完了却留着写 0」 | 把 `coins` 那个区挪到一块从不差的地方 | 退出码 1，**两头一起报**：硬比区破了（原处那 650 个露出来）+「缺口区 coins 一帧都不差了 —— 账还完了而表没改」 |
| 「动画帧号无所谓」 | `SHOP_FROZEN_FRAME` 0 → 1 | 退出码 1，硬比区 40 帧合计 **232038** 个超容差像素。原版侧那条线程整次导出停在第 0 格，喂别的帧号两端立刻对不上 |
| 「`ip` 那一列不用透传」 | `slimTrace` 里去掉 `ip: tick.ip` | 退出码 **2**，取图页第 0 步就抛「第 0 步的 ip=undefined 指向剧本第 undefined 条」。这一条是**真踩出来的**，不是事后补的篡改 |

## 商店接上之后量到的（2026-09-09，xl-knp.10）

三条商店剧本（`every: 1`，共 101 帧）一次就落到**只差字形**：

| 剧本 | 帧数 | 硬比区 | 六个缺口区的「单帧最多 / 上界」 |
|---|---|---|---|
| shop-trade | 40 | **逐像素相等** | 14113/28226 · 18570/37140 · 3520/7040 · 650/1300 · 545/1090 · 6132/12264 |
| shop-categories | 25 | **逐像素相等** | 14113/28226 · 18565/37130 · 3520/7040 · 650/1300 · 545/1090 · 8041/16082 |
| shop-edges | 36 | **逐像素相等** | 14113/28226 · 18570/37140 · 3520/7040 · 650/1300 · 545/1090 · 8152/16304 |

六个区（矩形与理由在 `expected.ts` 的 `shopGaps`）：商品名与购买件数、价目与
存货、背包持有量、右上角金钱、左上角四行属性标签、底部店主那两三行话。
**成因只有一族 —— 字形**（xl-9bd.17）。菜单那边的另外两族（素材没进烘焙管线、
ADR-0001 签过字的偏离）在商店这边都是零。

三件量的时候才知道的事：

- **矩形不是照 `shop/layout.ts` 的常量写的，是量出来的。** 量法留成了
  `web/scripts/measureGaps.ts`：第一档把两侧位图逐帧对齐、取超容差像素的并集、
  连通聚类，交出每一块的外接框（三条各聚出 75/76 块）；第二档拿划好的矩形回核，
  逐区报「单帧最多」并**把落在所有区之外的差异像素点出来**。
- **价目与存货切不开**：价目画在 `x+180`=633，五位数（灵神天药 15000）一直画到
  732；而存货画在药店 `x+245`=698、装备店 `x+260`=713，**落在价目那一列里面**。
  缺口区不许重叠，所以它们合成一个区 —— 两列是同一族账，合并不混成因。
  商品名（453）与购买件数（553/555）之间只隔 3 px，同理合并。
- ⚠️ **「区外零差异」与「两端都什么都没画」长得一模一样**，所以做了反证：逐块回
  原版位图数「整条剧本里变过多少像素」（`shop-trade`，两侧各数一遍）——商品图
  (820,200) 22910、招牌 (200,0) 3914/3916、店主动画 (364,515) 3898/3897、
  张小凡动画 (0,0) 360。硬比区里真的有东西画过，而且两端画的是同一批。

两条**这条流水线守不住**的东西，写在这里免得被当成已经守住了：

- **那八格动画怎么循环**。`ShopDriver` 把 Clock 倍率设成 1e-9，动画线程整次导出
  停在第 0 格（`replay/main.ts` 的 `SHOP_FROZEN_FRAME`）。守它的是
  `shop/render/animation.test.ts`（xl-knp.9）。
- **陆雪琪 (0,160) 与文敏 (0,320) 那两条动画**。三条商店剧本的 `setup.party`
  都只有 `zhang`，那两个位置在这条流水线里一帧都走不到。要盖住得另补一条 party
  更满的商店真值 —— 那属于补真值那一层。

## 战斗接上之后量到的（2026-09-07，xl-rh9.9）

`BattlePanel.paint()` 那 25 层照序画出来之后，五条战斗剧本一次就落到
**"只差状态栏那几行字"**：

| 剧本 | 帧数 | 最差帧 | 上界 | 包围盒 |
|---|---|---|---|---|
| battle-min（分区） | 17 | 硬比区**逐像素相等**；三块文字区 1825 / 1829 / 1696 | 3650 / 3658 / 3392 | — |
| battle-em3-box | 30 | #50 0.9467% | 1.89% | (92,547)-(971,612) |
| battle-defeat-scene | 11 | #50 0.8916% | 1.78% | (92,547)-(971,612) |
| battle-defeat-start | 8 | #25 0.4640% | 0.93% | (105,547)-(629,612) |
| battle-defeat-slot2 | 5 | #25 0.5193% | 1.04% | (105,547)-(629,612) |
| battle-victory（分区） | 34 | 硬比区**逐像素相等**；六块文字区 2047 / 2045 / 1612 / 453 / 1683 / 1033 | 4094 / 4090 / 3224 / 906 / 3366 / 2066 | — |

`battle-min` 的硬比区逐像素相等，意思是：背景图、开场云雾、行动条与七颗小
头像、三个人的走图、三只怪的走图、四颗指令按钮（**连哪一颗亮着**）、指示图、
技能动画的每一帧、被击动画、飘起来的伤害数字、怒气槽、血条与灵力条、游标，
全部对上。剩下的只有状态栏那九行字的字形 —— 与 dorm-intro / dorm-walk 是
**同一笔账**（原版 `文鼎粗钢笔行楷` 未交付，xl-9bd.17 已裁定不打包字体）。

三个缺口矩形是**量出来的**：17 帧合起来，超容差的像素落在六段列区间
（92–168 / 240–327、414–490 / 562–649、736–812 / 884–951）与三段行区间
（547–561 等级、578–592 血、598–612 灵力）上，矩形按这个分布裹。

三条打输剧本各有**一帧差 0 个像素** —— 末帧全灭图从两侧对开到合拢、把整屏
盖住。那是第 24 层那两个 1:1 源矩形唯一的正面判据。

### 结算画面那一层（xl-rh9.13）

`battle-victory` 是第一条走到**结算之后**的剧本（837 步，一路记到原版自己
`switchTo("scene")`）。第 22 层画出来之后实测（`--every 25`，34 帧，容差 8）：
**硬比区 34 帧逐像素相等 —— 六个缺口区之外超容差的像素合计 0 个**。

也就是说卷轴从 y=80 往下拉开的每一格、物品框从中心对开的每一格、第一页三张
人物图、第二页那一张（只有升了级的陆雪琪）、升级小图、「获得物品」图，全部
逐像素对上。剩下的差异**只有字**，而且分成两处：状态栏那九行（与 battle-min
同一块地方），加上结算画面上的三栏数字 ——

    行 137–211 列 695–764   掉落物三行 + 「金钱 N」（x=690）
    行 177–440 列 532–580   每人两行经验数字（x=532）
    行 368–440 列 502–521   第二页那四行属性数字（x=502）

三个新缺口区就是按这个分布裹的。**上界一律实测 ×2**，与前五条同一条规矩。

两次像素级篡改（绕开外壳直接跑 `pnpm exec vite-node scripts/compare.ts --
battle-victory --skip-capture`，理由见「验证动作本身的坑」第 3 条）：

| 篡改 | 结果 |
|---|---|
| 把 `victory-loot` 那个矩形横着挪 100 px | 退出码 1，**两头一起红**：「硬比区破了：第 750 帧起…最坏 1033 个 @ (695,137)-(764,211)」+「缺口区 victory-loot 一帧都不差了」 |
| 基准侧第 775 帧换成全黑 1024×640 | 退出码 1，最差帧 1.2260% → **98.5999%**，硬比区破 + **六个缺口区全部超界**（2.11–5.07 倍） |

### 篡改验证（2026-09-07 实测，14 次，每一次都先确认篡改真的写进去了）

接上一个新驱动器之后，"这条流水线还在验东西吗"必须重新问一遍。逐条报数：

| # | 篡改 | 变红的判据 |
|---|---|---|
| 1 | `BATTLE_LAYERS` 里对调 instruct / reminder | drawList「逐层同序」（次序是从 GBK 源码现解的） |
| 2 | 只改真实绘制顺序、名单不动 | drawList「层次序是 BATTLE_LAYERS 的子序列」 |
| 3 | 抹掉按钮命中框的 −15 / −6 | hitBox 4 条（边界横 / 纵、偏移不是装饰、与 `step.ts` 交叉验证） |
| 4 | 帧号反推规则挑错（走图那类改用「回第一张」） | frames 全部「绕回不动那一类」（8 个长度） |
| 5 | 缺口区上界调到比矩形面积还大 | expected「上界必须够得着」 |
| 6 | 名单里摘掉 `battle`（表态还写 gap） | `pnpm typecheck` TS2353 + unassembled 三条 |
| 7 | battle-min 表态改回 `unassembled` | unassembled 两条 |
| 8 | 把 `panel-text-yu` 矩形挪开 100 px | 比对「硬比区破了」，退出码 1 |
| 9 | **基准侧第 50 帧换全黑**（分区那条） | 0.8102% → **99.1631%**，硬比区 + 三个缺口区全超界，退出码 1 |
| 10 | **基准侧第 25 帧换全黑**（整屏那条） | 0.4640% → **99.5995%**，「差得比声明的多，**107.33 倍**」，退出码 1 |
| 11 | 真的画错一处：状态栏格距 322 → 320 | 硬比区破 + 两个缺口区超界，退出码 1 |
| 12 | 把 background-anim 从「盖不到」挪进「盖到了」 | 层覆盖登记两条 |
| 13 | 按钮贴图的 `onHit`：`command:` 那一支写成「按下」 | drawList「第 200 拍『击』还亮着上一轮松手留下的贴图」 |
| 14 | 同上，`enemy:` 那一支写成「松开」 | **绿的** —— 见下面那一段 |

**第 14 行是绿的，而那不是判据失灵。** 按钮贴图那段代码有两支：点按钮那一路
最后走「松开」，点怪物那一路最后走「按下」。后一支今天**观测不到** —— 点怪物
的坐标离四颗按钮都很远，四颗全落到"落空"那一支上，写「按下」还是「松开」推出
来的东西完全相同。这一条是**先预测、后验证**的：改写那段代码时就把「这一支
今天观测不到」写进了注释，然后跑一遍确认它确实绿。

本仓库的规矩是每一条没变红的篡改都要往下追一步，分清是判据失灵还是这一场
观测不到（`docs/agents/dispatch.md` 那一节，xl-rh9.7 一张票里撞到 9 次、
一次都不是判据失灵）。这是第 10 次。

第 9、10 两行是**反向**那一条：上一节记的全黑实测是场景侧的旧账
（`dorm-walk`），战斗侧这两条是重新量的。第 11 行值得单看 —— 它不是改判据，
是**真的画错一处渲染**，证明这套比对验的是渲染器本身，不只是表格自洽。

⚠️ 改基准侧的图必须绕开 `tools/compare-frames.sh`（它会重跑 Java 导出把手改的
东西覆盖掉），直接 `pnpm exec vite-node scripts/compare.ts -- <剧本> --skip-capture`。

⚠️ **篡改之后不要用 `git checkout -- <文件>` 还原**：同一个文件里尚未提交的新增
代码会被一并抹掉，`reflog` 救不回来（它从没进过对象库）。先 commit 再篡改，
或者 `cp 文件 文件.bak`。

### 接线那天撞到的两件事，都是判据自己抓出来的

**1. 两张 Pixi 画布上下摞着。** 注册战斗装配之后跑第一轮全量（剧本按字典序，
`battle-*` 排在最前），五条**场景**剧本的最差帧从 0.74%–37% 一齐跳到
**99.5%–99.99%**，`--self-check` 同时报「最先变的是 #0，不是注入点」。原因是
两套装配各建一个 `Application`、各往 `#host` 里塞一张 canvas，而 `replay.html`
的 CSS 给 `#host canvas` 定死了 1024×640 且 `display:block` —— 第二张被推出
视口，截到的根本不是场景那张。改成**每套装配一个自己的宿主 div、同一时刻只
显示一个**。修好之后五条场景剧本回到 37.07% / 34.08% / 2.8482% / 0.7401% /
29.18%，与本文上面记录的数一致。

这一条同时被两道判据抓住：整屏表态的 `maxRatio`（xl-l3o 立的那条上界）与
`--self-check`。**加一个驱动器会不会碰坏已经接好的那几个**，从此有了答案。

**2. 自检的注入层要挑一层"每帧都在"的。** 战斗侧原先照场景那边挪「主角」层，
`battle-defeat-start` 与 `battle-defeat-slot2` 报「改坏了却没响，一帧都没变」。
追下去**不是判据失灵**：那两场三个人都倒下了，第 7 层整层不画（画的是第 8 层
死亡动画），末帧还被全灭图整个盖住 —— 挪一层没画出来的东西当然不会变。改成挪
第 1 层背景图之后五条全部「首个变化帧 = 注入点」。

## 自检：故意改坏一处渲染

每条剧本都红着的时候，一条恒红的流水线与一条恒绿的流水线一样没有诊断力，
而两者都能安安静静地退出 0。`--self-check` 是这件事的可执行答案：

从采样帧中间那一帧起，取图页把主角**画偏 8 像素**（世界状态一个字节不动，
坏的只有画出来的那一帧），然后断言两件事——注入点之前每一帧的差异逐帧不变，
以及差异第一次变化的帧号**正好是注入点**。

实测（2026-09-06，dorm-walk，22 帧，每 25 个 tick 取一帧）：

```
流水线自检：故意改坏一处渲染
  取图 dorm-walk：22 帧（第 275 tick 起故意改坏渲染）
  通过  dorm-walk    22 帧 · 注入点 #275 · 首个变化帧 #275 · 变了 11/22 帧
```

把注入的位移改成 0（等于"没改坏"）之后它确实会红，报的是
`首个变化帧 无（改坏了却没响）`——这条自检本身也验过它的失败态长什么样。

## 第一件被量出来的事：原版画地图是拉伸的

流水线跑起来的第一天就量到：宿舍第 0 帧两端 **83%** 的像素不同，而画面用眼睛
看完全一样。

原因在 `src/scene/Map.java`：`isStatic` 是 Java 的布尔默认值 `false`，而它唯一的
赋值（`row > 20 || col > 32`）也是赋 `false`，所以

```java
g.drawImage(image, 0, 0, 32 * 32, 20 * 32,
            firstTileX * 8, firstTileY * 8, lastTileX * 8 - 8, lastTileY * 8 - 8, scene);
```

这条**拉伸**分支恒成立，`g.drawImage(image, 0, 0, scene)` 那条 1:1 分支是死代码。
宿舍的视口是 `firstTile(0,0)` / `lastTile(128,80)`，源矩形因此是 `(0,0)-(1016,632)`，
被拉到 1024×640，放大 1.0079 倍。实测 x=1000 处两端错开 8 像素、x=10 处错开 0，
正是这个比例。

Web 侧现在把地图 1:1 贴上去，于是整屏都差一点点。这不是"资产不一致"——
`maps/宿舍.png` 与烘出来的 `宿舍.webp` **解码后逐像素相同**（无损 WebP，已实测：
浏览器把 WebP 贴成 `<img>` 截下来，与源 PNG 的 655360 个像素一个不差），
差的是画法。跟票：`xl-9bd.16`。

**不要去"修好"原版**——迁移期间原版是规格。

## 时间加速

两端都能按倍率加速，且**加速不改变游戏行为**：

- Java 侧是 `tools.Clock`，把散在 16 个文件的 30 处时间常数收拢成一个可缩放量；
- Web 侧是 `state/loop.ts` 的 `Ticker.timeScale`，倍率乘在**真实流逝的毫秒**上。
  这一侧不需要收拢时间常数，因为时间本来就是 `step()` 的入参。

Web 侧的倍率**不乘在 tick 步长上**。那是最顺手的写法，但会跳过定时器的触发
时刻，主角走的距离立刻对不上真值。判据写成了一条不变量：**k× 跑 T 毫秒，
与 1× 跑 k·T 毫秒得到逐字段相同的世界**（`src/state/loop.test.ts`，四个倍率）。

墙钟实测（`tools/speed-probe.sh`，2026-09-06，macOS / openjdk 17 / Node 24）：

```
Java 侧（tools.Clock.sleep，每个倍率量 2000 游戏毫秒）
  factor=1.0   真实  2306.3 ms  绝对倍率  0.87×  相对首行  1.00×
  factor=2.0   真实  1306.1 ms  绝对倍率  1.53×  相对首行  1.77×
  factor=5.0   真实   525.0 ms  绝对倍率  3.81×  相对首行  4.39×
  factor=10.0  真实   265.4 ms  绝对倍率  7.54×  相对首行  8.69×

Web 侧（state/loop.ts 的推进器，每个倍率量 400 真实毫秒）
  scale=1     真实   403.9 ms  游戏    400 ms  实测倍率  0.99×  偏差 1.0%
  scale=2     真实   403.6 ms  游戏    800 ms  实测倍率  1.98×  偏差 0.9%
  scale=5     真实   402.6 ms  游戏   2010 ms  实测倍率  4.99×  偏差 0.2%
  scale=10    真实   404.2 ms  游戏   4040 ms  实测倍率  9.99×  偏差 0.1%
```

Java 侧的**绝对**倍率被 `Thread.sleep` 的固定超时开销压低（每次多睡几毫秒，
与倍率无关），所以那一列恒小于 1；回答"线不线性"的是**相对首行**那一列。
它与 `docs/MIGRATION-PLAN.md` §4 里那张战斗层面的表（1.00 / 1.86 / 3.84 / 7.41）
同一个量级。Web 侧偏差最大 1.0%，探针在超过 20% 时非零退出——加速封顶的
表现是游戏"跑得没那么快"，不报错，所以必须有人量。

**跨机器、跨 JDK / Chrome 版本的一致性未验证。** 上面每个数字都是这一台机器
上的一次实测。

## 加一条机制剧本要做什么

1. 往 `tools/traces/scripts/` 里写剧本（格式见 `docs/trace-format.md`）；
2. `tools/export-trace.sh --check` 导出并验确定性，产物入库；
3. 在 `web/src/compare/expected.ts` 里表态：现在该 `match` 还是 `gap`
   （`gap` 要写清楚差在哪、挂哪张票，并**跑一遍读出最差帧、按 2 倍写上界**）。
   漏了这一步 `pnpm test` 就红。
   缺口能圈成几块矩形的，就顺手写成**分区表态**（上面那一节）——剩下那大半张
   画布因此变成硬判据，而不是继续躺在一句"已知缺口"底下。

## 菜单四层接上之后量到的（2026-09-07，xl-rh9.12）

药品菜单 / 技能菜单 / 敌我两层战斗状态图标 / 提示图画出来之后，`battle-menus`
从 `unpainted` 换成了实测的整屏 gap：19 帧（`--every 25`，容差 8），最差帧
**#225 4.2029%**，上界 8.41%。

> ⚠️ **这一节的数字是 xl-rh9.12 那一趟量的，xl-7ip 之后已被推翻。** 2026-09-07
> 背景动画的编码从 `cwebp -q 80` 换到 `-q 95`（见下面「两笔新账」），
> `battle-menus` 的最差帧变成 **#125 0.9975%**、上界 1.9949%，#225 掉到
> 0.8199%。下面几处提到 4.2029% / 8.41% 的地方同理。**活的数字只有一处：
> `web/src/compare/expected.ts`**，那里逐帧的账是 xl-7ip 重量过的。这一节
> 留着原样，是因为它记的是「那一趟看见了什么」，不是「今天是什么」。

**为什么它不走分区表态**：分区那一套要求「所有缺口区加起来不超过画布的 40%」，
而这条剧本最大的一笔账是**满屏**的（背景动画，见下）。硬画成分区就是把整块
画布圈进缺口区，那正是那条 40% 想拦的作弊路子。

分辨力改由另一个量提供：把 battle-min 那三个文字缺口区（88..974 × 545..614）
扣掉之后，**每一帧状态栏之外还差多少个像素**。逐帧量出来是：

| tick | 整屏 | 状态栏之外 | 是什么 |
|---|---|---|---|
| 0 / 50 / 75 / 175 / 250 / 275 / 375..450 | 0.39–0.82% | **0** | 只有状态栏那九行字 |
| 100 / 150 | 0.86 / 0.98% | 313 / 1110 | 药品菜单六行存货数字，每行恒 53 个 |
| 125 | 0.9975% | 1221 | 介绍文字 845 + 存货 265 + 提示图 111 |
| 200 | 0.9352% | 818 | 背景动画（追星破月 第 2 帧） |
| 225 / 300 / 325 / 350 | 2.5–4.2% | 满屏 | 背景动画 |

> ⚠️ **最后两行的「满屏」被 xl-7ip 推翻了（2026-09-07 重量）。** 换 `-q 95`
> 之后这五帧的区外分别是 **13 / 62 / 58 / 11 / 5** 个像素，最差帧因此从 #225
> 换成 #125，而 #125 的 `anim.bgDrawn` 是 `false` —— 它那 1221 个区外像素与
> 背景动画毫无关系，重量前后一个都没变（上面前三行因此**仍然成立**）。
>
> 连带一件事：上面那句「这条剧本最大的一笔账是满屏的」**今天不再成立**，
> 所以「为什么它不走分区表态」的那个理由已经过期。要不要真改成分区表态是
> 另一件事 —— 药品菜单那几块得先各画一个缺口区，xl-7ip 没做。

**三条正面结论**，都是"差 0 个像素"换来的：

- **t=175 与 t=275 状态栏之外一个像素都不差**，而这两帧技能菜单开着（分别是
  文敏那组三颗与张小凡那组两颗）—— 背板、每颗按钮现在贴的那一张、返回按钮
  的位置、技能说明图，全部逐像素相等。t=275 尤其值钱：那一帧菜单画的是张小凡
  那组，说明图挂的却还是文敏上一轮鼠标扫过的那张（`SkillMenu.isDrawIntro`
  原版一旦置真再没清过），**这个原版缺陷也是逐像素对上的**。
- **t=375..450 状态栏之外一个像素都不差**，而那几帧第 3 槽那只怪身上挂着
  「体力下降」—— 第 18 层逐像素相等。第 17 层（我方）由 t=250 / 275 同理钉住，
  那几帧文敏挂着「敏捷提升」。
- 药品菜单那几帧之外的差异**只剩字**：六行存货数字每行恒 53 个、`hp 300 mp 0`
  那行 845 个，而背板、七颗按钮（第七颗返回的命名规则跟前六颗不一样）、
  金创药那张 120×220 的介绍图都不差。

### 两笔新账

**背景动画的有损重编码。** `image/背景动画/` 那 753 张源文件是 JPG，烘焙器对
非 PNG 走 `cwebp -q`（`bake.ts` 的 `toWebp`），于是那几帧满屏差一点点。
当时它是最差帧的唯一成因。**五条老剧本一次技能都没用过**，所以这笔账到那一趟
才第一次被看见 —— 同一个理由让 `battleTextureIds` 少推了菜单技能的动画帧
（原先只推普攻那一套），而"少推一条"的表现在那五条上是什么都没发生。

**xl-7ip（2026-09-07）把档位从 q80 提到 q95，这笔账缩了 30~60 倍但没还清。**
三件当时以为、后来实测推翻的事：

- 要改的是 **753 张**，不是 1770 张 —— `技能动画` 那 1017 张全是 PNG，本来就
  走 `-lossless`。
- **`-q 100` 到不了 0**（还剩几百个超容差像素）：cwebp 的有损档一律做 YUV420
  色度下采样，那一笔跟 `-q` 无关。归零只有 `-lossless`，而那要给仓库永久加
  89 MiB、单个技能的按需下载从 2.9 M 涨到 16.3 M，**裁定不换**。
- 六个档位的全量代价（753 张合计的字节数、最重的单个技能目录、四条剧本最差帧
  那张图里超容差的像素数）量在 `bake.ts` 的 `BATTLE_LOSSY_QUALITY` 头注里。

结果：四条用技能的剧本（`battle-menus` / `battle-{zhang,yu,lu}-skills`）的上界
从 8.41~13.98% 重量到 0.67~1.99%，而且**上界现在由字形那一笔（xl-9bd.17）
定着**，不再由素材编码定着。逐帧的账在 `expected.ts`。

**xl-9do（2026-09-07）又裁掉了 115 张画不出来的部分，四条上界一条没动。**
原版 `BackgroundAnimation` 把图整张画在 (0,0) 不缩放、画布 1024×640，所以
1240×744 那 115 张（伏虎冲天 74 + 追星破月 41）有 29% 的像素从没被画出来过，
烘焙时裁掉，按需产物 39160 KB → 37522 KB。

⚠️ 那张票原本假设「裁掉画不出来的部分是等价变换」，**实测推翻了**：cwebp 的
有损档先对整张图做分段分析（SNS）再定量化参数，输入变小就重掷一次骰子，
**可视区内的像素也会跟着动**，而且逐张有好有坏。曾经有一版把 753 张全裁了，
1066×639 那档掷坏了 —— `神剑傲州/31` 相对源 JPEG 的超容差像素 107 → 18139，
`battle-zhang-skills` 的最差帧从 0.2693% 顶到 **2.5040%**，超上界 3.76 倍，
这条流水线退出 1。**那是这四条上界第一次真的拦下一个回归。**
最终只裁面积省 25% 以上的那一档，逐档实测在 `bake.ts` 的 `backgroundAnimCrop`
头注里。

**xl-x6w（2026-09-08）加了 `-sns 0` 并把上面那条面积门槛删掉，改回「越界就裁」。**
`-sns` 是 cwebp 对整张图做分段分析的那个启发式，关掉它编码就变成局部的。
按需产物 37522 KB → **36729 KB**，753 张相对源 JPEG 的超容差合计 153588 →
142986（−6.9%；相对 xl-7ip 那条 q80→q95 之前的基线 178445 是 −19.9%）。
2×2 全量实测在 `bake.ts` 的 `BATTLE_LOSSY_SNS` 头注里。

⚠️ **上面这一段说「重掷骰子」的那件事没有消失，只是小了一个量级** —— xl-x6w
的票面写的是「抽签消失了」，依据是一张图（`神剑傲州/31` 在 `-sns 0` 下裁与
不裁是 67 vs 68），**全量 753 张实测推翻**：`-sns 0` 下裁与不裁仍有 348 张读数
不同、逐张 |Δ| 合计 25551，只是裁引起的最坏单张恶化从 **+18032 降到 +2869**。
门槛因此可以删，但 `-sns 0` 与「越界就裁」是**捆在一起**的一个决定。

⚠️ **本文开头「余量一律 2 倍」那条规矩，这一趟被有意破了两处，记在这里。**
`-sns 0` 是逐张抽签（753 张里 220 张变差、430 张变好），落到帧上
`battle-zhang-skills` 的最差帧从 #75 0.3333% 变成 #350 0.3589%、
`battle-yu-skills` 从 #875 0.6740% 变成 #175 0.7158%（源头是 `龙翔九天/15`
16→671 与 `苍龙盖天/22` 30→1511，两张都**不是裁出来的**）。照「×2」重算这两条
上界会**放宽**到 0.7178% / 1.4316%，而 xl-x6w 的票面要求「上界只许变紧不许
放宽」。**2026-09-08 用户裁定：保持原上界不动**，于是这两条的余量是
**1.86× / 1.88×**，不是 2×。`battle-lu-skills` 的最差帧变好了，上界照规矩
收紧到 1.2579%；`battle-menus` 的最差帧一个像素没动（#125 是药品菜单那一帧，
不放背景动画），上界照旧。

**提示图缩放时的取整平局，111 个像素**（只在 t=125）。提示图是战斗里唯一真的
在缩放的一层（源恒为 128×24，目标由 `Reminder.update()` 每拍张开，这一帧是
80×16，k=1.6）。差异图上是一排**周期 5** 的竖条 —— 正是 `1.6i+0.8` 落在整数上
的那几列。

⚠️ **它不是采样网格整体错位，这一点是试出来的、不是推出来的。** 两个方向都
实测过：把精灵挪半个目标像素（`sprite.position + 0.5`）、把源矩形挪半个纹素
（`frame.x - 0.5·sw/dw`），各自把这一块从 111 个推到 **800 个以上**。两边都
变差说明 0 已经是局部最优，剩下的是两端在纹素边界上的取整谁也说服不了谁
（Java2D 的定点 vs GPU 的浮点）。再凑一个 ε 就是拿一条剧本的一帧去拟合。

### 另外十条剧本一条都没变差

「接一条新剧本别把老的碰坏」这句话在这个仓库有过一次现成的教训（xl-rh9.9：
加一套装配让已经接好的驱动器截到了错的画布，最差帧从 0.74% 跳到 99.9%），
所以这一趟的整轮实测照抄在这里 —— `tools/compare-frames.sh --self-check`，
**11 条剧本 726 帧，11/11 符合预期**：

| 剧本 | 帧数 | 最差帧 | 上界 |
|---|---|---|---|
| battle-defeat-scene | 11 | #50 0.8916% | 1.7832% |
| battle-defeat-slot2 | 5 | #25 0.5193% | 1.0386% |
| battle-defeat-start | 8 | #25 0.4640% | 0.9280% |
| battle-em3-box | 30 | #50 0.9467% | 1.8934% |
| **battle-menus** | **19** | **#225 4.2029%** | **8.4058%** |  <!-- xl-7ip 之后：#125 0.9975% / 1.9949% -->
| battle-min（分区） | 17 | 硬比区逐像素相等；1825 / 1829 / 1696 | 3650 / 3658 / 3392 |
| bigmap-walk | 34 | #450 37.0726% | 74.2200% |
| dorm-exit | 48 | #875 34.0767% | 68.1500% |
| dorm-intro | 165 | #800 2.8482% | 5.7000% |
| dorm-walk（分区） | 22 | 硬比区逐像素相等；527 / 1397 / 4323 | 1054 / 2794 / 8646 |
| milestone | 367 | #8825 29.1754% | 58.3500% |

除 `battle-menus` 那一行是新的，其余十行与 xl-rh9.9 / xl-1vu.3 记的一字不差
（`bigmap-walk` 的最差帧从 #475 37.1101% 挪到 #450 37.0726%，是同一档的机器
抖动，两个数都在 74.22% 的上界之内）。场景那五条走的是另一套装配与另一个
注入点，这一票一个字都没动它们。

### 流水线那一层的篡改验证（2026-09-07 实测，都退出码非 0）

单元测试那一层的 17 次篡改在提交信息里逐条报了数（`pnpm test` 跑得到的那些）。
**上界与表态这两样 `pnpm test` 验不到** —— 它们只有真流水线说了算，所以单列：

| 篡改 | 结果 |
|---|---|
| `battle-menus` 的 `maxRatio` 压到 0.0001 | `差得比声明的多：第 225 帧 4.2029% > 上界 0.0100%（420.29 倍）`，退出码 1。**xl-7ip 之后同一条篡改真跑过（2026-09-07）**，报的是 `第 125 帧 0.9975% > 上界 0.0100%（99.75 倍）`，退出码 1 |
| `battle-menus` 的表态改成 `match` | `回归：第 0 帧起偏离`，退出码 1 |
| （改之前）`breakBattleOps` 只挪第 1 层 | `首个变化帧 #250`，不是注入点 #225，退出码 1 —— 见上面那一节 |

⚠️ 第一条与第二条在 `pnpm test` 里**都是绿的**，这不是判据失灵：上界与表态的
判据在 `verdict.ts` / `regions.ts`，而喂给它们的那两个数要真的开一次浏览器才有。
`pnpm test` 验的是这张表**自身**的形状（每条 gap 有没有上界、上界够不够得着、
缺口区重不重叠）。两层各管一头，缺哪一层都会让另一层看起来像失灵。

## 场景各条剧本换成分区表态（2026-09-10，xl-yg6.12，M5 收口）

11 条场景剧本（`driver` 缺省或写 `scene` 的那些，现数）全部从整屏 `maxRatio` 换成
**从差异图上量出来的**缺口分区（`web/scripts/measureGaps.ts`），最后一轮
`tools/compare-frames.sh`（1007 帧，容差 8）**11 条硬比区全部逐像素相等**。

### 量的过程里还掉的三族账，按露出来的先后

1. **大地图的有损重编码**（xl-9bd.14 那一族）。大地图.jpg / 大地图夜.jpg 走 q80，站在
   大地图上的帧满屏差 25%~38%，满屏的账切不成矩形。用户裁定改走 `-near_lossless 40`
   （只点名这两张，`bake.ts` 的 `MAP_NEAR_LOSSLESS`）：产物与 Java `ImageIO` 解出的源
   JPEG 逐像素比 0 个超容差、最大通道差 4，两张合计 +10066024 B。bigmap-walk 37.07% →
   0.60%，battle-door 38.56% → 1.13%。
2. **`OtherEvent.addMap` 那一层整层没画**。重编码一归零就露出来：右下角金币 HUD，加上
   大地图上十张建筑遮掩图。遮掩图跟着镜头卷动 —— battle-door 里它每帧 6495 个像素、
   从屏幕右下游到左上，并集铺满整屏，**同样切不成静态矩形**。用户裁定在本票里画上
   （`web/src/scene/mapOverlays.ts`，约 120 行）。之后图标与遮掩图逐像素相等，剩下的
   只有金币**数字**的字形（`coin-digits`，归 xl-9bd.17）。
3. **「藏在它底下的账」**：旁白背景第 599 行的缩放取整（xl-t0h）。此前每帧最多 264 个
   像素、低于判定阈值（≈131 个才算偏离帧的那条线对它是一帧一帧判的，而它一直藏在整屏
   上界里没人数过）；分区之后区外不许有一个像素，它就只能单独成区。

### 两处写明的例外（几何上分不开）

- `narratage-bg-row-599`：那一行同时横穿底部对话框第 4 行文字，旁白帧与对话帧从不同时
  出现，可矩形是静态的。
- question-answer / question-memory 的 `coin-digits`：字形 + **取图页不跑钱包导致数值
  不同**（原版答题后 9117 / 9493，web 取图页仍是 10000）。取图页（`replay/main.ts`）
  只调 `step()`、不走 session —— **凡是只由 session 记的账，这条流水线一律看不见**，
  钱包只是今天撞到的那一样（xl-yg6.15 现数了一遍）。

### 读数会漂的两个区

提示框红字滑入那一格（`reminder-text`）两轮之间读数不同：question-answer 2015 → 2231、
question-memory 2207 → 1046。那一格的内容随取样帧落在滑动的哪一拍而变，两轮都在上界
（3970 / 4414）之内。

### 篡改验证（2026-09-10，每一条先确认改动写进去了，cp 备份还原后 cmp）

| 改了什么 | 结果 |
|---|---|
| `mapOverlays.ts` 体育馆大门 x 918 → 917 | `mapOverlays.test.ts` 两条红，退出码 1 |
| `coinDigits` 矩形左沿 934 → 945 | bigmap-walk「硬比区破了：34 帧合计 636 个」，退出码 1 |
| bigmap-walk 加一块 (0,0)-(10,10) 的 `ghost` 区 | 「缺口区 ghost 一帧都不差了」，退出码 1 |
| `coinDigits` 上界 ×2 → ÷2 | 「coin-digits 第 825 帧 580 个 > 上界 290」，退出码 1 |
| 同一条命令、不篡改（对照） | 退出码 0，「1/1 条剧本符合预期」 |
| `sceneRenderer.ts` 拿掉 `drawOverlays` 那一句，重新取 bigmap-walk | 「硬比区破了：34 帧合计 24696 个；最坏第 600 帧 2506 个 @ (914,588)-(1012,635)」（正是体育馆门匾那张遮掩图）+「coin-digits 第 450 帧 1554 个 > 上界 1160」，退出码 1 |

最后一条是**这一层有没有被守住**的判据：`mapOverlays.test.ts` 只验摆位算得对不对，
渲染器按设计没有测试缝，真把这一层画没了，`pnpm test` 全绿 —— 只有逐帧比对会红。

## 存读档面板与读档剧本接上（2026-09-10，xl-i06.12，M6 收口）

**先验证了「不加装配会红」**（改动之前跑 `tools/compare-frames.sh saveload-menu saveload-start`，
退出码 1，两条逐条点名 `driver=saveload` 与 `xl-i06.12`），再把 `saveload` 加进
`IMPLEMENTED_DRIVERS` 与取图页装配表。

### 原版存档从哪来

两条 saveload 剧本起手要草稿区那三个槽，三条读档剧本（场景剧本带 `load`）起手要那一份档读回来
的样子。**没有另写浏览器侧的原版存档读取器**：比对器在 Node 那一半用状态层判据的同一个读取器
解好（`web/src/compare/saveFixtures.ts` —— 读档那一路 `loaderReadBack`、写档装置那一路
`fromRecorderText`，后者的布局从 Java 源码现读、在浏览器里做不了），随剧本送进页面。
取图页的读档起手走产品那一路的 `applyReadBack`（钱也落进钱包，金币 HUD 画的是存档里的数）；
存读档面板的起手与逐步推进与状态层判据共用 `web/src/saveload/replay.ts`。

### 量出来的分区（矩形与读数在 `expected.ts`）

| 剧本 | 帧 | 缺口区 | 硬比区 |
|---|---|---|---|
| saveload-menu | 9 | thumb-0/1/2（xl-cpo）· map-name-0/1/2 · task-0/1/2（字形） | 逐像素相等 |
| saveload-start | 5 | thumb-0（xl-cpo）· map-name-0/1/2 · task-0/2（字形） | 逐像素相等 |
| load-slot0/1/2 | 8/11/9 | coin-digits（字形） | 逐像素相等 |

- **缩略图整块挂 xl-cpo，不在本票拟合。** 大迷宫.png 那一格两端逐像素相等（反证：原版那块
  4655 种颜色、空槽位置 144 种），所以不声明；读数记在 xl-cpo 上。
- **追出来的第三族：地图图片与碰撞网格不等。** load-slot2 读进 脚本20，渲染器原先对「图 ≠
  网格 × 32」一律拒绝渲染。现数 96 个场景里 22 个对不上：图比网格大（大迷宫.png）的画法与相等
  时相同，放行（`web/src/scene/mapSize.ts`）；图比网格小的 20 个另立 **xl-i06.14**，照旧拒绝。
  load-slot2 硬比区逐像素相等是「放行得对」的读数。
- 取图页每条场景剧本起手重置钱 / 药 / 队伍单例 —— 一个页面连着装好几条剧本，读档剧本写进
  钱包的数会画到下一条的金币 HUD 上。

### 读数（2026-09-10，本票最后一轮）

- **全量** `tools/compare-frames.sh --self-check`：38 条剧本 × 各自帧数 = **1651 帧**，
  **38/38 条剧本符合预期**；五条新接的硬比区全部逐像素相等（区外 0 个）。
- 自检：`saveload-menu` 注入点 #4 → 首个变化帧 #4（变了 5/9 帧），`saveload-start` #2 → #2（3/5），
  `load-slot0/1/2` #100/#125/#100 → 同帧。
- ⚠️ 同一轮自检里 `question-memory` 报「首个变化帧 #1200，不是注入点 #1225」。追下去：#1200 两次
  取图差 146 个像素，全在 `reminder-text` 那一格（提示框红字）；**基线 95593e3 上连跑 3 次也失败
  1 次**，不是本票引入的，立了 xl-2e0。所以全量 `--self-check` 的退出码此刻偶发为 1。

### 玩家侧此刻走得通哪一段（SPEC 要求收口时写明）

- 画面这一层：存读档面板、以及读档之后回到的三个场景，与原版逐像素只差缺口区里那几族。
- **这条流水线守不住的**：取图页只走起手 + `step()`，不跑会话；存档是否真的写进浏览器存储、
  关掉浏览器再开是否还在，这里一帧都没验（那归 `save/browserStore.ts` 与 xl-i06.8 的判据）。
  跨面板端到端（从场景一路存、读、再走回来）仍是 SPEC 单立的后续票，未做。
- 取图页起手重置钱包那一句（`replay/main.ts`）**没有任何判据守着**：拿掉它、让读档剧本的钱漏到
  下一条剧本，金币数的数值变了，而 `coin-digits` 那一格的上界按字形那一族留的 2 倍照样兜得住
  （实测 load-slot0 后接 dorm-walk：380/1040，照样通过）。产品路那一侧（`applyReadBack` 写钱）
  由状态层三条单元测试守着。

## 图比碰撞网格小的那批场景放行（2026-09-10，xl-czb.3，还 xl-i06.14 的账）

xl-i06.12 给「地图图片 < 瓦片数 × 32」加了一道有意的硬失败。现数（读数不是规格，
`web/src/scene/mapSize.test.ts` 每次现扫 `tools/ground-truth/` + `maps/` 原始素材）：
短 20 / 长 2 / 相等 74；主线（`脚本1` 起顺 `nextScript` 走，45 本）上落在名单里的 8 本，
最早排第 9（`脚本7`）。

### 先量原版：源矩形根本取不到缺的那几像素

测法：一个一次性探针，用原版 `tools.Reader.readImage` 读图（ImageIcon），照 `Map.drawMap`
那条 `drawImage(img, 0,0,1024,640, sx1,sy1,sx2,sy2)` 画进一块先填满哨兵色 `0xFFFF00FF` 的
1024×640 `TYPE_INT_ARGB`（与 `ScenePanel.backImage` 同型），数哨兵残留，并逐像素对最近邻公式
`floor((i + 0.5) × 源 / 目标)`。

| 源矩形 | 图 | 哨兵残留 | 公式不符 |
|---|---|---|---|
| 实际的 (0,0)-(1016,632) | 6 张短图 + 宿舍.png 对照 | **0** | **0**（最大源列/行 1015/631） |
| 故意越界 (0,0)-(1024,640) | 仙一教学楼一楼.png 1022×640 | 1280（x ≥ 1022 两整列） | 0 |
| 同上 | 教室2.png 1023×639 | 1663，最左一个在 x=0 | 0 |
| 同上 | 基础实验楼乙五层.png 1024×639 | 1024（y=639 一整行） | 0 |

32×20 的静止地图 `lastTile` 被夹成 128/80（三条新真值的 `viewport` 列逐拍都是这个值），
源矩形最远只到 **网格 − 8**。所以这批场景原版的画法与相等时逐像素相同，**渲染器不加任何
拉伸 / 补边 / 夹取的分支**，只把 `checkMapSize` 的门槛从「网格」改成「源矩形最远那一格」。
比源矩形还小的那一种仍然硬失败 —— 反事实那几行说明原版在那里的画法不同（而且不是简单地
少画一条边），今天没有场景走得到，没有复刻。

### 判据：三条剧本的硬比区

每种缺法一条，只站着等（判据是地图边缘，不是走路）：`mapshort-width`（仙一教学楼一楼）/
`mapshort-both`（仙二205夜，教室2.png）/ `mapshort-height`（基础实验楼乙五层，主线 脚本7 /
7+ / 26 同一张图）。三条都只剩 `coin-digits`，地图右 / 下边缘在硬比区里：

| 剧本 | 帧 | 硬比区 | coin-digits 最差 / 上界 |
|---|---|---|---|
| mapshort-width | 4 | 逐像素相等 | 575 / 1150 |
| mapshort-both | 4 | 逐像素相等 | 586 / 1172 |
| mapshort-height | 4 | 逐像素相等 | 493 / 986 |

「比网格大」那一支没被碰坏：同一轮 `load-slot2` 9 帧硬比区逐像素相等（coin-digits 500/1000）。

⚠️ `mapshort-both` 走夜版而不是 `仙二205`：后者第 4 个 NPC 少一个字段，原版 `Reader` 抛
`ArrayIndexOutOfBoundsException` 并被它自己的 `catch` 吞掉、场景照样进得去；web 侧
`state/npc.ts` 对它当场抛（注释说原版会崩，与实测不符）。与地图无关，立了 **xl-d8u**。

### 篡改：把放行改成「拉伸整张短图」，前后对照

篡改点：`sceneRenderer.ts` 的 `place()` 里，图比网格小时把 `lastTileX/Y` 改成让源矩形等于
整张图（1022 / 639）—— 票面点名禁止的那种「看起来对」的兜底。`cp` 备份、`cmp` 还原。

- **改动后那版 + 篡改**：3/3 红，全落在硬比区 —— width 最差帧 286482 个、both 487172 个、
  height 345175 个超容差像素；width / height 的 coin-digits 还各超了上界。
- **改动前那版（master 的 `mapSize.ts`）+ 同一条篡改**：取图页在 `checkMapSize` 当场抛，
  `vite-node scripts/compare.ts` 退出 2 —— **与它不篡改时一模一样**。也就是说旧版分不出
  「拉伸了」与「照原版画了」，两者都是抛；新版不篡改全过、篡改全红。

### 收 /code-review 之后补的（同一天）

**探针入库**：`tools/src/devtools/MapEdgeProbe.java`，头注里是那条命令（从仓库根目录跑，
`tools/build.sh` 之后）。按它重跑，上表每一行逐字相同；缺图时退出 1 并点名 —— ImageIcon 缺图
给的是 -1×-1 的空壳，不拦的话会读成一行正常的「哨兵 0」。

**SPEC 的前提被量出来不成立，照实写下。** SPEC 说拉伸 / 补边 / 夹取「与原版的差别正好藏在那
一两列里」。实测是**那一两列原版根本不取**，所以「把图补边到 1024」或「采样坐标夹进图内」在这里
**画出来与原版逐像素相同** —— 不是判据看不见，是它们本来就不改行为（dispatch「篡改本身没改
行为」那一族）。票面 AC「改成拉伸（或补边）确认红」因此只有**拉伸**那一半成立；补边那一半
换成了下面这条真正会改画面的「边缘少贴两像素」。

**只动边缘的篡改**（「边上有一两列怪怪的」那种画面）：图比网格小时，贴到最右 / 最下的那几块
碎片各少贴 2 px，把画布右 / 下边缘的两列 / 两行留成底色。

- **改动后那版 + 篡改**：3/3 红，全在硬比区 —— width 4 帧合计 7992 个（最坏帧落在
  (0,638)-(998,639)）、both 120 个（最坏帧 30 个 @ (1022,591)-(1023,615)）、height 13296 个。
- **改动前那版 + 同一条篡改**：取图页在 `checkMapSize` 抛、退出 2，与不篡改时一样。

⚠️ **观测极限，照实登记**：`mapshort-both` 只红了 30 个像素。教室2.png 右 / 下边缘那两列两行
**大部分是近黑的**，与底色的差在容差 8 之内 —— 那一段上「没画」与「照原版画了」逐像素分不开，
判据只在边缘恰好有颜色的地方咬得住。`mapshort-width` 的最坏帧也只落在下边缘两行、没落在右边缘
两列上，同一个原因。三条剧本合起来每种缺法都红了，但**单看一条剧本，不能读成「边缘每一个
像素都有判据守着」**。

**门槛只在 32×20 上有读数。** 短图名单里 20 个场景全是 32×20 网格；`网格 − 8` 对别的网格尺寸
是按 `computeViewport` 的夹取推出来的，没有真值剧本走进过别的尺寸的短图（今天也没有这样的
场景）。

**主线上几本、排第几**：没有代码现算它（那归「主线链与可达闭包」那张票的分析器）。今天的读数
由下面这段现算，从仓库根目录跑：

```bash
python3 - <<'EOF'
import json, glob, subprocess
D = {json.load(open(f))['script']: json.load(open(f)) for f in glob.glob('tools/ground-truth/*.json')}
def size(m):
    o = subprocess.run(['sips', '-g', 'pixelWidth', '-g', 'pixelHeight', 'maps/' + m], capture_output=True, text=True).stdout.split()
    return int(o[o.index('pixelWidth:') + 1]), int(o[o.index('pixelHeight:') + 1])
cur, i, hits = '脚本1.txt', 0, []
while cur in D:
    i += 1; d = D[cur]; w, h = size(d['mapName'])
    if w < d['col'] * 32 or h < d['row'] * 32: hits.append((i, cur))
    cur = d['nextScript'][2] if d['nextScript'] else None
print('链长', i, '落在短图名单里', len(hits), hits)
EOF
```

（macOS 的 `sips`；链从 `ScenePanel` 构造函数写死的 `currentScript[2] = "脚本1.txt"` 起。）

### 没做的

- `大迷宫1`（图比网格大的另一个场景）仍然没有真值剧本走进去过，放行是按同一条推理。
- 比源矩形还小的那条画法（反事实那几行）没复刻，走不到。
- `仙二205` 在 web 侧仍然当场抛（与地图无关），见 xl-d8u。
