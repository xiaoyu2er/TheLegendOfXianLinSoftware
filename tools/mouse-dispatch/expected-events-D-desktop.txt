# 落点 = 露出来的桌面时，**D 组**（xl-8eg 那五段序列）按下 / 松手的实测读数。
# tools/mouse-dispatch-probe.sh 在 `--where desktop` 上拿它单独对账。
#
# 读数出处：xl-23v，macOS 24.6.0 + openjdk 17，2026-09-17，三轮逐字一致，落点 3320,1260。
# 每一段是什么、为什么这么切，见 tools/mouse-dispatch/expected-events-D.txt 的头注（默认落点那份）。
#
# **与默认落点（expected-events-D.txt）的关系：四行里三行逐字相同，只有第 2 行不同，**
# **而那一行不同是按构造的** —— 它是 D2「在外面按右、再松左」那一下松手的位置，xy 就是**落点
# 换算到面板内坐标**（落点 3320,1260，面板左上角 0,53 ⇒ 3320,1207）。默认落点那份是 1254,250，
# 因为那一支的「窗口外」是驱动器自己开在固定位置的那块窗口。
#
# ⚠️ **所以这一行是跟着机器走的**：换一块屏幕、换一处露出来的桌面，它就会变，而**那不是回归**。
#    对不上的时候先看是不是只有这一行、以及跑的时候 WHERE 那行打的落点是多少，再决定改哪边。
#    （其余三行与落点无关，对不上才是真该查的。）
#
# ⚠️ D3 段（只剩被挡掉那只键按着、拖回面板）在这一支上同样是**Java 侧零条事件** —— 与默认落点
#    的结论相同。见跑完打出来的分段读数，不在下面这四行里（那一层只有按下 / 松手）。
PRESSED btn=1 mex=0x400 src=start.StartPanel xy=600,300
RELEASED btn=1 mex=0x1000 src=start.StartPanel xy=3320,1207
PRESSED btn=3 mex=0x1000 src=start.StartPanel xy=600,300
RELEASED btn=3 mex=0x100 src=start.StartPanel xy=660,360
