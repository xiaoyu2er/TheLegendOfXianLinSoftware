# 仙林软件奇侠传：Java → Web 迁移

十三年前的 Swing 单机游戏搬进浏览器。原版是规格，不改；判断"搬对了没有"
靠从原版导出的机器可读记录，不靠人看着像不像。

这份文件是**词汇表**，不是规格也不是计划。每个决定的理由在 `docs/adr/`，
每张票的验收标准在 `bd show <id>`，做法在 `docs/` 下各篇。

## Language

### 两组最容易混的词

**脚本**：
原版的游戏内容文件，`script/` 下 96 份 GBK 文本，一份一个场景。文件名本身
就叫「脚本1」「剧情1」「宿舍」。
_Avoid_: 剧本（那是另一样东西，见下）、script（英文在代码里指前者，在
`traces/scripts/` 里指后者，中文交流时不要用）

**剧本**：
喂给导出器的声明式测试用例，`tools/traces/scripts/` 下的 JSON，一份描述
「按哪些键、跑到什么条件为止」。它和游戏内容无关。
_Avoid_: 脚本、测试用例、trace script

> 两个词只差一个字，指的东西完全不相干。已经在文档里这样用了，这里把它钉死。

**真值**：
从原版导出的、可逐字节比对的记录。**单说「真值」是有歧义的**，共有三种，
指代时必须带限定语：
- **数据层真值** —— `tools/ground-truth/`，96 份脚本 × 26 个字段。
- **行为真值** —— `tools/traces/out/`，逐拍记录状态字段。也叫 trace。
- **参考序列** —— `tools/reference/`，原版截图，按 1/5 抽样。

_Avoid_: 单独的「真值」「ground truth」「基准」

### 判据

**判据**：
一条能跑出红绿的检查。**判据的失败必须和它的通过长得不一样**——这是这个
项目最贵的一条教训，反复出现在各处文档里。
_Avoid_: 测试、校验、check（这些指具体手段，判据指"凭什么算过"）

**响亮失败**：
一件事做不到时，非零退出并说清楚是哪一件、归哪张票；而不是安静地跳过。
它的反面是"找不到东西"被当成通过。
_Avoid_: 报错、抛异常

**篡改验证**：
故意改坏实现，确认判据变红。不变红就说明判据没在验。本项目对"判据有效"
的唯一证明方式。
_Avoid_: mutation testing、变异测试

### 跨端比对

**驱动器**：
一类面板的逐步推进方式。共四种——`scene`、`battle`、`menu`、`shop`——
每种定义自己的"一步"是什么。剧本用 `driver` 字段声明自己属于哪种。
_Avoid_: runner、执行器

**一步**：
行为真值里的一行。**每种驱动器的"一步"不是同一回事**：场景是一拍，战斗
是一次主循环体加一次重绘，菜单和商店是一次输入事件。
_Avoid_: 一帧、一 tick（tick 只在场景与战斗里等于一步）

**装配**：
取图页把某个驱动器的真值还原成可截图的浏览器画面。装配不出来的驱动器
必须响亮失败——一条没装配的剧本比出来是"零帧差异"，和"两端完全一致"
长得一样。
_Avoid_: 渲染、回放

**表态**：
某条剧本在跨端比对里声称的处境，三选一：`match`（逐像素相等）、
`gap`（比过了，剩一笔有上界的账）、`unassembled`（一帧都还没比过）。
_Avoid_: 状态、预期、expectation

**缺口区 / 硬比区**：
分区表态里画出来的矩形是**缺口区**，允许有差异且各自有上界；矩形之外
全是**硬比区**，一个像素都不许差。缺口区不再差也会红——那说明账还完了
而表没改。
_Avoid_: 白名单、忽略区

### 迁移期特有

**假货**：
web 端为了让上游先跑起来而临时顶上的东西，真的那一份在别的里程碑里。
假货必须登记在册并配双向检查，因为**一个被忘掉的假货和一个正常工作的
真货长得一样**。
_Avoid_: mock、stub、占位（这三个词在测试语境里另有所指）

**原版缺陷**：
原版里确实错了、但 web 端照样复刻的行为。它们不是待修的 bug，是规格的
一部分——真值记着它们，改掉就对不上。
_Avoid_: bug、已知问题
