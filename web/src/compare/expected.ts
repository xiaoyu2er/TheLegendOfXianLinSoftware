import { readdirSync } from 'node:fs'
import { repoPath } from '../test/repoPath'
import { STAT_ROW_GAP } from '../shop/layout'
import type { ExactRegionSpec } from './exactRegions'
import type { GapRegion } from './regions'

/**
 * 每条剧本在**当前这个移植进度下**应该是什么结果。
 *
 * 为什么需要这张表：Web 版还没做对话框、素材重编码还差一层，跨端比对现在
 * 必然红。
 * 一条"现在肯定红"的流水线不会有人看，第二天就变成噪声；而把阈值放松到能过，
 * 就变成了这个项目最贵的那种检查 —— 通过条件是"没找到问题"。
 *
 * 所以这里显式声明每条剧本的预期，**两头都会红**（`src/assets/knownMissing.ts`
 * 是同一个套路）：
 *
 * - `match` 的剧本出现偏离帧 → 红，报告指出第一个偏离的帧号；
 * - `gap` 的剧本一帧都不偏了 → **也红**，因为那说明缺口补上了，这张表过期了。
 *
 * 否则"缺口还在"与"表早就该改了"看起来会一模一样。
 *
 * **每笔账都有上界**（xl-l3o）。上面那两条只管"有没有偏离"，不管偏了多少 ——
 * 于是渲染整个崩掉也照样绿。实测（2026-09-06）：把基准侧 `snapshotImage()` 换成
 * 一张全黑的 1024×640，`dorm-walk` 的最差帧从 0.7401% 跳到 99.9565%，
 * `tools/compare-frames.sh` 退出 0、报"1/1 条剧本符合预期"。所以整屏表态要写
 * `maxRatio`（每帧的占比上界），分区表态要给每个缺口区写 `maxPixels`
 * （每帧的像素数上界）。
 *
 * **余量为什么是 2 倍。** 上界要挡住"什么都没画出来"，又不能让另一台机器的正常
 * 抖动变红。这条流水线上最会抖的东西是**后备字体**：两端都退到各自的后备链，
 * 同一帧的差异会跟着链子走。本仓库记录的实测（xl-9bd.17 / xl-9bd.18 的注释，
 * 数字是当时量的、不是这次）里，`dorm-intro` 最会写字的那一帧 t=800 在
 * **5.5397% → 5.6325% → 2.8482%** 之间来回，最宽的一档是 1.98 倍；这一次重量
 * 到的正是 2.8482%，与记录里改完之后的那个数一致。所以取 **2 倍**：它盖得住
 * "这台机器挑了另一条后备链"，而盖不住"这一帧没画出来"（全黑 ≈ 100%，比任何
 * 一条剧本的上界都高出一截）。
 *
 * 重编码那几条（大地图的有损 WebP）其实比字形稳得多 —— 两端的解码器都是定死的
 * —— 2 倍对它们是宽的；这里仍然用同一条规则，宁可规则统一、余量说得清，也不要
 * 每条剧本各拍一个数。
 */
export interface Expectation {
  /**
   * - `match` —— 两端逐像素相等。
   * - `gap`   —— **比过了**，剩一笔说得清、有上界的账。
   * - `unassembled` —— **一帧都还没比过**：取图页装配不出这个驱动器，整条
   *   流水线在这条剧本上非零退出（见 `replay/drivers.ts`）。它不是 `gap`：
   *   `gap` 的每一个数都该是量出来的，而这种剧本一个像素都没画过，写任何
   *   上界都是编的。web 侧把那个面板做出来之后，这一条要么变 `match`、
   *   要么带上真量出来的 `maxRatio` / `gaps` 变成 `gap`。
   *
   * 这个取值是 xl-1vu.4 与 xl-1vu.5 合流时定的：两张票各自都写了
   * `status: 'gap'` 且刻意不写上界（理由都对——没量过），而 xl-l3o 同时立了
   * 「每条 gap 都必须有上界」。三者都对，说明缺的是一个状态，不是一个数。
   *
   * - `unpainted` —— **驱动器装得出，可这条剧本会走进一层还没实现的绘制**，
   *   `battleDrawList` 当场抛（xl-rh9.11）。同样一帧都没比过，所以同样不许带
   *   任何量出来的数。
   *
   *   为什么它不能并进 `unassembled`：那一个说的是「整个面板还没做」，由
   *   `replay/implemented.ts` 那份**驱动器**名单裁决，而这一个是同一个面板里
   *   某几层还没画。两者归的票不是一张，而且 `unassembled.ts` 的双向对撞是
   *   拿驱动器名单撞的 —— 把 `battle` 从那份名单里摘掉，五条已经在比的战斗
   *   剧本会一起哑掉。
   *
   *   它也不是一个逃生舱：`drawList.test.ts` 里那条「unpainted 的剧本必须真的
   *   抛，而且抛的那句话点的正是它挂的那张票」会把它撞回来。哪天那几层画出来
   *   了，那条判据先红。
   */
  readonly status: 'match' | 'gap' | 'unassembled' | 'unpainted'
  /** `gap` / `unassembled` / `unpainted` 必须说清楚差在哪／为什么比不了、归哪张票。`match` 不写。 */
  readonly why?: string
  readonly issue?: string
  /**
   * **幅度上界**（xl-l3o）：每一帧的差异占比都必须 ≤ 这个数。
   *
   * 整屏表态的 `gap` 必须写它（写了 `gaps` 的分区表态不用 —— 那边的上界逐区
   * 挂在 `GapRegion.maxPixels` 上，且缺口区之外一个像素都不许差）。没有上界
   * 时通过条件只剩一句"有偏离帧"，于是**渲染整个崩掉也照样绿**：实测把基准侧
   * `snapshotImage()` 换成全黑图，最差帧从 0.7401% 跳到 99.9565%，流水线退出 0。
   *
   * 取值一律**实测**：跑一遍 `tools/compare-frames.sh`，读报告里那条剧本的
   * 最差帧占比，再按下面每条剧本注释里写明的余量放大。判据与"为什么是每帧
   * 不是所有帧之和"在 `verdict.ts`。
   */
  readonly maxRatio?: number
  /**
   * **分区表态**（xl-1vu.3）。写了它，这条剧本就换一套更硬的判据（判据本身在
   * `regions.ts`，这里只放表）：
   *
   * - 列出来的矩形是**缺口区**，照旧记账，且**每个区各自双向红** —— 某个区
   *   一帧都不差了，就是那笔账还完了而表没改；
   * - 矩形**之外**全是**硬比区**，每一个像素都必须在容差内相等。不是"超过
   *   阈值才算偏"，是**一个都不许有**。
   *
   * 为什么值得多写这几行：不写分区时，"这条剧本红着"这件事本身没有分辨力 ——
   * 缺口挪到了别的地方、多长出一块新的偏离，`status: gap` 照样通过。写了分区，
   * 偏离多出一个像素落在没人声明过的地方就会红。
   *
   * 缺口区**不许互相重叠**，也不许把整屏铺满（那就该老老实实写 `status: match`）。
   */
  readonly gaps?: readonly GapRegion[]
  /**
   * **逐像素相等区**（xl-aq0）。矩形不写在这里，**每一帧从行为真值现读**
   * （`exactRegions.ts` 说了算），区里一个超容差的像素都不许有。
   *
   * 它与上面的 `gaps` 是两套东西，可以同时挂在一条剧本上：`gaps` 是**整屏
   * 切分**（列出来的是缺口，其余全硬比），而放提示图的那几条战斗剧本还欠着
   * 满屏的背景动画重编码（xl-7ip），切不成矩形；这一套反过来，**只声明极小的
   * 几块「这里必须逐像素相等」**，屏幕其余部分照旧由 `maxRatio` 兜着。
   *
   * 为什么要它：`maxRatio` 挡不住小面积的回归。提示图的缩放采样（xl-ttu）改回
   * GPU 那一版会让 111 个像素重新差起来，而 111/655360 = 0.017%，远在上界之下
   * —— 全套检查一条都不红。见 `exactRegions.ts` 的文件头。
   */
  readonly exact?: readonly ExactRegionSpec[]
}

/** 逻辑画布的尺寸。缺口区的矩形必须落在里面。 */
export const CANVAS_WIDTH = 1024
export const CANVAS_HEIGHT = 640

/**
 * 四条画提示图的战斗剧本共用的那块逐像素相等区（xl-aq0）—— 它们守的是**同一个
 * 改动**（xl-ttu 的定点采样），所以理由与票号是同一份，只有从真值现数出来的
 * `drawnTicks` 逐条不同 —— 那是整条真值里提示图画着且矩形非空的拍数，与取帧
 * 密度（`--every`）无关。各自实测到的读数留在各条剧本自己的注释里。
 */
const REMINDER_EXACT = {
  name: 'reminder',
  source: 'reminder',
  why: '提示图的缩放采样按原版的定点规律在 CPU 上做（scaledBlit.ts），不走 GPU 的最近邻',
  issue: 'xl-ttu / xl-aq0',
} as const

/**
 * 商店三条剧本共用的那六块缺口区（xl-knp.10）。
 *
 * **矩形是共用的，数不是。** 两家店的绘制坐标逐字相同（`shop/layout.ts` 的
 * 头注：两个构造函数是复制粘贴的关系），所以同一组矩形对三条剧本都成立，抄三遍
 * 只会让改一处漏两处；而 `maxPixels` 是**逐条剧本实测**的，由调用处传进来。
 *
 * 矩形比实测外接框各边留 **5–7 px** 余量（本文件文件头那条规矩：后备字体链换
 * 一台机器会让字形外扩几个像素，紧贴外接框的话硬比区会红在一圈描边上）。逐边
 * 都是 6，只有三处例外，各自的理由写在这里 —— **这个数是照三条剧本的实测外接框
 * 反算出来的，不是拍的**：
 *
 * - `coins` 的上沿只到 y=0（实测外接框顶在 y=4，再往上就是画布边界），**4 px**；
 * - `list-*` 三个区的下沿 586 与 `keeper-message` 的上沿 587 挨着（缺口区不许
 *   重叠）。两者的实测外接框相距 12 行（581 与 593），两边各自 **5 / 6 px**；
 * - `list-price-stock` 与 `list-held` 的上沿是 **7 px**：三条剧本里那两列最靠上
 *   的差异像素落在 y=184，而它们与 `list-name-purchase`（y=183）共用同一条上沿。
 *
 * ⚠️ **`keeper-message` 的右沿对 `shop-trade` 是白送的。** 那条剧本的实测外接框
 * 只到 x=870，而矩形按另外两条（装备店有第三行 `messageremark`，画在 x=730）
 * 量到 951 —— 于是 `shop-trade` 有 87×53 px 本可以硬比的地方落进了缺口区。
 * **不按剧本收窄是有意的**：870 这个数是那一条剧本的店主恰好说了那么长一句话，
 * 收到它上面等于把一句台词的长度钉成判据，换一句话就红在一个与渲染无关的地方。
 * 三条剧本共用一组矩形，让度记在这里。
 *
 * 为什么价目与存货**合成一个区**：价目画在 `x+180`=633，五位数（灵神天药 15000）
 * 一直画到 732；而存货画在药店 `x+245`=698、装备店 `x+260`=713，**落在价目那一列
 * 的 x 范围里面**。切成两个区必然重叠，而缺口区不许重叠。两列是同一族账（都是
 * 字形），合并不会把两种成因混进一个区。
 *
 * 同理商品名（453 起）与「这一单买几件」（药店 `x+100`=553、装备店 `x+102`=555）
 * 之间只隔 3 px，一并合成一个区。
 */
function shopGaps(worst: {
  readonly nameAndPurchase: number
  readonly priceAndStock: number
  readonly held: number
  readonly coins: number
  /**
   * 左上角那组属性标签**一个人一组**：第 i 组整体下移 `STAT_ROW_GAP` px
   * （原版 `drawIcon` 里三段 `30+i*150`，各自锁在 `if(SaveAndLoad.…)` 里面）。
   * 所以这里是**一个数组，一格一组**，长度就是那条剧本 `setup.party` 里的人数。
   *
   * ⚠️ **长度是登记，不是分母**（`expected.ts` 是一张静态表，读不到真值头部），
   * 所以要人写对。写短了会红，而且红得很响 —— xl-knp.11 实测：把
   * `shop-party` 的 `[545, 545, 547]` 改成 `[545]`，
   * `tools/compare-frames.sh shop-party --skip-capture` 退出码 1，报
   * 「硬比区破了：第 0 帧起有像素超容差，15 帧合计 16380 个；最坏的是第 0 帧
   * 1092 个 @ (55,163)-(74,391)」—— 正是被漏掉的第二、三组那两块。写长了：
   * 多出来的那个区一帧都不差，而 `expected.test.ts` 那条「每个区都得真的差过」会红。
   *
   * 逐组各写各的读数，因为它们**不一样**：xl-knp.11 实测 545 / 545 / 547。
   */
  readonly statLabels: readonly number[]
  readonly keeperMessage: number
}): readonly GapRegion[] {
  // 上界一律是「单帧最多」的 2 倍，与本文件其余各条同一条规矩。乘在这一处，
  // 免得六个区各写一个算好的数 —— 那种数改错了没人看得出来。
  const bound = (single: number) => single * 2
  const GLYPH = 'xl-9bd.17'
  return [
    {
      name: 'list-name-purchase',
      maxPixels: bound(worst.nameAndPurchase),
      rect: { x0: 447, y0: 177, x1: 580, y1: 586 },
      why: '商品名与「这一单买几件」两列的字形（20 号白字）',
      issue: GLYPH,
    },
    {
      name: 'list-price-stock',
      maxPixels: bound(worst.priceAndStock),
      rect: { x0: 632, y0: 177, x1: 738, y1: 586 },
      why: '价目与存货两列数字的字形（20 号白字）',
      issue: GLYPH,
    },
    {
      name: 'list-held',
      maxPixels: bound(worst.held),
      rect: { x0: 752, y0: 177, x1: 778, y1: 586 },
      why: '背包持有量那一列数字的字形（20 号白字）',
      issue: GLYPH,
    },
    {
      name: 'coins',
      maxPixels: bound(worst.coins),
      rect: { x0: 899, y0: 0, x1: 977, y1: 25 },
      why: '右上角钱袋里那串金钱数字的字形',
      issue: GLYPH,
    },
    // 队伍里每个人一组，第 i 组整体下移 `STAT_ROW_GAP` px。⚠️ 循环的分母是**给进来的
    // 那份读数有几格**，不是写死的 1 或 3 —— 三条只有 zhang 的剧本与
    // `shop-party` 共用这一个函数。
    ...worst.statLabels.map((single, i) => ({
      // 一律带下标，第 0 组也不例外 —— `stat-labels` 与 `stat-labels-1` 并排时
      // 读不出前者也是一个下标。
      name: `stat-labels-${i}`,
      maxPixels: bound(single),
      // ⚠️ 行距取自 `shop/layout.ts` 的 `STAT_ROW_GAP`，不在这里再抄一个 150：
      // 抄一份的话改了那边这里静默不动，而"矩形挪错了"的样子是**区外多出
      // 差异像素**，与"渲染回归了"分不开。
      rect: {
        x0: 49,
        y0: 7 + STAT_ROW_GAP * i,
        x1: 80,
        y1: 97 + STAT_ROW_GAP * i,
      },
      why: `左上角第 ${i} 组四行属性标签的字形（图标本身逐像素相等）`,
      issue: GLYPH,
    })),
    {
      name: 'keeper-message',
      maxPixels: bound(worst.keeperMessage),
      rect: { x0: 447, y0: 587, x1: 957, y1: 639 },
      why: '底部店主那两三行话的字形',
      issue: GLYPH,
    },
  ]
}

/**
 * 场景面板右下角的金币数（`OtherEvent.addMap`：`正楷` BOLD 16 白字，基线 (940,630)）。
 *
 * xl-yg6.12 把 addMap 那一层画上之后（`scene/mapOverlays.ts`），`money.png` 图标与
 * 大地图那十张遮掩图在 11 条场景剧本上**逐像素相等**，这里只剩数字的字形：`正楷`
 * 两端都没有，各退到自己的后备链，归 xl-9bd.17。
 *
 * 实测外接框在 2026-09-10 那一轮的 11 条场景剧本上一律是 (940,617)-(993,630)（measureGaps；
 * 条数是那天的读数，不是分母）。矩形各边
 * 留 **6 px**：位数随剧本变（10000，答题剧本里还有四位数），字宽跟着变；而紧贴外接框的
 * 54×14 = 756 个像素装不下 2 倍余量（单帧最多 586，×2 = 1172），`expected.test.ts`
 * 那条「上界够不着」会当场红。
 */
function coinDigits(single: number): GapRegion {
  return {
    name: 'coin-digits',
    maxPixels: single * 2,
    rect: { x0: 934, y0: 611, x1: 999, y1: 636 },
    why: '右下角金币数的字形（原版 正楷 两端都没有）',
    issue: 'xl-9bd.17',
  }
}

/**
 * 从脚本1 开场的那两条剧本（`dorm-intro` 与 `milestone`）共用的缺口区（xl-yg6.12）。
 *
 * **矩形是共用的，数不是**（与 `shopGaps` 同一个套路）：两条开场走的是同一段旁白与
 * 同一批对话，矩形从 `dorm-intro` 的差异图上量出来，再拿 `milestone` 的差异图回核过
 * （区外同样是 0）；`maxPixels` 逐条剧本实测，由调用处传进来。
 *
 * 顶上那 6 条横带的上下沿只留 **3 px**：旁白一行一行排，相邻两行的外接框只隔 6~7 行
 * （76→83、106→115 …），缺口区又不许重叠。切成逐行而不是一整块，是因为一整块
 * (44,40)-(915,268) 让 `dorm-intro` 的缺口区合计到 41.0%，过了 40% 那条线。
 */
function script1Gaps(worst: {
  readonly speechName: number
  /** 顶上 6 条横带各自的「单帧最多」，一格一条。 */
  readonly topText: readonly [number, number, number, number, number, number]
  readonly bottomUpper: number
  readonly bottomLower: number
  readonly coin: number
}): readonly GapRegion[] {
  const GLYPH = 'xl-9bd.17'
  // 顶上 6 条横带：旁白第 1..6 行，前三条同时也是样式 1 对话框的正文。
  // dorm-intro 实测外接框依次是 (50,43)-(711,76) / (50,83)-(909,106) / (50,115)-(809,143)
  // / (50,163)-(309,182) / (51,203)-(597,223) / (50,243)-(258,262)。
  const TOP_BANDS = [
    { x0: 44, y0: 40, x1: 717, y1: 79 },
    { x0: 44, y0: 80, x1: 915, y1: 109 },
    { x0: 44, y0: 112, x1: 815, y1: 146 },
    { x0: 44, y0: 160, x1: 315, y1: 185 },
    { x0: 45, y0: 200, x1: 603, y1: 226 },
    { x0: 44, y0: 240, x1: 264, y1: 265 },
  ] as const
  return [
    {
      name: 'speech-name',
      // 实测外接框 (3,2)-(369,33)。名字牌沿顶端滑入，下沿紧贴 top-text-1。
      maxPixels: worst.speechName * 2,
      rect: { x0: 0, y0: 0, x1: 375, y1: 39 },
      why: '名字牌滑入路径上的名字字形（原版字体未交付）',
      issue: GLYPH,
    },
    ...TOP_BANDS.map((rect, i) => ({
      name: `top-text-${i + 1}`,
      maxPixels: worst.topText[i]! * 2,
      rect,
      why: `旁白第 ${i + 1} 行${i < 3 ? ' / 样式 1 对话框正文' : ''}的字形（原版字体未交付）`,
      issue: GLYPH,
    })),
    {
      name: 'bottom-text-upper',
      // 实测外接框 (200,503)-(599,598)。下沿停在 598，给下面那一行让位。
      maxPixels: worst.bottomUpper * 2,
      rect: { x0: 194, y0: 497, x1: 605, y1: 598 },
      why: '底部样式 2 对话框前三行正文的字形',
      issue: GLYPH,
    },
    {
      name: 'bottom-text-lower',
      // 实测外接框 (200,599)-(558,614)（xl-03x.15 重量）。上沿原先停在 600，把
      // y=599 那一行让给旁白背景的取整区；那一区的账还完之后（旁白帧上第 599 行
      // 两条剧本一个像素都不差，左段 0..193 与右段 606..1023 同样 0），这一行剩下
      // 的只有对话框第 4 行文字（(200,599)-(555,599)，与本区同在 #3425 那 3 帧），
      // 于是并回这里。⚠️ 代价：旁白帧上第 599 行 x=194..605 这一段也落进了这个区，
      // 不再被硬比守着（实测旁白帧那一段 0 个；区是静态的，两族分不开）。
      maxPixels: worst.bottomLower * 2,
      rect: { x0: 194, y0: 599, x1: 605, y1: 620 },
      why: '底部样式 2 对话框第 4 行正文的字形',
      issue: GLYPH,
    },
    coinDigits(worst.coin),
  ]
}

export const EXPECTED: Readonly<Record<string, Expectation>> = {
  'battle-min': {
    status: 'gap',
    // **第一条进跨端逐帧比对的战斗剧本**（xl-rh9.9）。原版 `BattlePanel.paint()`
    // 那 25 层照序画出来之后，实测（`tools/compare-frames.sh battle-min`，
    // `--every 25`，17 帧，容差 8）：
    //
    //   硬比区 17 帧**逐像素相等**
    //   panel-text-zhang  合计 29697、最差 1825
    //   panel-text-yu     合计 28971、最差 1829
    //   panel-text-lu     合计 26895、最差 1696
    //
    // 也就是说：背景图、开场云雾、行动条与七颗小头像、三个人的走图、三只怪的
    // 走图、四颗指令按钮（连**哪一颗亮着**）、指示图、技能动画的每一帧、被击
    // 动画、飘起来的伤害数字、怒气槽、血条与灵力条、游标 —— 全部逐像素相等。
    // 剩下的差异**只有状态栏那九行字**。
    //
    // 差异的分布是量出来的，不是看出来的：17 帧合起来，超容差的像素落在
    // 六段列区间与三段行区间上，六段列区间恰好是三个人各自的两块
    // （`当前等级 :N` 一块、`hp/hpMax` 与 `mp/mpMax` 一块），三段行区间恰好是
    // 三行字的高度：
    //
    //   列 92–168 / 240–327（张） 414–490 / 562–649（文） 736–812 / 884–951（陆）
    //   行 547–561（等级）  578–592（血）  598–612（灵力）
    //
    // 下面三个缺口区就是按这个分布画的（每人一块，把那两段列区间与三段行区间
    // 一起裹住），**区外一个像素都不许差**。
    //
    // 这笔账是什么：原版 `BattlePanel` 用的字体是 `文鼎粗钢笔行楷`
    // （`new Font("文鼎粗钢笔行楷", Font.BOLD, 15)`），那款字没有随游戏交付，
    // 两端各自退到本机的后备链，同样的基线坐标画出来的笔画不同。与 dorm-intro /
    // dorm-walk 剩下的那一笔是**同一笔账**（xl-9bd.17 已裁定不打包字体），
    // 只是换了个面板。位置是准的：三行字的行区间两端逐像素对齐，说明基线算对了。
    why: '状态栏那九行字的字形（原版 文鼎粗钢笔行楷 未交付，两端各退到自己的后备链）',
    issue: 'xl-9bd.17',
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 1825 个（2026-09-07，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 3650,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-yu',
        // 实测最差帧 1829 个，上界取 2 倍。
        maxPixels: 3658,
        rect: { x0: 410, y0: 545, x1: 652, y1: 614 },
        why: '第二格（文敏）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-lu',
        // 实测最差帧 1696 个，上界取 2 倍。
        maxPixels: 3392,
        rect: { x0: 732, y0: 545, x1: 974, y1: 614 },
        why: '第三格（陆雪琪）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
  },
  'battle-menus': {
    status: 'gap',
    // 点得下去的「技」与「物」那一条（xl-rh9.11，461 步）。xl-rh9.12 把药品
    // 菜单 / 技能菜单 / 提示图 / 敌我两层战斗状态图标画出来之后，它从
    // `unpainted` 换成了这条**真量出来的** gap。
    //
    // 实测（`tools/compare-frames.sh battle-menus`，`--every 25`，19 帧，
    // 容差 8）。逐帧的账，以及"状态栏那块之外还差多少"这一列 —— 后者是把
    // battle-min 那三个文字缺口区（88..974 × 545..614）扣掉再数的：
    //
    //   t=0                 0.3925%   状态栏之外 **0** 个
    //   t=25/50/75/175/250/275 0.72..0.82%  状态栏之外 **0** 个
    //   t=100/150           0.86/0.98% 之外 313/1110 —— 六行存货数字，每行 53 个
    //   t=125               **0.9805%** 之外 1110 = 药品介绍文字 845 + 存货 265
    //                                                              ← 最差帧
    //   t=200/225           0.8105/0.8226%  之外 1/80   追星破月 第 2/27 帧
    //   t=300/325/350       0.8234/0.8223/0.8223%  之外 12/5/5  浪里寻花 第 3/28/53 帧
    //   t=375..450          0.80..0.82%  状态栏之外 **0** 个
    //
    // ⚠️ **t=200/225 那一行是 xl-9do 之后 2026-09-07 又重量了一次**：追星破月
    //   那 41 张源是 1240×744，被 xl-9do 裁到画布，区外从 13/62 变成 11/104
    //   （同一台机器上先后两次实测，裁之前那一遍读到的正是 13/62，与 xl-7ip
    //   记的逐字相同）。**最差帧仍是 #125、仍是 0.9975%、上界不动** —— #125 是
    //   药品菜单那一帧，`anim.bgDrawn=false`，跟背景动画毫无关系。
    //   这条剧本另外三帧的背景动画是 浪里寻花，源 1066×639、没被裁，58/11/5
    //   三个数一个没动。
    //
    //   **t=225 那一帧是变差的：62 → 104。** 按票面原本的口径（「区外像素数逐个
    //   相等，数变了就说明裁到肉了」）它就是裁到肉，所以这里明写，不让它混在
    //   「重量」两个字里被读成中性。为什么仍然接受：那条口径本身是**做不到**的
    //   （有损编码器的量化参数是对整张图算的，输入变小必然重掷骰子，见
    //   `bake.ts` 的 `backgroundAnimCrop` 头注），而 1240×744 那一档**整体是
    //   赚的** —— 115 张相对源 JPEG 的超容差像素合计 49537 → 24680，减半；
    //   这条剧本上 t=200 也从 13 降到 11。上界一条没动。2026-09-07 用户裁定。
    //
    //   ⚠️ **这五个数没有任何门禁在守**：流水线只核 `maxRatio`（整屏占比上界），
    //   而这一趟五个数都动了、四条上界一条没动。要发现这一层的变化，只能像本票
    //   这样把两端的 PNG 逐帧拉出来按状态栏三块矩形扣一遍再数。xl-7ip 那趟也是
    //   人工数的。把它做成判据是另一张票的事，这里只把「它现在是人工的」写明白。
    //
    // ⚠️ **xl-x6w 之后 2026-09-08 又重量了一次**（`-sns 0` + 越界就裁，见
    //   `bake.ts` 的 `BATTLE_LOSSY_SNS`）：放背景动画那五帧的区外从 11/104/58/11/5
    //   变成 **9/80/67/5/5**，五帧合计 189 → 166。**t=100/125/150 那三个数
    //   （313/1221/1110）一个都没动** —— 那三帧是药品菜单，`anim.bgDrawn=false`，
    //   一笔背景动画的账都没有。**这本身是这一趟改的是编码而不是渲染的现成判据**：
    //   动的恰好是放背景动画的那五帧，不动的恰好是不放的那三帧。
    //   **最差帧仍是 #125、仍是 0.9975%、上界仍是 1.9949%，一个数没动。**
    //
    // ⚠️ **xl-ttu 之后 2026-09-08 又重量了一次，提示图那笔账还清了。** 缩放采样
    //   改成按原版的定点规律在 CPU 上算（`battle/render/scaledBlit.ts`），三个
    //   放提示图的帧区外变了：**t=125 从 1221 掉到 1110、t=200 从 9 掉到 1、
    //   t=300 从 67 掉到 12**；不放提示图的那六个数（313 / 1110 / 80 / 5 / 5 与
    //   四个 0）一个没动。**最差帧仍是 #125，但它从 0.9975% 掉到 0.9805%，
    //   上界跟着从 1.9949% 收到 1.9610%**（仍是实测最差帧的 2 倍）。
    //
    //   提示图那三块矩形现在**逐像素相等**：t=125 的 80×16 是 0/1280、
    //   t=200 的 20×4 是 0/80、t=300 的 30×6 是 0/180（矩形从真值的
    //   `reminder.dx1/dy1/dx2/dy2` 读，不是量的）。t=200 剩下的那 1 个在
    //   (782,93)、d=9，是技能动画的有损重编码，离提示图那块 270 个像素远。
    //
    //   **改前那五个数是这一趟自己跑出来的，不是抄上一行的**：把渲染器换回
    //   改前那一版（`git show HEAD~1:…/battleRenderer.ts`）重跑一遍，读到的正是
    //   313/1221/1110 与 9/80/67/5/5 —— 与上面 xl-x6w 那段记的改前读数逐字
    //   相同（那八个数现在只在那一段里留着，逐帧表已换成改后的）。顺带证明了
    //   一件事：**这一趟的改动没有任何单元测试在守** —— 渲染器换回去，
    //   `pnpm test` 全绿。
    //
    //   ⚠️ **那句话的后半截已经不成立了（xl-aq0，2026-09-08）。** `pnpm test`
    //   仍然全绿（同一条路重跑了一遍：把渲染器换回 GPU 采样那一版，70 个测试
    //   文件 1112 条一条不红）—— 渲染器按设计没有测试缝，这一条不会变。但
    //   **跨端逐帧比对现在会红**：下面那块 `exact` 声明了提示图那一层的
    //   逐像素相等区，矩形每帧从真值现读。换回 GPU 采样的那一版实测退出码 1，
    //   四条剧本一起红（menus 3 帧合计 174、zhang 4 帧 444、yu 3 帧 236、
    //   lu 5 帧 209），而**整屏那条上界四条全部照过** —— 0.9975% ≤ 1.9610%
    //   这类，正是这张票要补的那个洞。
    //
    // ⚠️ **最后四行是 xl-7ip 之后 2026-09-07 重量的，最差帧换人了。** 重量前
    // 那五帧是 0.9352 / **4.2029** / 2.78 / 3.92 / 2.54%，#225 的区外是满屏；
    // 换成 `cwebp -q 95` 之后它们的区外掉到 13 / 62 / 58 / 11 / 5 个，最差帧
    // 从 #225 变成 #125 —— 而 #125 是药品菜单那一帧，`anim.bgDrawn` 是 false，
    // 它的 1221 个区外像素与背景动画毫无关系，重量前后一个都没变。
    // **也就是说这条剧本的上界现在由字形那一笔（xl-9bd.17）定着。**
    //
    // 三件事由这张表钉住，都是这一票的正面判据：
    //
    // 1. **t=175 与 t=275 状态栏之外差 0 个像素** —— 这两帧技能菜单开着
    //    （分别是文敏那组三颗与张小凡那组两颗），背板、每颗按钮的贴图、
    //    返回按钮、技能说明图**逐像素相等**。t=275 那一帧说明图挂的还是文敏
    //    上一轮的（`isDrawIntro` 原版从不清），这个原版缺陷也是逐像素对上的。
    // 2. **t=375..450 状态栏之外差 0 个像素**，而那几帧怪物身上挂着「体力下降」
    //    ——怪物那层状态图标逐像素相等；我方那层由 t=250/275 同理钉住。
    // 3. 药品菜单那几帧之外的差异**只剩字**：六行存货数字每行恒 53 个、
    //    介绍文字 845 个，背板与七颗按钮贴图、金创药那张介绍图都不差。
    //
    // 剩下的两笔账：
    //
    // - **背景动画的有损重编码**（xl-7ip，已还过一轮）。`image/背景动画/` 那
    //   753 张源文件是 JPG，烘焙器对非 PNG 走 `cwebp -q`，于是满屏差一点点。
    //   五条老剧本一次技能都没用过，所以这笔账到 xl-rh9.12 才露面，当时它是
    //   最差帧 #225 的唯一成因（27544 个像素里状态栏那块只占 5000 上下）。
    //   xl-7ip 把战斗素材那条路的档位从 q80 提到 q95（`bake.ts` 的
    //   `BATTLE_LOSSY_QUALITY`，六个档位的全量代价实测在那条常量的头注里），
    //   这一笔从「满屏」缩到「五帧合计 149 个像素」，最差帧因此换成了 #125。
    //   **没有还清**：归零只有 `-lossless` 一条路，要给仓库永久加 89 MiB、
    //   单个技能的按需下载从 2.9 M 涨到 16.3 M，2026-09-07 裁定不换。
    //
    // **提示图缩放那一笔（xl-ttu）已经还清了**，所以它不在上面那个列表里。它原先是
    // 111 个像素（只在 t=125）：提示图是战斗里唯一真的在缩放的一层（源 128×24 →
    // 目标 80×16，k=1.6），差异图上一排**周期 5** 的竖条，正是 `1.6i+0.8` 落在
    // 整数上的那几列。它不是采样网格整体错位（那两种平移 xl-rh9.12 都实测过，
    // 各自把这一块从 111 推到 800 以上），而是两端在纹素边界上打平时的取整不同。
    // 现在按原版的定点规律在 CPU 上算，那三块矩形逐像素相等。
    //
    // ⚠️ **顺带纠正上面两段对背景动画那笔账的高估。** xl-x6w 段说「放背景动画
    // 那五帧的区外……五帧合计 189 → 166」、xl-7ip 段说「缩到五帧合计 149 个
    // 像素」，两句都把那五帧的区外**整个**算作背景动画的账。这一趟证明不是：
    // 其中 **63 个是提示图**（t=200 的 8 个 + t=300 的 55 个），背景动画在那五帧
    // 上的真实账是 **103**。那两段的读数没错，错的是归属，所以只在这里更正，
    // 不去改它们记下的当时读数。
    //
    // 上界按仓库规矩取最差帧的 2 倍：0.9805%（6426/655360）→ 1.9610%。
    maxRatio: 0.019610,
    why:
      '状态栏那九行字 + 药品菜单的存货与介绍文字（同一笔字形账，这条上界现在由它定）；' +
      '背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0）',
    // **两笔账两张票**：字形归 xl-9bd.17（已裁定不打包字体），背景动画的有损
    // 重编码归 xl-7ip。提示图那张（xl-ttu）已经关了，所以不再列在这里 ——
    // 它的账在上面的逐帧表里留了痕。上界现在由字形那笔定着。
    issue: 'xl-9bd.17 / xl-7ip',
    // **逐像素相等区**（xl-aq0）：提示图那一层的目标矩形，每一帧从真值的
    // `reminder.dx1..dy2` 现读，**区里一个超容差的像素都不许有**。守的是
    // xl-ttu 那个改动 —— 缩放采样按原版的定点规律在 CPU 上做，而不是交给 GPU
    // 的最近邻。把渲染器换回改前那一版，`pnpm test` 全绿、整屏的 maxRatio 也
    // 照样过（111 个像素只占 0.017%），只有这条会红。理由与另外两条被否掉的
    // 路见 `exactRegions.ts` 的文件头。
    //
    // 实测（`tools/compare-frames.sh battle-menus`，`--every 25`，容差 8）：
    // 采样到的 19 帧里 **3 帧**画着提示图，t=125 的 80×16 是 0/1280、
    // t=200 的 20×4 是 0/80、t=300 的 30×6 是 0/180 —— 与 xl-ttu 记在上面那段
    // 里的读数逐字相同（那一趟是人工数的，这一趟是流水线数的）。
    exact: [{ ...REMINDER_EXACT, drawnTicks: 63 }],
  },
  'battle-drugs': {
    // 战斗里真的用药（xl-byy）：与 battle-menus 同一场遭遇，多了回复数字与存货变化。
    // 实测（2026-09-12，`tools/compare-frames.sh battle-drugs`，30 帧，容差 8）：
    // 30/30 帧偏离、最差 #125 0.9688% @ (92,234)-(951,612) —— 药品菜单开着那一帧，
    // 与 battle-menus 的最差帧同一处同一笔字形账。上界照 battle-menus 取最差帧的两倍。
    // 提示图逐像素相等区：采样到 2 帧、4160 个像素全等。
    status: 'gap',
    maxRatio: 0.0194,
    why:
      '状态栏那九行字 + 药品菜单的存货与介绍文字（同一笔字形账，与 battle-menus 同源）；' +
      '背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0）',
    issue: 'xl-9bd.17 / xl-7ip',
    exact: [{ ...REMINDER_EXACT, drawnTicks: 42 }],
  },
  // ===== xl-rh9.14 的六条：剩下那些技能与秘术 =====
  //
  // 六条原先都是 `unpainted`（技能菜单与状态图标归 xl-rh9.12、小精灵归
  // xl-rh9.15）。xl-rh9.18 把 xl-rh9.12 合进来之后**五条比得成了**，
  // xl-rh9.15 把第 11 层小精灵画出来之后**第六条也比得成了** —— 六条各自换成
  // 下面这些**真量出来的**表态，这一批里再没有 `unpainted`。
  //
  // 五条的账合起来只有两笔，逐帧数出来的（`tools/compare-frames.sh`，
  // `--every 25`，容差 8）：
  //
  // 1. **状态栏那几行字**，与 battle-min / battle-menus 同一笔（xl-9bd.17）。
  // 2. **背景动画的有损重编码**（xl-7ip）。这一笔只在**背景动画正在放**的那些
  //    帧上出现，而且是满屏的。
  //
  // 第 2 笔与"哪几帧"的对应关系是核过的，不是看出来的：三条技能剧本一共 111
  // 个取样帧，其中**状态栏之外还有差异的恰好 29 帧**（xl-x6w 之后 2026-09-08
  // 重量；xl-7ip 那一趟是 29 帧，xl-9do 那一趟 28 帧），这 29 帧的真值里
  // `anim.bgDrawn` **全是 true**（逐帧从 `tools/traces/out/*.trace.json` 现读核
  // 的，不是看出来的）。反过来有**一个**例外 —— `battle-lu-skills` 的 t=450
  // （`劈风追月` 第 88 帧）：背景动画在放，可状态栏之外一个像素都不差。所以这条
  // 对应是**单向**的：区外有差异 ⇒ 那一拍背景动画在放。
  //
  // ⚠️ 例外的个数在这三张票里来回过一趟，值得记一笔：xl-9do 把伏虎冲天那 74 张
  // （1240×744）裁到画布，`battle-yu-skills` 的 t=75 区外从 3 个降到 **0**，成了
  // 第二个例外；xl-x6w 加 `-sns 0` 之后它又回到 **2** 个，于是掉回名单里、例外
  // 回到一个。**同一件事读三遍读出三个数，那正是「区外像素数是编码抽签的结果」
  // 这句话的现场证据** —— 例外的个数不是一条性质，是一个读数。
  //
  // ⚠️ **xl-7ip 之后（2026-09-07 重量），第 2 笔不再定这三条的上界了。**
  // 背景动画那 753 张 JPG 从 `cwebp -q 80` 换到 `-q 95`（`scripts/bake.ts` 的
  // `BATTLE_LOSSY_QUALITY`，档位取舍的全量实测在那条常量的头注里），区外的
  // 像素数缩小了 30~60 倍：三条剧本最差帧的区外从 30118 / 51884 / 50953 降到
  // 220 / 1000 / 548。**上界现在由第 1 笔（字形）定着**，也就是 xl-9bd.17 那张
  // 票——这正是 xl-7ip 要的结果，因为字形那一笔是有人认领的，而素材编码那一笔
  // 跟"这条剧本画对了没有"毫无关系。
  //
  // xl-7ip 那一趟**那 29 帧的名单一帧没变**（幅度掉了两个数量级而落点分毫未动，
  // 那本身就是"改的是编码、不是渲染"的现成判据）。xl-9do 这一趟名单**动了一帧**
  // ——`battle-yu-skills` 的 t=75 掉出去了 —— 而这一次动是**对的**：xl-7ip 改的是
  // 全部 753 张的档位，落点本就不该变；xl-9do 只裁 1240×744 那 115 张（伏虎冲天
  // 74 张 + 追星破月 41 张），受影响的帧必须、也只该是放这两个动画的那几帧。
  // 逐帧核过：变的恰好是 yu 的 t=50/75/100（伏虎冲天，153/3/6 → 148/0/2）与
  // menus 的 t=200/225（追星破月，13/62 → 11/104），**其余 24 帧一个数都没动**。
  //
  // ⚠️ **xl-x6w 这一趟（2026-09-08）与前两趟都不一样：名单没动，但区外的数几乎
  // 全动了，而且有两帧大幅变差。** 战斗有损那条路加了 `-sns 0`（关掉 cwebp 对
  // 整张图的分段分析），同时把 xl-9do 那条面积门槛删掉、改回「越界就裁」——
  // 753 张全裁。`-sns 0` 是逐张抽签：753 张源图里 **220 张变差、430 张变好、
  // 103 张持平**，合计偏离 153588 → 142986（−6.9%）、体积 32.70 → 31.93 MiB。
  // 全量实测见 `scripts/bake.ts` 的 `BATTLE_LOSSY_SNS` 头注。
  //
  // 落到这三条剧本上，**合计不是全赢**：区外合计 zhang 953 → 1307、
  // yu 1282 → 2522（都变差），lu 1403 → 1284（变好）。两个坏运气都指名道姓：
  // `龙翔九天/15`（16 → 671，顶起 zhang 的新最差帧 #350）与 `苍龙盖天/22`
  // （30 → 1511，顶起 yu 的新最差帧 #175）。两张都是 1066×639，**不是裁出来的**
  // ——保留门槛只裁 1240×744 的话它们是 672 / 1451，一样坏。是 `-sns 0` 本身在
  // 这两张图上掷坏了。
  //
  // **上界因此一条都没有放宽**（xl-x6w 票面的验收标准）：zhang 与 yu 的最差帧
  // 变差了，按仓库「最差帧 ×2」的规矩算出来会是 0.7178% / 1.4316%，比现在的
  // 0.6665% / 1.3480% **松**，所以不改 —— 它们现在的余量是 1.86× / 1.88×，
  // 不是 2×，那是有意的。lu 的最差帧变好了，上界跟着**收紧**。
  //
  // 两条秘术剧本一帧都没有第 2 笔（它们不放背景动画 —— `heroMishu` 一句
  // `backgroundAnimation.set` 都没有），于是它们能写**分区表态**：状态栏那一块
  // 之外**一个像素都不许差**。
  'battle-zhang-skills': {
    status: 'gap',
    // 张小凡的五招（xl-rh9.14，500 步）。实测 20 帧，**xl-x6w 之后 2026-09-08
    // 重量**（q80 那一列是 xl-7ip 之前的，留着看这一笔总共缩了多少）：
    //
    //   t=0/25/50/100..150/225..325/400/425   状态栏之外 **0** 个
    //          整屏(今天)  区外(今天)  区外(xl-x6w 前)  区外(q80)
    //   t=75    0.3275%       184          220           30118  横剑摆渡 第 14 帧
    //   t=175   0.3020%        75           82           20897  银鹰掠地 第 14 帧
    //   t=200   0.2945%         4            8           13762  银鹰掠地 第 39 帧
    //   t=350   0.3589%       524           80            8646  龙翔九天 第 15 帧 ← 最差帧
    //   t=375   0.2707%         1            4            3852  龙翔九天 第 40 帧
    //   t=450   0.3252%       459          469           19300  神剑傲州 第 6 帧
    //   t=475   0.2650%        60           90           28711  神剑傲州 第 31 帧
    //
    // **最差帧从 #75 换成了 #350，而且是变差换过去的**（0.3333% → 0.3589%）：
    // 七帧里六帧都变好了，只有 t=350 从 80 涨到 524 —— `龙翔九天/15` 这一张源图
    // 相对源 JPEG 的超容差像素从 16 涨到 671，`-sns 0` 在它身上掷坏了。**上界
    // 不跟着放宽**（见上面那段的理由），仍是 0.6665%，余量 1.86×。
    //
    // 也就是说：**不放背景动画的每一帧，状态栏之外逐像素相等** —— 技能菜单的
    // 背板与按钮、技能说明图、五套技能动画的每一帧、被击动画、伤害数字，全对。
    //
    // 最差帧仍**不是被背景动画顶上去的**：#350 那一帧超容差的 2352 个像素里，
    // 区外占 524 个，其余 1828 个是状态栏那几行字 —— 字形那一笔仍是大头。
    //
    // ⚠️ **xl-9do 之后重量过，这条剧本上面七个数一个都没动。** 这不是"没查"，
    // 是查过之后的结论，而且它本身是那张票的一条判据：xl-9do 只裁 1240×744
    // 那一档（伏虎冲天 74 张 + 追星破月 41 张），而这条剧本的五套背景动画
    // 横剑摆渡 / 银鹰掠地 / 龙翔九天 / 神剑傲州 全是 1066×639，一张都不该被裁。
    // **它还是那张票收窄范围的直接原因**：先前有一版把 753 张全裁了，1066×639
    // 那档重掷量化骰子掷坏了 —— `神剑傲州/31` 相对源 JPEG 的超容差像素从 107
    // 涨到 18139，这条剧本的最差帧从 #475 0.2693% 顶到 **2.5040%**（超上界
    // 3.76 倍，`tools/compare-frames.sh` 退出 1）。也就是说这条上界**真的拦下过
    // 一次素材层面的回归**，它不是装饰。
    //
    // 上界仍是 xl-7ip 那一趟按「最差帧 ×2」定的 0.3333%（2184/655360）→ 0.6665%。
    // xl-x6w 之后最差帧是 0.3589%（2352/655360），照规矩算会是 0.7178% —— **更松，
    // 所以不采纳**（xl-x6w 票面：上界只许变紧不许放宽）。
    maxRatio: 0.006665,
    why: '状态栏那几行字（字形，这条上界现在由它定）；背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0），只在放背景动画的 7 帧上、七帧合计 1307 个像素',
    issue: 'xl-9bd.17 / xl-7ip / xl-x6w',
    // **逐像素相等区**（xl-aq0）：提示图那一层的目标矩形，每一帧从真值的
    // `reminder.dx1..dy2` 现读，**区里一个超容差的像素都不许有**。守的是
    // xl-ttu 那个改动 —— 缩放采样按原版的定点规律在 CPU 上做，而不是交给 GPU
    // 的最近邻。把渲染器换回改前那一版，`pnpm test` 全绿、整屏的 maxRatio 也
    // 照样过（111 个像素只占 0.017%），只有这条会红。理由与另外两条被否掉的
    // 路见 `exactRegions.ts` 的文件头。
    //
    // 实测（`--every 25`，容差 8）：20 帧里 **4 帧**画着提示图，
    // t=75 / t=175 / t=350 各是 120×24 的 0/2880、t=450 是 60×12 的 0/720。
    // 前三帧背景动画正放着（横剑摆渡 / 银鹰掠地 / 龙翔九天），那笔有损重编码的
    // 账在区外照旧记着，**区内一个像素都不差** —— 这条不会被 xl-7ip 那笔误伤。
    exact: [{ ...REMINDER_EXACT, drawnTicks: 84 }],
  },
  'battle-yu-skills': {
    status: 'gap',
    // 文敏的五招（xl-rh9.14，1175 步）。实测 47 帧，状态栏之外有差异的 8 帧
    // 全是背景动画在放。**xl-x6w 之后 2026-09-08 重量**：
    //
    //          整屏占比(今天)  区外(今天)  区外(xl-x6w 前)  整屏占比(q80，xl-7ip 前)
    //   t=50      0.5556%        142           148           3.96%   伏虎冲天 第 11 帧
    //   t=75      0.5344%          2             0           2.86%   伏虎冲天 第 36 帧
    //   t=100     0.5344%          4             2           5.17%   伏虎冲天 第 61 帧
    //   t=175     0.7158%       1286            24           3.54%   苍龙盖天 第 22 帧 ← 最差帧
    //   t=200     0.5205%         10             4           1.68%   苍龙盖天 第 47 帧
    //   t=800     0.5310%         67            65           1.31%   妙手回春 第 17 帧
    //   t=850     0.5157%         37            39           1.03%   蝶影神灵 第 4 帧
    //   t=875     0.6697%        974          1000           6.5454% 蝶影神灵 第 29 帧
    //   其余 39 帧 0.31..0.53%     0             0            同左    状态栏之外 **0** 个
    //
    // **最差帧从 #875 换成了 #175，而且是变差换过去的**（0.6740% → 0.7158%）：
    // `苍龙盖天/22` 这一张源图相对源 JPEG 的超容差像素从 30 涨到 1511，`-sns 0`
    // 在它身上掷坏了（1066×639，不是裁出来的：保留门槛也是 1451）。**上界不跟着
    // 放宽**，仍是 1.3480%，余量 1.88×。
    //
    // **t=75 回到了「区外有差异」那份名单**（xl-9do 让它掉出去过），这条剧本从
    // 7 帧回到 8 帧、合计从 1282 涨到 2522 —— 这 +1240 是这么来的：t=175 一帧
    // 就涨了 +1262，其余七帧加起来净减 22。
    //
    // 最右那一列是 xl-rh9.18 当时记下的整屏占比，**不是区外像素数** —— 那一趟
    // 只留了占比。区外像素数只有 q95 这一列是量出来的。
    //
    // 妙手回春那一帧值得单说：文敏那一招一发伤害都不打，只给全员回血并复活，
    // 而这一帧状态栏之外也只有背景动画那一笔。
    //
    // 新的最差帧 t=175：那一帧超容差的 4691 个像素里，区外 1286 个、状态栏那几行
    // 字 3405 个 —— **上界仍由字形定着**，素材编码那一笔虽然涨了，还没夺回来。
    //
    // 上界仍是 xl-7ip 那一趟按「最差帧 ×2」定的 0.6740%（4417/655360）→ 1.3480%。
    // xl-x6w 之后最差帧是 0.7158%（4691/655360），照规矩算会是 1.4316% —— **更松，
    // 所以不采纳**（xl-x6w 票面：上界只许变紧不许放宽）。
    maxRatio: 0.013480,
    why: '状态栏那几行字（字形，这条上界现在由它定）；背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0），只在放背景动画的 8 帧上、八帧合计 2522 个像素',
    issue: 'xl-9bd.17 / xl-7ip / xl-x6w',
    // **逐像素相等区**（xl-aq0）：提示图那一层的目标矩形，每一帧从真值的
    // `reminder.dx1..dy2` 现读，**区里一个超容差的像素都不许有**。守的是
    // xl-ttu 那个改动 —— 缩放采样按原版的定点规律在 CPU 上做，而不是交给 GPU
    // 的最近邻。把渲染器换回改前那一版，`pnpm test` 全绿、整屏的 maxRatio 也
    // 照样过（111 个像素只占 0.017%），只有这条会红。理由与另外两条被否掉的
    // 路见 `exactRegions.ts` 的文件头。
    //
    // 实测（`--every 25`，容差 8）：47 帧里 **3 帧**画着提示图，
    // t=50 是 110×22 的 0/2420、t=800 是 120×24 的 0/2880、t=850 是 40×8 的 0/320。
    // 三帧背景动画都放着（伏虎冲天 / 妙手回春 / 蝶影神灵），区内照样一个不差。
    exact: [{ ...REMINDER_EXACT, drawnTicks: 84 }],
  },
  'battle-lu-skills': {
    status: 'gap',
    // 陆雪琪的五招（xl-rh9.14，1100 步）。实测 44 帧，状态栏之外有差异的 14 帧
    // 全是背景动画在放。**xl-x6w 之后 2026-09-08 重量**：
    //
    //          整屏占比(今天)  区外(今天)  区外(xl-x6w 前)
    //   t=50      0.6290%        415          427   灵凤吐珠 第 17 帧 ← 最差帧
    //   t=100     0.5838%         58           60   踏月无痕 第 3 帧
    //   t=125     0.5765%         12           13   踏月无痕 第 28 帧
    //   t=150     0.5739%          1            3   踏月无痕 第 53 帧
    //   t=250     0.5682%          3            4   星火乾坤圈 第 24 帧
    //   t=300     0.5684%         54           55   亟电崩离 第 2 帧
    //   t=325     0.5673%         47           49   亟电崩离 第 27 帧
    //   t=375     0.5727%        110          103   劈风追月 第 13 帧
    //   t=400     0.5524%         23           18   劈风追月 第 38 帧
    //   t=425     0.6224%        460          548   劈风追月 第 63 帧
    //   t=600     0.5614%         11           15   劈风追月 第 2 帧
    //   t=625     0.5501%         16           20   劈风追月 第 27 帧
    //   t=650     0.5603%          3            1   劈风追月 第 52 帧
    //   t=675     0.5582%         71           87   劈风追月 第 77 帧
    //   其余 30 帧 0.15..0.60%      0            0
    //
    // **这条剧本是四条里唯一全面变好的**：区外合计 1403 → 1284，最差帧从 #425
    // 换成 #50 并且**降到 0.6290%**（#425 自己从 0.6354% 降到 0.6224%，让位是因为
    // 它降得比 #50 多）。上界因此**收紧**。十四帧里十一帧变好、三帧变差
    // （t=375/400/650），最大的一笔变差是 +7 个像素。
    //
    // xl-7ip 之前（q80）这条剧本最差 **#425 6.9888%**、次差 #625 6.0075%；现在
    // 最差的 #50 那 4122 个超容差像素里区外只占 415 个，其余 3707 个是状态栏那几行
    // 字 —— **上界由字形定着**。#625 也从次差掉进了普通帧（0.5501%，区外 16）。
    //
    // ⚠️ 反过来不成立，这里有**唯一一个反例**：t=450 背景动画照放（劈风追月
    // 第 88 帧），可状态栏之外一个像素都不差。所以这条剧本的注释里那句对应
    // 只能写单向的："区外有差异 ⇒ 背景动画在放"。**xl-x6w 重量之后它还是唯一
    // 一个**（15 帧 bgDrawn=true，区外为 0 的仍只有 t=450 —— 逐帧从真值现读核的）。
    //
    // 上界按仓库规矩取最差帧的 2 倍：0.6290%（4122/655360）→ **1.2579%**
    // （xl-x6w 收紧，此前是 0.6354% → 1.2708%）。
    maxRatio: 0.012579,
    why: '状态栏那几行字（字形，这条上界现在由它定）；背景动画的有损重编码（JPG 源 → cwebp -q 95 -sns 0），只在放背景动画的 14 帧上、十四帧合计 1284 个像素',
    issue: 'xl-9bd.17 / xl-7ip / xl-x6w',
    // **逐像素相等区**（xl-aq0）：提示图那一层的目标矩形，每一帧从真值的
    // `reminder.dx1..dy2` 现读，**区里一个超容差的像素都不许有**。守的是
    // xl-ttu 那个改动 —— 缩放采样按原版的定点规律在 CPU 上做，而不是交给 GPU
    // 的最近邻。把渲染器换回改前那一版，`pnpm test` 全绿、整屏的 maxRatio 也
    // 照样过（111 个像素只占 0.017%），只有这条会红。理由与另外两条被否掉的
    // 路见 `exactRegions.ts` 的文件头。
    //
    // 实测（`--every 25`，容差 8）：44 帧里 **5 帧**画着提示图，
    // t=50 与 t=375 是 120×24 的 0/2880、t=100 是 30×6 的 0/180、
    // t=300 与 t=600 是 20×4 的 0/80。
    exact: [{ ...REMINDER_EXACT, drawnTicks: 126 }],
  },
  'battle-mishu-zhang': {
    status: 'gap',
    // 张小凡的秘术金钟罩（xl-rh9.14，600 步）。**分区表态** —— 实测 24 帧，
    // 每一帧的每一个超容差像素都落在张小凡那一格的文字里，最差帧 2128 个；
    // 那块之外，24 帧**一个像素都不差**（末帧连那块也是 0）。
    //
    // 这条剧本只有张小凡出战，所以只有一个缺口区（另外两格根本不画）。
    // 它比整屏上界硬：金钟罩那张状态图标、秘术动画 18 帧、怒气槽清零、
    // 控制台四颗按钮的贴图，全在硬比区里逐像素相等。
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 2128 个，上界取 2 倍。
        maxPixels: 4256,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '状态栏第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩状态栏那三行字的字形；这条剧本不放背景动画，所以没有第二笔账',
    issue: 'xl-9bd.17',
  },
  // ⚠️ **上面这条是 xl-7ip 的反向判据，2026-09-07 真跑过。** 背景动画的编码从
  // q80 换到 q95 之后重跑 `tools/compare-frames.sh battle-mishu-zhang`：硬比区
  // 仍是 24 帧逐像素相等、`panel-text-zhang` 合计 44598、最差 2128/4256 ——
  // 与换编码之前 xl-rh9.18 记下的数**逐字相同**。它本就不该动（`heroMishu`
  // 一句 `backgroundAnimation.set` 都没有），所以「它动了」才是信号：那说明
  // 改到的不只是背景动画那 753 张。下面 `battle-mishu-yu` 同理。
  //
  // **xl-x6w（2026-09-08）第三次跑了同一条反向判据，仍逐字相同**：加 `-sns 0`
  // 并把 753 张全裁之后，硬比区仍是 24 帧逐像素相等、`panel-text-zhang` 合计
  // 仍是 44598、最差仍是 2128/4256。这一趟它尤其该跑：编码设置与裁剪范围**同时**
  // 动了，「只波及背景动画」这句话比 xl-7ip 那趟更需要一个反证。
  // 配套的正面读数是改动面本身 —— 那两个提交动的 753 个 `.webp` **全部**在
  // `web/public/battle-anim/背景动画/` 下，技能动画那 1017 张与其余有损路径
  // 一个字节没动（`git diff --name-only -z master...HEAD` 现数的）。
  'battle-mishu-yu': {
    status: 'gap',
    // 文敏的秘术潜能爆发（xl-rh9.14，775 步）。同样是**分区表态**：实测 31 帧，
    // 超容差像素全部落在文敏那一格的文字里，最差帧 2202 个，区外恒 0。
    //
    // 这条剧本还顺带钉住了一个原版缺陷的画面：潜能爆发退回去那一下属性折半、
    // `refreshValue()` 把 hp 夹回新上限，那几帧的血条宽度也在硬比区里。
    //
    // xl-7ip 的反向判据（2026-09-07 重跑）：换 q95 之后硬比区仍是 31 帧逐像素
    // 相等、`panel-text-yu` 合计 64463、最差 2202/4404，与换之前逐字相同。
    // **xl-x6w（2026-09-08）第三次重跑，三个数仍逐字相同**：加 `-sns 0` 且
    // 753 张全裁之后，硬比区 31 帧逐像素相等、合计 64463、最差 2202/4404。
    gaps: [
      {
        name: 'panel-text-yu',
        // 实测最差帧 2202 个，上界取 2 倍。
        maxPixels: 4404,
        rect: { x0: 410, y0: 545, x1: 652, y1: 614 },
        why: '状态栏第二格（文敏）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩状态栏那三行字的字形；这条剧本不放背景动画，所以没有第二笔账',
    issue: 'xl-9bd.17',
  },
  'battle-mishu-lu': {
    status: 'gap',
    // 陆雪琪的秘术召出小精灵那一条（xl-rh9.14，1080 步）。第 11 层由 xl-rh9.15
    // 画出来之后，它从 `unpainted` 换成了这条**真量出来的**分区表态。
    //
    // 实测（`tools/compare-frames.sh battle-mishu-lu --every 25`，44 帧，
    // 容差 8）：
    //
    //   硬比区 44 帧**逐像素相等**（三个文字缺口区之外，超容差的像素合计 **0** 个）
    //   panel-text-zhang  合计 65974、最差 1637（#325）
    //   panel-text-yu     合计 64491、最差 1637（#150）
    //   panel-text-lu     合计 87794、最差 2140（#700）
    //
    // 那个 **0** 就是这张票的判据，逐样点名它盖住了什么：
    //
    // - **小精灵本体**画在 `(pet.x, pet.y)`，而 `pet.x/y/isDraw` 三个字段
    //   **一个都不在行为真值里**（导出器的 `snapshotState` 没取 `bp.pet`）。
    //   所以位置对不对、`Pet.update()` 那个浮动的相位对不对，逐步的 `toEqual`
    //   一个字都验不到 —— 只有这 44 帧的像素验得到。**浮动一轮是 9 拍**
    //   （原版注释说的"上浮五拍、下沉五拍"是错的，来历只在 `battle/step.ts`
    //   的 `updatePet` 上写一份），而取样点是 25 的倍数 —— `gcd(25, 9) = 1`，
    //   所以召出来（第 855 拍）之后的 9 个取样点**把 9 个相位一人一次全采到
    //   了**：875/900/925/950/975/1000/1025/1050/1075 的相位依次是
    //   2/0/7/5/3/1/8/6/4，实测 y 依次是 398/400/398/(没画)/397/399/399/
    //   397/396。那个"没画"正是下一条说的出手那一段。
    // - **行动条上那一颗小头像**：真值里有 `bar.pet`（petX），可"该不该画
    //   这一颗"不在真值里。原版判的是 `bp.pet!=null` 而**不是** `pet.isDraw`
    //   —— 小精灵出手那几拍本体消失、这一颗还在，那几拍也在这 0 里。
    // - **`小精灵攻击` 那 22 帧动画**与它打出去的伤害数字。
    //
    // 这条剧本**不放背景动画**（秘术与小精灵攻击都不走 `skillAttack`，
    // `backgroundAnimation.isDraw` 全程 false），所以没有 xl-7ip 那笔账，
    // 剩下的差异只有状态栏那九行字。
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 1637 个，上界取 2 倍。
        maxPixels: 3274,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '状态栏第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-yu',
        // 实测最差帧 1637 个，上界取 2 倍。
        maxPixels: 3274,
        rect: { x0: 410, y0: 545, x1: 652, y1: 614 },
        why: '状态栏第二格（文敏）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-lu',
        // 实测最差帧 2140 个，上界取 2 倍。陆雪琪那一格最大，因为她放秘术
        // 掉灵力、字数比另外两格多变几次。
        maxPixels: 4280,
        rect: { x0: 732, y0: 545, x1: 974, y1: 614 },
        why: '状态栏第三格（陆雪琪）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩状态栏那九行字的字形；小精灵那三层（本体 / 行动条上那一颗 / 攻击动画）在硬比区里逐像素相等',
    issue: 'xl-9bd.17',
  },
  'battle-victory': {
    status: 'gap',
    // **第一条走到结算之后的剧本**（xl-rh9.13，837 步，一路记到原版自己
    // `switchTo("scene")`）。第 22 层画出来之后实测
    // （`tools/compare-frames.sh battle-victory`，`--every 25`，34 帧，容差 8）：
    //
    //   硬比区 34 帧**逐像素相等**（区外超容差的像素合计 **0** 个）
    //   panel-text-zhang   合计 65480、最差 2047
    //   panel-text-yu      合计 65727、最差 2045
    //   panel-text-lu      合计 49144、最差 1612
    //   victory-attr-nums  合计   453、最差  453
    //   victory-exp-nums   合计  2741、最差 1683
    //   victory-loot       合计  4132、最差 1033
    //
    // 也就是说：卷轴从 y=80 往下拉开的每一格、物品框从中心对开的每一格、
    // 第一页三张人物图、第二页那张（只有升了级的陆雪琪）、升级小图、
    // 「获得物品」图 —— 全部逐像素相等。剩下的差异**只有字**：状态栏那九行，
    // 加上结算画面上的三栏数字。
    //
    // 六个矩形是量出来的，不是画出来的：34 帧合起来，超容差的像素落在十二段
    // 行区间上，每一段的列区间都干净地分成几簇 ——
    //
    //   行 137–211 列 695–764              掉落物三行 + 「金钱 N」（x=690）
    //   行 177–440 列 532–580              两栏经验数字（x=532）
    //   行 368–440 列 502–521              陆雪琪那四行属性（x=502）
    //   行 547–612 列 92–178 / 240–327 …   状态栏，与 battle-min 同一块地方
    //
    // 这笔账与 battle-min / dorm-intro 是**同一笔**：原版 `文鼎粗钢笔行楷`
    // 未交付，两端各退到自己的后备链（xl-9bd.17 已裁定不打包字体）。
    // 结算画面上没有一处是「画错了」的 —— 位置全对，笔画不同。
    //
    // ⚠️ **上面那组读数是 837 步那一版剧本的**。xl-03x.14 在剧本里加了一步
    // `debugKill`（第一回合打完、控制台一出来就按 J 秒杀），真值缩到 **158 步**，
    // 重量（2026-09-11，`tools/compare-frames.sh battle-victory`，`--every 25`，
    // **7 帧**，容差 8）：硬比区 7 帧逐像素相等；六个区的最差帧
    // 2047 / 2045 / 1612 / 453 / **2047** / 1033 —— 只有 victory-exp-nums 从 1683
    // 涨到 2047（仍在 3366 以内），其余与旧读数相同。上界没动。
    // 秒杀之后控制台与指令提示**一直盖在结算画面上**（原版行为，见 `step.ts` 的
    // `debugKill`），那几层也落在硬比区里、逐像素相等。
    why: '状态栏九行字 + 结算画面三栏数字的字形（原版 文鼎粗钢笔行楷 未交付）',
    issue: 'xl-9bd.17',
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 2047 个（2026-09-07，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 4094,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-yu',
        // 实测最差帧 2045 个，上界取 2 倍。
        maxPixels: 4090,
        rect: { x0: 410, y0: 545, x1: 652, y1: 614 },
        why: '第二格（文敏）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-lu',
        // 实测最差帧 1612 个，上界取 2 倍。
        maxPixels: 3224,
        rect: { x0: 732, y0: 545, x1: 974, y1: 614 },
        why: '第三格（陆雪琪）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'victory-attr-nums',
        // 实测最差帧 453 个（合计也是 453 —— 34 帧里只有一帧采样到了第二页），
        // 上界取 2 倍。
        maxPixels: 906,
        rect: { x0: 498, y0: 364, x1: 526, y1: 444 },
        why: '第二页那四行属性数字的字形（这一场只有陆雪琪升级，所以只有第三行）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'victory-exp-nums',
        // 实测最差帧 1683 个，上界取 2 倍。
        maxPixels: 3366,
        rect: { x0: 528, y0: 173, x1: 584, y1: 444 },
        why: '第一页每人两行经验数字（这一场获得多少 / 还差多少）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'victory-loot',
        // 实测最差帧 1033 个，上界取 2 倍。
        maxPixels: 2066,
        rect: { x0: 691, y0: 133, x1: 768, y1: 215 },
        why: '掉落物三行与「金钱 N」那一行的字形',
        issue: 'xl-9bd.17',
      },
    ],
  },
  'battle-em3-box': {
    status: 'gap',
    // em3 命中框那一场（xl-rh9.8 补齐状态层，xl-rh9.9 接上画面）。实测
    // （`--every 25`，30 帧，容差 8）最差帧 #50 **0.9467%**，包围盒
    // (92,547)-(971,612) —— 与 battle-min 同一块地方，也就是同一笔字形账。
    // 上界取 2 倍 = 1.89%。
    //
    // 它比 battle-min 宽 20 px，因为这一场三个人的等级是 10 级，血量数字
    // 多了一位（`1960/1960`）。
    //
    // **为什么这一条是整屏表态而不是分区**：票面要的是 battle-min 进分区、
    // 其余四条写量出来的 gap。顺带记一个实测事实给下一个人：这四条的偏离
    // 也全落在状态栏那三块文字上，硬比区照样是干净的 —— 也就是说它们随时
    // 可以照 battle-min 的样子改成分区表态，只差把三个矩形按各自的数字宽度
    // 重量一遍。
    maxRatio: 0.018934,
    why: '状态栏那九行字的字形（同 battle-min，10 级队伍的数字多一位）',
    issue: 'xl-9bd.17',
  },
  'battle-defeat-scene': {
    status: 'gap',
    // 打输回地图那条出口（xl-rh9.3 的真值，xl-rh9.8 的状态层）。实测
    // （`--every 25`，11 帧）最差帧 #50 **0.8916%**，包围盒 (92,547)-(971,612)，
    // 又是状态栏那三块字。上界取 2 倍 = 1.78%。
    //
    // **11 帧里有 1 帧差 0 个像素**：末帧 t=250，全灭图已经从两侧对开到合拢
    // （`lsx2` 每拍 +8，到 512 就盖满），整屏被那两张图盖住，连状态栏的字都
    // 盖掉了。也就是第 24 层「全灭图」那两个 1:1 的源矩形是**逐像素对上的** ——
    // 这一帧是它唯一的正面判据，三条打输剧本各有一帧。
    maxRatio: 0.017832,
    why: '状态栏那九行字的字形（同 battle-min）；末帧全灭图盖满，差 0 个像素',
    issue: 'xl-9bd.17',
  },
  'battle-defeat-start': {
    status: 'gap',
    // 打输回标题那条出口。实测（`--every 25`，8 帧）最差帧 #25 **0.4640%**，
    // 包围盒 (105,547)-(629,612)。上界取 2 倍 = 0.93%。
    //
    // 包围盒只到 x=629，因为这一场**只有两个人出战**（张小凡 + 文敏），
    // 第三格整格空着 —— 原版那里是 `drawImage(null,…)`，什么都不画，而 web
    // 侧也什么都不画，于是那一格逐像素相等。这条顺带证明了状态栏是**按格位**
    // 画的、缺席的人不会让后面的人往前挪。
    //
    // 末帧（t=175）同样是全灭图盖满、差 0 个像素。
    maxRatio: 0.00928,
    why: '状态栏那六行字的字形（这一场只有两人出战，第三格整格空着）',
    issue: 'xl-9bd.17',
  },
  'battle-defeat-slot2': {
    status: 'gap',
    // 罹年居士在第 2 槽、照样回标题（xl-rh9.6 的真值）。实测（`--every 25`，
    // 5 帧）最差帧 #25 **0.5193%**，包围盒 (105,547)-(629,612) —— 与
    // battle-defeat-start 同形（也是两人出战）。上界取 2 倍 = 1.04%。
    // 末帧（t=100）全灭图盖满，差 0 个像素。
    maxRatio: 0.010386,
    why: '状态栏那六行字的字形（同 battle-defeat-start）',
    issue: 'xl-9bd.17',
  },
  // xl-3hn：主线剧情战原样照搬的那十份。账都只有状态栏那一笔。
  'battle-script3': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，45 帧，容差 8）最差 #700 **0.5064%**，
    // 包围盒 (92,547)-(951,612) —— 只剩状态栏那几行字，与 battle-min 同一笔
    // 字形账。两人出战（张小凡 + 陆雪琪），中间那格空着，所以只有两个区。
    //
    // 原先还有第二笔：背景图「校园小道」半透明的边，45 帧**每一帧**状态栏之外
    // 恰好差 1665 个像素，包围盒整屏、最差 0.7605%。成因是原版离屏缓冲非预乘
    // ARGB、从不清屏、上屏扔 alpha；xl-84z 让战斗渲染器画进一张持久缓冲再反预乘
    // 上屏（`battle/render/battleRenderer.ts` 文件头第四处），区外归零。
    //
    // **区外一个像素都不许差**，这比按比例给上界严：那一笔回来时最差帧才
    // 0.76%，按比例的上界拦不住它。
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
    gaps: [
      {
        name: 'panel-text-zhang',
        // 实测最差帧 1680 个（f000700），上界取 2 倍。
        maxPixels: 3360,
        rect: { x0: 88, y0: 545, x1: 330, y1: 614 },
        why: '第一格（张小凡）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'panel-text-lu',
        // 实测最差帧 1660 个（f000175），上界取 2 倍。
        maxPixels: 3320,
        rect: { x0: 732, y0: 545, x1: 974, y1: 614 },
        why: '第三格（陆雪琪）的等级 / 血 / 灵力三行字的字形',
        issue: 'xl-9bd.17',
      },
    ],
  },
  'battle-script6': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，19 帧，容差 8）最差 **0.5310%**，包围盒
    // (92,547)-(951,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。两人出战（张小凡 + 陆雪琪），第三格空着，所以比 battle-min 窄。
    // 上界取 2 倍。
    maxRatio: 0.01062,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'battle-script12': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，58 帧，容差 8）最差 **0.9103%**，包围盒
    // (92,547)-(971,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。三人出战。
    // 上界取 2 倍。
    maxRatio: 0.018206,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'battle-script15': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，40 帧，容差 8）最差 **0.9167%**，包围盒
    // (92,547)-(971,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。三人出战。
    // 上界取 2 倍。
    maxRatio: 0.018334,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'battle-script17': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，27 帧，容差 8）最差 **0.8870%**，包围盒
    // (92,547)-(971,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。三人出战。
    // 上界取 2 倍。
    maxRatio: 0.01774,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'battle-script25': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，37 帧，容差 8）最差 **0.6456%**，包围盒
    // (92,547)-(649,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。两人出战（张小凡 + 文敏）。
    // 上界取 2 倍。
    maxRatio: 0.012912,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'battle-script31': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，38 帧，容差 8）最差 **0.6383%**，包围盒
    // (92,547)-(649,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。两人出战（张小凡 + 文敏）。
    // 上界取 2 倍。
    maxRatio: 0.012766,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'battle-script38-2': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，30 帧，容差 8）最差 **0.9656%**，包围盒
    // (92,547)-(971,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。三人出战。
    // 上界取 2 倍。
    maxRatio: 0.019312,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'battle-script38-3': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，31 帧，容差 8）最差 **0.9656%**，包围盒
    // (92,547)-(971,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。三人出战。
    // 上界取 2 倍。
    maxRatio: 0.019312,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'battle-script39': {
    status: 'gap',
    // 实测（2026-09-12，`--every 25`，69 帧，容差 8）最差 **0.9534%**，包围盒
    // (92,547)-(971,612) —— 状态栏那几行字，与 battle-min 同一笔字形账。三人出战。
    // 上界取 2 倍。
    maxRatio: 0.019068,
    why: '状态栏那几行字的字形（同 battle-min）',
    issue: 'xl-9bd.17',
  },
  'dorm-walk': {
    status: 'gap',
    // 地图底图已逐像素对齐（xl-9bd.16），NPC 已实现并逐 tick 对齐（xl-9bd.9），
    // 对话框已实现（xl-9bd.10）。
    //
    // 对话框接进来之后最差帧从 12.73% 降到 1.01%（实测 --every 100），剩下的
    // 那一块**只有正文的字形**：差异图上对话框的边框、名字牌与头像整个是暗的
    // （= 逐像素相同），亮起来的只有字。原因是原版用的 `文鼎粗钢笔行楷` 绝大
    // 多数机器上没有，Java2D 的基线与 DOM 的行盒也不是一回事。
    //
    // xl-9bd.17 已裁定：**不打包字体**，把 web 的后备链换成与基准同一个落点
    // （本机默认无衬线，见 `src/textFont.ts`）。同一条命令的最差帧 #500 因此
    // 从 1.0056% 降到 0.7401%，包围盒不变。剩下的字形差是接受的账。
    why: '对话正文、名字牌与右下角金币数的字形（字体不在，基线也不同）',
    issue: 'xl-9bd.17',
    // 这条是**第一条分区表态**（xl-1vu.3）。整屏一个状态时它只说得出"红着"，
    // 而逐帧把差异按区拆开之后，22 帧（`--every 25`，容差 8）里剩下的偏离
    // 全落在下面这三块，**三块之外一个像素都不差**：
    //
    //   t=0..300、400、525       缺口区之外 0 个（走动、跑动、对话收掉之后）
    //   t=425                    speech-name 里 1397 个 —— 名字牌上的三个字
    //   t=450..500               speech-name + speech-text 合计
    //                            1588/3098/4323 个 —— 名字与正文的字形
    //   全部 22 帧               coin-hud 里恒 527 个
    //
    // 也就是说：地图、家具、NPC、主角的走动帧**与跑动帧**、对话框的边框与
    // 名字牌本身，在这条剧本的每一帧上都是**逐像素相等**的。这是分区判据买到的
    // 东西 —— 以前这句话只能靠读注释相信，现在少一个像素就红。
    //
    // 跑动帧原先在这里挂着一个 `hero-run` 缺口区（412/360/368 个像素）。
    // xl-u39 把它还掉了：起因是 `roles/zhangxiaofanRun/15.png`、`16.png` 是
    // 42×65 而原版 `drawHero` 的源矩形只取 42×64，Web 侧却是 `setSize(42,64)`
    // 缩下来 —— 裁与缩的区别。烘焙器改成按源矩形裁之后这三帧归零，而**这张表
    // 没改就红了**（"缺口区一帧都不差了"），正是分区判据该有的样子。
    gaps: [
      // 原先这里是 `coin-hud`（925..993 × 615..630，恒差 527，挂 xl-yg6.1）。xl-yg6.12 把
      // addMap 那一层画上之后图标那一截归零，区换成只剩数字的 coin-digits。
      // 实测外接框 (940,617)-(993,630)，单帧最多 520（#0），22/22 帧。
      coinDigits(520),
      {
        name: 'speech-name',
        // 实测最差帧 1397 个（2026-09-06，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 2794,
        rect: { x0: 0, y0: 0, x1: 271, y1: 40 },
        // 名字牌（192×32）沿画面顶端从左边滑到 (272,32)，牌子本身逐像素对上，
        // 偏的只有牌子上的三个字。按 x=272 与 speech-text 切开只为不重叠。
        why: '名字牌滑入路径上的名字字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'speech-text',
        // 实测最差帧 4323 个（2026-09-06，`--every 25`、容差 8），上界取 2 倍。
        maxPixels: 8646,
        rect: { x0: 272, y0: 0, x1: 752, y1: 192 },
        // 样式 1 的对话框：从 (752,192) 往左上长到 (272,32)，正文原点
        // (boxX+40, boxY+40)。见 `ui/dialogueLayout.ts`。
        why: '样式 1 对话框里的正文字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
    ],
  },
  'bigmap-walk': {
    status: 'gap',
    // 大地图这条剩下的是**素材重编码**，不是渲染：大地图.jpg 烘成 q80 的有损
    // WebP，把烘焙产物解回来按同一个采样公式取，与原版第 0 帧就已经差 37.22%。
    // 幅度上界（xl-l3o）：这一次实测最差帧 #475 **37.1101%**（整屏，34 帧各帧
    // 都在 37% 上下），上界取 2 倍 = 74.22%。全黑图给到 99.96%，在上界之上，会红。
    //
    // ⚠️ **xl-yg6.12 换成了分区表态**（2026-09-10，34 帧，`--every 25`，容差 8）。
    // 大地图.jpg 改走 near_lossless 40（`bake.ts` 的 `MAP_NEAR_LOSSLESS`）之后重编码这一笔
    // 归零，底下露出 `OtherEvent.addMap` 那一层 —— 右下角 (914,588)-(1012,635) 那一块是
    // 体育馆门匾的遮掩图，web 侧整层没画；那一层画上之后（`scene/mapOverlays.ts`）也归零。
    // **剩下的只有右下角金币数的字形**，区外 0 个。
    gaps: [
      // 实测外接框 (940,617)-(993,630)，单帧最多 580（#825），26/34 帧。
      coinDigits(580),
    ],
    why: '只剩右下角金币数的字形；地图、十张遮掩图、主角与 NPC 逐像素相等',
    issue: 'xl-9bd.17',
  },
  'dorm-exit': {
    status: 'gap',
    // 出口切换（xl-9bd.12）。这条剧本横跨三个场景，于是把另外两条各自的缺口
    // 都继承了过来，一笔新账都没添。实测（`tools/compare-frames.sh dorm-exit
    // --every 100`，12 帧，容差 8）逐帧分得干干净净：
    //
    //   宿舍段（t=0..400，5 帧）  固定偏 527 个像素、0.0804%，包围盒恒为
    //                             (925,615)-(993,630) —— 右下角还没画的金币
    //                             HUD（xl-yg6.1），跟 dorm-intro 那条量到的
    //                             是同一个数。
    //   大地图段（t=500..800）    整屏 34.01%–34.07%，包围盒是整张画布 ——
    //                             大地图.jpg → q80 WebP 的重编码差异
    //                             （xl-9bd.14，bigmap-walk 那条量到 37.22%）。
    //   脚本1 段（t=900）         又回到 527 个像素、0.0804%，包围盒一模一样；
    //                             t=1000/1100 是 3970 个像素、0.6058%，包围盒
    //                             (200,503)-(993,630)，正是对话框那一块加
    //                             HUD —— 对话正文的字形（xl-9bd.17）。
    //                             （这一格在 xl-9bd.17 合并前量到的是 5592 个
    //                             像素、0.8533%：换掉楷体后备链之后字形差本身
    //                             缩了，与 dorm-walk 最差帧 1.0056%→0.7401%
    //                             是同一回事。）
    //
    // 进第三个场景的那一刻重新掉回**跟出门前逐像素一样的那 527 个**（t=900、
    // 925 两帧，一个字都还没打），这就是可核的证据：地图、主角、NPC 都落在了
    // 对的地方，换场景没带进任何新的偏离；之后涨上去的全在对话框那个包围盒里。
    // 注意第三段是**脚本1**，不是走回宿舍 —— 路线是 宿舍 → 大地图 → 脚本1。
    // 幅度上界（xl-l3o）：这一次实测最差帧 #875 **34.0767%**（大地图那一段，
    // 整屏），上界取 2 倍 = 68.15%。
    //
    // ⚠️ **xl-yg6.12 换成了分区表态**（2026-09-10，48 帧，`--every 25`，容差 8）。大地图段
    // 那 34% 的重编码归零（near_lossless 40），大地图右缘那几块遮掩图的账随 addMap 那一层
    // 画上也归零。剩下两块，区外 0 个：
    gaps: [
      {
        name: 'bottom-text',
        // 实测外接框 (200,503)-(599,552)，单帧最多 3443（#1000），10/48 帧 —— 脚本1 段
        // 底部样式 2 对话框的前两行。
        maxPixels: 6886,
        rect: { x0: 194, y0: 497, x1: 605, y1: 558 },
        why: '脚本1 段底部对话框正文的字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      // 实测外接框 (940,617)-(993,630)，单帧最多 561（#500），48/48 帧。
      coinDigits(561),
    ],
    why: '对话正文与右下角金币数的字形；三个场景的地图、遮掩图、人物与换场景逐像素相等',
    issue: 'xl-9bd.17',
  },
  'milestone': {
    status: 'gap',
    // M1 的里程碑剧本（xl-9bd.13）：原版开场一条龙 —— 脚本1 的旁白与 23 句
    // 主线对话、跟曾书书搭一次话、出宿舍门进大地图夜（脚本2）、那边的旁白与
    // 27 句对话、进大活夜（背景音乐第一次切换）、再出来回到大地图夜（第二次
    // 切换）。它把另外四条机制剧本各自量到的缺口原样继承过来，**没有添一笔
    // 新账**。实测（`tools/compare-frames.sh milestone --every 250`，37 帧，
    // 容差 8）逐段分得干干净净：
    //
    //   脚本1 的旁白段（t=0..750）    第 0 帧只差 15 个像素（旁白背景缩放的
    //                                 取整边界，与 dorm-intro 同一笔），有字的
    //                                 三帧 **0.9096%/1.8335%/2.7330%**，包围盒
    //                                 是旁白正文那一片 —— 只剩字形（xl-9bd.17）。
    //                                 xl-9bd.18 把旁白改成逐字按格摆之前，这三
    //                                 帧是 1.74%/3.71%/5.42%，多出来的是每字约
    //                                 0.37 px 的累积横向漂移。其余各段一个数都
    //                                 没动（那些帧没有旁白），实测重跑
    //                                 `--every 250` 的 37 帧逐帧核对过。
    //   脚本1 的对话段（t=1000..4250）0.28%–0.79%，包围盒 (200,503)-(993,630)
    //                                 = 对话框那一块加 HUD；其中 t=1750 差
    //                                 **0 个像素**（对话框已弹出、一个字还没
    //                                 打），t=2500/3500/4250 回到 527 个像素、
    //                                 0.0804%、包围盒恒为 (925,615)-(993,630)
    //                                 —— 只剩右下角还没画的金币 HUD（xl-yg6.1）。
    //   脚本2 段（t=5250..8250）      整屏 25.85%–28.72%，包围盒是整张画布 ——
    //                                 大地图夜.jpg → q80 WebP 的重编码差异
    //                                 （xl-9bd.14，bigmap-walk 在 大地图.jpg 上
    //                                 量到 37.22%）。
    //   大活夜段（t=8500、8750）      **553 个像素、0.0844%，包围盒又是
    //                                 (925,615)-(993,630)**。大活.png 烘成
    //                                 无损 WebP，于是这一段只剩 HUD。
    //   大地图夜段（t=9000）          28.98%，整屏 —— 又是那张 jpg。
    //
    // 大活夜那两帧是这条剧本最有分量的证据：连过两道门、跨了四个场景之后，
    // 偏离**掉回只剩右下角那一小块**。地图、主角、NPC、对话框、旁白都落在了
    // 对的地方，出口与音乐切换没带进任何新的偏离。
    //
    // 与像素无关的那一半判据在 `src/state/traceReplay.test.ts`（逐 tick 的
    // 主角/NPC/对话/旁白/视口全部对齐）与 `src/state/milestone.test.ts`
    // （这条剧本确实把四个环节都走到了）里，两边都不许有例外。
    // 幅度上界（xl-l3o）：这一次实测最差帧 #8825 **29.1754%**（大地图夜那一段，
    // 整屏），上界取 2 倍 = 58.35%。
    //
    // ⚠️ **xl-yg6.12 换成了分区表态**（2026-09-10，367 帧，`--every 25`，容差 8）。大地图夜
    // 那 25%~29% 的重编码归零（near_lossless 40），遮掩图的账随 addMap 那一层画上归零。
    // 剩下的与 dorm-intro **同一组矩形**（开场同是脚本1），逐区读数是这条剧本自己量的：
    // 与 dorm-intro 不同的只有 top-text-4（1983 @ #5125，脚本2 那段）与金币数（584 @ #8875）。
    // 区外 0 个。
    gaps: script1Gaps({
      speechName: 1295,
      topText: [3707, 4950, 4638, 1983, 3466, 1366],
      bottomUpper: 11130,
      bottomLower: 2389,
      coin: 584,
    }),
    why: '旁白、对话正文与金币数的字形；四个场景的地图、遮掩图、人物、两道门与旁白背景逐像素相等',
    issue: 'xl-9bd.17',
  },
  'dorm-intro': {
    status: 'gap',
    // 旁白（xl-9bd.11）与对话框（xl-9bd.10）都接上之后实测
    // （`tools/compare-frames.sh`，每 25 个 tick 一帧，共 165 帧，容差 8）。
    // 两张票各自的表态都说"欠着对方"，合并之后两边都不欠了，剩下的是这三笔：
    //
    //   1. **字形。** 原版的 `文鼎粗钢笔行楷` 没有随游戏交付，两端各自退到
    //      后备字体，同样的基线坐标画出来的笔画不同。xl-9bd.17 已裁定不打包
    //      字体、只把 web 的后备链换成与基准同一个落点（本机默认无衬线），
    //      理由与三条链的实测表在 `src/textFont.ts`：165 帧的差异像素合计从
    //      1126591（1.0418%）降到 992667（0.9180%），而字最多的那一帧
    //      t=800 反向从 5.5397% 涨到 5.6325% —— 浏览器的粗体比 Java2D 重。
    //      对话段的最差帧仍是 t=3425（包围盒正是对话框那一块），从 3.0641%
    //      降到 2.1432%。**这一笔是接受的账，不是待补的功能。**
    //
    //      xl-9bd.18 又从这一笔里切走了**横向漂移**那一半：旁白原先是
    //      `fillText(整行)`，而浏览器给中文的步进不是原版 `FontMetrics` 那个
    //      整 20 px，一行 43 个字累起来差十几个像素。改成逐字按格摆之后
    //      （同一条命令重量两次，原版侧那 165 张位图完全相同，只重截 web 侧）：
    //      **合计 992667 个像素、0.9180% → 687547 个、0.6358%；最差帧 t=800
    //      从 5.6325% 降到 2.8482%**。t=800 那六行旁白的墨迹右边界
    //      （亮度 > 220，与 textFont.ts 同一条判据）由
    //      433/924/822/313/607/261 变成 **427/908/808/308/597/258**，
    //      原版是 426/908/808/308/597/253 —— 中间四行**逐像素相同**。
    //      第一行差 1 px 是末字的笔画本身，步进已经一样了；第六行
    //      '我们的故事就此展开......' 还差 5 px，是那六个**半角**句点：它们
    //      在两端的字体里步进本来就不同（基准侧 Java 量到 5 px），属于字形
    //      那笔账，不是排版。
    //      可核的证据：采样到的 165 帧里**有 8 帧差 0 个像素**（t=1425、1750、
    //      1925、2050、2950、3075、3175、3950），它们全是"对话框已弹出、头像
    //      正在滑入、一个字都还没打"（真值 printing=false、cursor=0）。也就是
    //      对话框的边框、名字牌、头像与滑入动画是逐像素对上的，偏的只有字。
    //   2. **右下角的金币 HUD 还没画**（xl-yg6.1）：没有对话框盖住它的帧固定
    //      偏 527 个像素、0.0804%，包围盒恒为 (925,615)-(993,630)。
    //   3. **旁白背景的缩放取整。** 第 0 帧（旁白已起、还没有字）只差 15 个
    //      像素，全在第 599 行，最大通道差 41 —— 639×395 拉满 1024×640 时最近
    //      邻的取整边界，两端各按各的规则舍入。占比 0.0023%，在判定阈值之内。
    // 当时的幅度上界（xl-l3o）：实测最差帧 #800 **2.8482%**（旁白字最多的那一帧），
    // 上界取 2 倍 = 5.70%。那个 2 倍余量正是这条剧本给出来的。
    //
    // ⚠️ **xl-yg6.12 把它换成了下面的分区表态**（2026-09-10，`--every 25`，165 帧，
    // 容差 8，矩形由 `scripts/measureGaps.ts` 从差异图上量）。整屏上界里任何地方
    // 画错了都藏得住，分区之后区外一个像素都不许差 —— 实测**区外 0 个**。
    //
    // 分区把上面第 3 笔账重新量了一遍，**它没有过去**：15 个像素只是第 0 帧的
    // 读数，旁白放着的 36 帧里第 599 行**整行**都可能差，单帧最多 264 个（#475）。
    // 它此前一直低于判定阈值（0.0002 × 655360 ≈ 131 个）不算偏离帧，藏在整屏
    // 上界里没人数过。成因是缩放采样的取整（GPU 最近邻 vs Java2D 不透明 blit），
    // 不属于字形 / 素材 / 签字偏离任何一族，单开 xl-t0h。
    //
    // ⚠️ **xl-03x.15 把这笔账还了**：旁白背景改由 CPU 按原版采样表拼（`narratageBgPasses`），
    // 第 599 行两条剧本旁白帧上一个像素都不差，那一区删掉；这一行剩下的对话框第 4 行
    // 字形并进 bottom-text-lower。
    //
    // 矩形与逐区的量法见 `script1Gaps`（与 milestone 共用）。
    gaps: script1Gaps({
      // 实测「单帧最多」（165 帧，`--every 25`，容差 8）：名字牌 #1625、六条横带
      // #3825/#775/#800/#700/#725/#800、底部 #3425、底部第 4 行
      // #3425、金币数 #825。底部第 4 行那一格 xl-03x.15 上沿并到 599 后重量：2219 → 2389
      // （多出来的 170 就是 y=599 那一行的对话框字形，与 milestone 同一读数）。
      speechName: 1295,
      topText: [3707, 4950, 4638, 1632, 3466, 1366],
      bottomUpper: 11130,
      bottomLower: 2389,
      coin: 520,
    }),
    why: '旁白、对话正文与金币数的字形（原版字体未交付）',
    issue: 'xl-9bd.17',
  },
  // ===================== 商店三条（xl-knp.10 接上 driver=shop） =====================
  //
  // 这三条从 `unassembled` 换成**真量出来的分区表态**。共同的几件事写在这里，
  // 逐条自己的读数在下面各自的 `shopGaps(...)` 实参里。
  //
  // - **矩形是从实际差异图上量出来的，不是照 `shop/layout.ts` 的常量写的。**
  //   量法留成了一个工具：`web/scripts/measureGaps.ts`。第一档把两侧位图逐帧
  //   对齐、取超容差像素的**并集**、连通聚类，交出每一块的外接框（shop 三条
  //   各聚出 75/76 块，全是列与行）；第二档拿划好的矩形回核，逐区报「单帧
  //   最多」，并且**把落在所有区之外的差异像素点出来**。
  // - **区外的读数是 0**：三条剧本 101 帧，硬比区里一个超容差的像素都没有。
  //   这就是「不许拿一个更大的框把没认出来的成因盖掉」那句话的可执行证据 ——
  //   多出一个像素落在没人声明过的地方，`regions.ts` 当场红。
  // - **成因只有一族：字形**（原版 `文鼎粗钢笔行楷` 未交付，后备链在
  //   `textFont.ts`），归 xl-9bd.17。菜单那边的另外两族在商店这边**都是零**，
  //   而那不是没查：`sources/Shop/` 那批素材 xl-knp.5 全烘进来了（缺的三张
  //   装备图两端都画不出来，见 `render/assets.ts` 的 `pictureTextureId`），
  //   ADR-0001 例外表里也没有商店的条目。**六个区逐个点名，一族都没混。**
  // - ⚠️ **反证做过**：区外零差异与「两端都什么都没画」长得一模一样。逐块回到
  //   原版位图上数「整条剧本里变过多少像素」（`shop-trade`，两侧各数一遍）：
  //   商品图 (820,200) 22910、招牌 (200,0) 3914/3916、店主动画 (364,515)
  //   3898/3897、张小凡动画 (0,0) 360、四行属性图标 (60,30..) 0（它本来就是四张
  //   不动的图）。也就是说硬比区里**真的有东西画过**，而且两端画的是同一批。
  // - ⚠️ **陆雪琪 (0,160) 与文敏 (0,320) 两条动画一帧都走不到**：三条商店剧本的
  //   `setup.party` 都只有 `zhang`（xl-knp.9 实测）。它们在这条流水线里是背景，
  //   要盖住得另补一条 party 更满的商店真值 —— 那属于补真值那一层，不在本票。
  // - ⚠️ **这条流水线守不住那八格动画怎么循环**：`ShopDriver` 把 Clock 倍率设成
  //   1e-9，动画线程整次导出停在第 0 格（`replay/main.ts` 的 `SHOP_FROZEN_FRAME`）。
  //   守那一层的是 `shop/render/animation.test.ts`（xl-knp.9）。
  //
  // 三条剧本的取帧密度都是 `every: 1`（剧本自报）：商店与菜单一样是**事件驱动**
  // 的，一步就是一次输入事件，落到默认的 25 会让 40 步只出 2 帧，却照样打印
  // 「比过了」。
  'shop-trade': {
    status: 'gap',
    // 40 帧。药店与装备超市各走一条完整的买卖（含买入被拒、加减两端）。
    // 实测：`tools/compare-frames.sh shop-trade`，容差 8。
    gaps: shopGaps({
      // 实测外接框 (453,183)-(574,581)，单帧最多 14113（第 24 帧）。
      nameAndPurchase: 14113,
      // 实测外接框 (638,185)-(732,580)，单帧最多 18570（第 29 帧）。
      priceAndStock: 18570,
      // 实测外接框 (758,184)-(772,580)，单帧最多 3520（第 24 帧）。
      held: 3520,
      // 实测外接框 (905,4)-(971,19)，单帧最多 650（第 0 帧）。
      coins: 650,
      // 实测外接框 (55,13)-(74,91)，单帧最多 545（第 0 帧）。
      // 这条剧本的 setup.party 只有 zhang，所以只有一组。
      statLabels: [545],
      // 实测外接框 (453,593)-(870,633)，单帧最多 6132（第 32 帧）。药店没有
      // 第三行（`ShopPanel` 没有 `messageremark`），所以它比另外两条窄。
      keeperMessage: 6132,
    }),
    why: '只剩字形（原版字体未交付）；商品图、招牌、钱袋、四条人物动画与全部按钮逐像素相等',
    issue: 'xl-9bd.17',
  },
  'shop-categories': {
    status: 'gap',
    // 25 帧。装备自选超市六类全走一遍。
    // 实测：`tools/compare-frames.sh shop-categories`，容差 8。
    gaps: shopGaps({
      // 实测外接框 (453,183)-(574,581)，单帧最多 14113（第 0 帧）。
      nameAndPurchase: 14113,
      // 实测外接框 (638,184)-(732,580)，单帧最多 18565（第 0 帧）。
      priceAndStock: 18565,
      // 实测外接框 (758,185)-(772,580)，单帧最多 3520（第 0 帧）。
      held: 3520,
      // 实测外接框 (905,4)-(971,19)，单帧最多 650（第 5 帧）。
      coins: 650,
      // 实测外接框 (55,13)-(74,91)，单帧最多 545（第 0 帧）。
      // 这条剧本的 setup.party 只有 zhang，所以只有一组。
      statLabels: [545],
      // 实测外接框 (453,593)-(951,633)，单帧最多 8041（第 2 帧）。装备店有
      // 第三行（`messageremark`，画在 x=730），所以右边比药店那条宽 81 px。
      keeperMessage: 8041,
    }),
    why: '只剩字形（原版字体未交付）；六类切换的按钮与商品图逐像素相等',
    issue: 'xl-9bd.17',
  },
  'shop-edges': {
    status: 'gap',
    // 36 帧。库存为 0 的那一件买下去、背包里没有那件东西按卖出。
    // 实测：`tools/compare-frames.sh shop-edges`，容差 8。
    gaps: shopGaps({
      // 实测外接框 (453,183)-(574,581)，单帧最多 14113（第 17 帧）。
      nameAndPurchase: 14113,
      // 实测外接框 (638,185)-(732,580)，单帧最多 18570（第 25 帧）。
      priceAndStock: 18570,
      // 实测外接框 (758,184)-(772,580)，单帧最多 3520（第 17 帧）。
      held: 3520,
      // 实测外接框 (905,4)-(971,19)，单帧最多 650（第 0 帧）。
      coins: 650,
      // 实测外接框 (55,13)-(74,91)，单帧最多 545（第 0 帧）。
      // 这条剧本的 setup.party 只有 zhang，所以只有一组。
      statLabels: [545],
      // 实测外接框 (453,593)-(951,633)，单帧最多 8152（第 18 帧）。
      keeperMessage: 8152,
    }),
    why: '只剩字形（原版字体未交付）；被拒的那两笔买卖只改店主那句话，其余逐像素相等',
    issue: 'xl-9bd.17',
  },
  'shop-party': {
    status: 'gap',
    // **队伍三个人都在的那一条**（xl-knp.11）。另外三条剧本的 `setup.party`
    // 只有 zhang，于是原版 `drawIcon` 里 `if(SaveAndLoad.lu)` 与
    // `if(SaveAndLoad.wen)` 两支一次都没走到 —— 陆雪琪 (0,160) 与文敏 (0,320)
    // 那两个 72×144 的框在另外三条里**每一帧都是 0/10368**（xl-knp.9 实测，
    // xl-knp.11 复量确认）。这一条把它们打开，于是那两条人物动画与另外两组
    // 属性图标真的进了逐帧比对。
    //
    // 原版侧那两个框的前后对照（`ExportTrace --frames --every 1` 出的位图与
    // `sources/Shop/shopback.png` 逐像素比，2026-09-09 实测）：
    //
    //   | 框 | shop-trade（40 帧） | shop-party（15 帧） |
    //   |---|---|---|
    //   | 陆雪琪 (0,160) | **0 / 10368**，每一帧 | **4487 / 10368**，每一帧 |
    //   | 文敏 (0,320)   | **0 / 10368**，每一帧 | **4844 / 10368**，每一帧 |
    //
    // 而这一轮比对的读数是「硬比区 15 帧逐像素相等」—— 也就是说那两条动画
    // 两端画的是同一批像素，不再是"两边都没画"。
    //
    // 15 帧。药店买一份金创药、装备店买一把月苗刀、切一次鞋子栏 —— 账目那一层
    // shop-trade 已经走完，这一条要的是**绘制**那一层的覆盖。
    // 实测：`tools/compare-frames.sh shop-party`，容差 8。
    gaps: shopGaps({
      // 实测外接框 (453,183)-(574,581)，单帧最多 14113（第 6 帧）。
      nameAndPurchase: 14113,
      // 实测外接框 (638,185)-(732,580)，单帧最多 18570（第 11 帧）。
      priceAndStock: 18570,
      // 实测外接框 (758,185)-(772,580)，单帧最多 3520（第 6 帧）。
      held: 3520,
      // 实测外接框 (905,4)-(971,19)，单帧最多 650（第 0 帧）。
      coins: 650,
      // **三组**，实测外接框 (55,13)-(74,91) / (55,163)-(74,241) /
      // (55,313)-(74,391)，单帧最多 545 / 545 / 547（都在第 0 帧）。
      statLabels: [545, 545, 547],
      // 实测外接框 (453,593)-(870,633)，单帧最多 6132（第 14 帧）。
      keeperMessage: 6132,
    }),
    why: '只剩字形（原版字体未交付）；三条人物动画、招牌、商品图与全部按钮逐像素相等',
    issue: 'xl-9bd.17',
  },
  // ===================== 菜单五条（xl-6lo.14 接上 driver=menu） =====================
  //
  // 这五条从 `unassembled` 换成**真量出来的分区表态**。量法与读数都在下面每条
  // 自己的注释里，共同的几件事写在这里，免得抄五遍：
  //
  // - **矩形是从差异图上量出来的，不是照布局常量写的。** 做法：先给每条剧本
  //   的全部帧算一张"差异像素并集"，聚类出各块的外接框，再逐块回到原版 /
  //   Web 两侧的截图上看那块画的是什么，确认成因之后才落成一个区。每一轮都
  //   跑一遍"有没有差异像素落在所有区之外"——落在外面就说明还有一个没认出来
  //   的成因，不许拿一个更大的框把它盖掉。
  // - **矩形比实测外接框各边留几个像素余量**（多数边 4–6）。这条流水线上最会
  //   抖的东西是后备字体链（`expected.ts` 文件头那 2 倍余量的由来），换一台
  //   机器字形会外扩几个像素；紧贴外接框的话，硬比区会红在一圈描边上。
  //   **留不出余量的边一律在那一条自己的注释里写明为什么** —— 有两种：挨着
  //   隔壁区（缺口区不许重叠），或者再往外一像素就会吞进另一个成因。
  // - **`maxPixels` 一律是"单帧最多"的 2 倍**，与本文件其余各条同一条规矩。
  // - 成因只有三族，逐区点名，**不混着写**：
  //   1. **字形**（原版 `文鼎粗钢笔行楷` 未交付，后备链在 `textFont.ts`）——
  //      归 xl-9bd.17；
  //   2. **`sources/Shop/装备/` 的插图还没进烘焙管线** —— 归 xl-234。
  //      ⚠️ **药品图那一半已经还掉了（xl-6lo.15）**：它从来就不需要开新根，
  //      `sources/Shop/药品/回复类/` 那六张 xl-rh9.12 早就烘进主包了，接上
  //      `drawList` 一条 image op 就完了。实测两条剧本里凡是物品页选中一瓶药
  //      的帧（`menu-equip` 的 #21..#23、`menu-scroll` 的 #13/#14，后者还换过
  //      一瓶）。⚠️ **那个区已经不存在了** —— 药品图 xl-6lo.15、装备图
  //      xl-234 先后接上，两半都还清之后整个 `shop-picture-*` 一族被删掉，
  //      从此由「缺口区一个像素都不差就要红」那条双向判据守着；
  //   3. **ADR-0001 例外表里签过字的有意偏离** —— Web 侧加的滚动条、以及列表
  //      裁到框内（原版一路画到框外）。归 xl-6lo.13。
  //
  // ⚠️ **量的过程里揪出过一处真缺陷，已经改掉，不在下面任何一个区里**：装备页
  // 「弃用」那颗按钮**画面比状态晚一帧**（原版 `drawThisPanel()` 先画按钮、
  // 后在 `drawHeroStuff()` 里改它的 `isDraw`）。状态层 45 个格子全绿、逐帧比对
  // 在 `menu-scroll` 第 2 / 5 帧各红 4800 个像素 —— 正是"字体之外的成因"。
  // 修法与判据见 `menu/equipPanel.ts` 的 `abandonDrawn`。
  'menu-equip': {
    status: 'gap',
    // 30 帧（剧本自报 `every: 1`）。物品页 → 装备页 → 奇术页各走一段。
    // 实测：`tools/compare-frames.sh menu-equip`，容差 8。
    gaps: [
      {
        name: 'task-title',
        // 实测外接框 (12,16)-(141,39)，单帧最多 1046。
        maxPixels: 2092,
        rect: { x0: 6, y0: 10, x1: 147, y1: 45 },
        why: '顶栏「当前任务:无」那行字的字形（25 号，原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-level',
        // 实测外接框 (227,179)-(243,201)，单帧最多 167。
        maxPixels: 334,
        rect: { x0: 221, y0: 173, x1: 249, y1: 207 },
        why: '卷轴上的等级数字（30 号红字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-side-text',
        // 实测外接框 (108,252)-(286,372)，单帧最多 3021。真值 `panel` 那一列
        // 现数：7 帧物品页 + 19 帧装备页 + 2 帧奇术页 + 2 帧天书页，所以这一块
        // 两样都有（x<119 那一条窄带只在物品页那几帧上差，实测合计 217 个、
        // 单帧最多 31 个）。
        maxPixels: 6042,
        rect: { x0: 102, y0: 246, x1: 292, y1: 378 },
        why: '卷轴右侧那片字：装备页六行装备名（21 号红字）+ 物品页两行「生命值/魔法值」（20 号蓝字）—— 两页画在同一片像素上，矩形分不开，而两者都是字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-value-bar',
        // 实测外接框 (125,391)-(219,490)，单帧最多 2031。四项属性的标签与数值
        // 都是字；右边那排升降数字是**图**，落在这个矩形外面且逐像素相等。
        maxPixels: 4062,
        rect: { x0: 119, y0: 385, x1: 225, y1: 496 },
        why: '卷轴上四项属性的标签与数值（22 号蓝字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'page-list-text',
        // 实测外接框 (456,160)-(730,200)，单帧最多 1293。
        // ⚠️ **左缘从 452 挪到 444（xl-234 合并时）**：那几列此前一直躲在
        // `shop-picture-current` 底下。药品图（xl-6lo.15）与装备图（xl-234）
        // 先后接上之后那个区整个删掉，**藏在它下面的一笔字形账立刻露出来** ——
        // 硬比区在 x=448..451 上破了（menu-equip 30 帧合计 161、menu-scroll
        // 528）。`DRUG_LIST_X` 就是 448，那是清单第一列的字，本来就该算在
        // 这个区里；把插图区当成它的容器是个巧合，不是判据。
        maxPixels: 2586,
        rect: { x0: 444, y0: 154, x1: 736, y1: 206 },
        why: '页内清单（药品名 / 装备名 + 数量）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'bottom-message-text',
        // 实测外接框 (57,562)-(917,588)，单帧最多 7276。
        maxPixels: 14552,
        rect: { x0: 51, y0: 556, x1: 923, y1: 594 },
        why: '底部那两行说明文字的字形（19 / 24 / 26 号）',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩字形（原版字体未交付）—— 两张商店插图分别由 xl-6lo.15（药品）与 xl-234（装备）接上，两族落点现量 0 个像素',
    issue: 'xl-9bd.17 / xl-234',
  },
  'menu-magic': {
    status: 'gap',
    // 47 帧（剧本自报 `every: 1`，**取到的帧数 = 拍数**）。它是唯一一条把
    // `mouse.code/frame` 与 `magic.animation.code` 逐帧推进记下来的真值 ——
    // 那两样都是**图**，在下面一个缺口区里都没有，也就是说它们逐像素相等。
    // 按缺省 25 个 tick 取一帧的话这条剧本只出 2 帧，动画一帧都采不到。
    gaps: [
      {
        name: 'task-title',
        // 实测外接框 (12,16)-(141,39)，单帧最多 1116。
        maxPixels: 2232,
        rect: { x0: 6, y0: 10, x1: 147, y1: 45 },
        why: '顶栏「当前任务:无」那行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-level',
        // 实测外接框 (227,179)-(243,201)，单帧最多 167。
        maxPixels: 334,
        rect: { x0: 221, y0: 173, x1: 249, y1: 207 },
        why: '卷轴上的等级数字（30 号红字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-side-text',
        // 实测外接框 (108,303)-(260,368)，单帧最多 2679 —— 47 帧里只有**两帧**
        // 有这笔账（合计恰好 2×2679）。真值 `panel` 那一列现数：这条剧本
        // 2 帧在物品页、45 帧在奇术页，**一帧都没进过装备页**，所以这一块画的
        // 是物品页那两行「生命值:700/700」「魔法值:330/330」，不是装备名。
        maxPixels: 5358,
        rect: { x0: 102, y0: 297, x1: 266, y1: 374 },
        why: '物品页卷轴右侧两行「生命值/魔法值」（20 号蓝字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'page-list-text',
        // 实测外接框 (539,179)-(843,207)，单帧最多 2951。奇术页那一行技能说明。
        maxPixels: 5902,
        rect: { x0: 533, y0: 173, x1: 849, y1: 213 },
        why: '奇术页技能说明那行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'bottom-message-text',
        // 实测外接框 (510,564)-(814,588)，单帧最多 2519。
        maxPixels: 5038,
        rect: { x0: 504, y0: 558, x1: 820, y1: 594 },
        why: '底部那几行说明文字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩字形 —— 这条剧本一张 sources/Shop/ 的插图都没画到，技能动画与鼠标游标逐像素相等',
    issue: 'xl-9bd.17',
  },
  'menu-magic-levels': {
    status: 'gap',
    // xl-03x.17。49 帧（`every: 1`），全在奇术页。**技能按钮、人物、技能动画逐像素相等** ——
    // 包括那几颗只有格数涨过才画得出来的按钮（张小凡第 2、陆雪琪第 3、文敏第 4），这正是这条
    // 剧本要看的东西：格数不跟着涨的话那几颗按钮整颗画不出来，落在硬比区里。
    //
    // 缺口区全是字形（xl-9bd.17），坐标是逐帧差异图上量的（比对器同一个 decodePng、单通道
    // 容差 8、扣掉已声明的区后取外接框）：区外的差异**只出现在有动画的帧上**（技能说明只在
    // 那时画），没动画的 0–2 / 34–36 / 42–43 帧区外是 0。
    //
    // 技能说明跟着被点的那颗按钮往下挪、而且折成两行，所以一颗一个区，**不合成一个大框**：
    // 合起来会盖住按钮列 x 538..552 那一条。三个框各外扩 2（不是别处的 6）—— 外扩 6 的话
    // 第 2、3 颗的框在 y 301..308 叠上。menu-magic 的 page-list-text（第 1 颗，y 173..213）
    // 与 bottom-message-text 这条剧本一帧都没差，不声明。
    gaps: [
      {
        name: 'task-title',
        // 实测单帧最多 1116（与 menu-magic 同一行字）。
        maxPixels: 2232,
        rect: { x0: 6, y0: 10, x1: 147, y1: 45 },
        why: '顶栏「当前任务:无」那行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-level',
        // 实测单帧最多 132（等级 2 / 5 / 10 三种数字都在这一框里）。
        maxPixels: 334,
        rect: { x0: 221, y0: 173, x1: 249, y1: 207 },
        why: '卷轴上的等级数字（30 号红字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'skill2-intro-text',
        // 实测外接框 (539,243)-(860,302)，第 3–33 帧，单帧最多 5980。
        maxPixels: 11960,
        rect: { x0: 537, y0: 241, x1: 862, y1: 304 },
        why: '奇术页第 2 颗技能的说明（两行）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'skill3-intro-text',
        // 实测外接框 (538,307)-(952,367)，第 37–41 帧，单帧最多 9379。
        maxPixels: 18758,
        rect: { x0: 536, y0: 305, x1: 954, y1: 369 },
        why: '奇术页第 3 颗技能的说明（两行）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'skill4-intro-text',
        // 实测外接框 (538,371)-(1007,433)，第 44–48 帧，单帧最多 10707。
        maxPixels: 21414,
        rect: { x0: 536, y0: 370, x1: 1009, y1: 435 },
        why: '奇术页第 4 颗技能的说明（两行）的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩字形 —— 技能按钮（含格数涨出来的那几颗）、人物与技能动画逐像素相等',
    issue: 'xl-9bd.17',
  },
  'menu-scroll': {
    status: 'gap',
    // 15 帧（剧本自报 `every: 1`）。装备页武器分类塞满 20 件，撑过装备4.png
    // 那个列表框 4 行 —— **ADR-0001 那两条有意偏离就落在这一条上**，各自一个区。
    gaps: [
      {
        name: 'task-title',
        // 实测外接框 (12,16)-(141,39)，单帧最多 1037。
        maxPixels: 2074,
        rect: { x0: 6, y0: 10, x1: 147, y1: 45 },
        why: '顶栏「当前任务:无」那行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-level',
        // 实测外接框 (227,179)-(243,201)，单帧最多 167。
        maxPixels: 334,
        rect: { x0: 221, y0: 173, x1: 249, y1: 207 },
        why: '卷轴上的等级数字（30 号红字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-side-text',
        // 实测外接框 (108,252)-(260,372)，单帧最多 2679。真值 `panel` 那一列
        // 现数：11 帧装备页 + 4 帧物品页，所以这一块两样都有（x<119 那一条窄带
        // 只在物品页那几帧上差，实测合计 124 个、单帧最多 31 个）。
        maxPixels: 5358,
        rect: { x0: 104, y0: 248, x1: 264, y1: 376 },
        why: '卷轴右侧那片字：装备页六行装备名（21 号红字）+ 物品页两行「生命值/魔法值」（20 号蓝字）—— 两页画在同一片像素上，矩形分不开，而两者都是字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-value-bar',
        // 实测外接框 (125,391)-(219,490)，单帧最多 1796。
        maxPixels: 3592,
        rect: { x0: 119, y0: 385, x1: 225, y1: 496 },
        why: '卷轴上四项属性的标签与数值（22 号蓝字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scrollbar',
        // 实测外接框 (770,155)-(777,506)，单帧最多 2780。**原版这里什么都没有**
        // —— 一条 8 像素宽的竖条整根是 Web 侧加的（`drawList.ts` 那唯一一种
        // `rect` 绘制）。所以这个区的账不是"画得不像"，是"多画了一样东西"。
        maxPixels: 5560,
        rect: { x0: 764, y0: 149, x1: 783, y1: 512 },
        why: 'Web 侧给列表加的滚动条，原版没有 —— ADR-0001 签过字的有意偏离',
        issue: 'xl-6lo.13',
      },
      {
        name: 'page-list-text',
        // 实测外接框 (456,160)-(730,509)，单帧最多 10897 —— 五条剧本里数它最大，
        // 因为框里 16 行字每一行都在差。
        // ⚠️ **下缘 509 一点余量都没有**：再往下就是 `clipped-rows-17-18`
        // （从 510 起），而缺口区不许重叠。两者中间那两条扫描线（510 / 511）
        // 实测本来就是空的 —— 第 16 行画完、第 17 行还没起。
        // ⚠️ **左缘从 452 挪到 444（xl-234 合并时）**：那几列此前一直躲在
        // `shop-picture-current` 底下。药品图（xl-6lo.15）与装备图（xl-234）
        // 先后接上之后那个区整个删掉，**藏在它下面的一笔字形账立刻露出来** ——
        // 硬比区在 x=448..451 上破了（menu-equip 30 帧合计 161、menu-scroll
        // 528）。`DRUG_LIST_X` 就是 448，那是清单第一列的字，本来就该算在
        // 这个区里；把插图区当成它的容器是个巧合，不是判据。
        maxPixels: 21794,
        rect: { x0: 444, y0: 156, x1: 734, y1: 509 },
        why: '装备清单框内那 16 行（装备名 + 数量）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'clipped-rows-17-18',
        // 实测外接框 (548,512)-(730,553)，单帧最多 1990。**这一块原版有字、
        // Web 侧一个像素都没画**：20 件武器撑过列表框，原版一路画到框外，
        // Web 侧裁到框内。第 17 / 18 行（基线 y=529 / 551）落在这里，与底部
        // 那两行说明的扫描线不重叠，所以单独成区、成因只有一个。
        //
        // ⚠️ 第 19 / 20 行（基线 y=573 / 595）**分不出来**：它们与底部说明
        // 那两行占同一片扫描线，一并记在下面 `bottom-message-text` 里。
        maxPixels: 3980,
        rect: { x0: 542, y0: 510, x1: 736, y1: 555 },
        why: '列表裁到框内之后第 17 / 18 行整行没画（原版照画到框外）—— ADR-0001 签过字的有意偏离',
        issue: 'xl-6lo.13',
      },
      {
        name: 'bottom-message-text',
        // 实测外接框 (59,556)-(943,596)，单帧最多 8876。
        // ⚠️ **上缘 556 一点余量都没有**：再往上就是 `clipped-rows-17-18`
        // （到 555 止），而缺口区不许重叠。
        // ⚠️ **这个区里有两个成因，几何上分不开**：底部两行说明的字形，
        // 加上被裁掉的第 19 / 20 行（基线 573 / 595）—— 后者与前者共用扫描线
        // y≈556..596。上面 `clipped-rows-17-18` 已经把分得开的那一半单独立了区。
        maxPixels: 17752,
        rect: { x0: 55, y0: 556, x1: 947, y1: 600 },
        why: '底部两行说明的字形 + 被裁掉的第 19 / 20 行（两者共用这片扫描线，矩形分不开）',
        issue: 'xl-9bd.17 / xl-6lo.13',
      },
    ],
    why: '字形 + ADR-0001 签过字的两条偏离（滚动条、列表裁剪）—— 商店插图那一族由 xl-6lo.15 与 xl-234 接完，现量 0 个像素',
    issue: 'xl-9bd.17 / xl-234 / xl-6lo.13',
  },
  'menu-hero': {
    status: 'gap',
    // 38 帧（剧本自报 `every: 1`）。三个人之间换人看属性：hero 取到 1 / 2 / 4，
    // 装备页四个槽位都穿过东西。
    gaps: [
      {
        name: 'task-title',
        // 实测外接框 (12,16)-(141,39)，单帧最多 1037。
        maxPixels: 2074,
        rect: { x0: 6, y0: 10, x1: 147, y1: 45 },
        why: '顶栏「当前任务:无」那行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-level',
        // 实测外接框 (227,179)-(243,201)，单帧最多 167。
        maxPixels: 334,
        rect: { x0: 221, y0: 173, x1: 249, y1: 207 },
        why: '卷轴上的等级数字（30 号红字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-side-text',
        // 实测外接框 (125,252)-(258,372)，单帧最多 3764 —— 五条里数它最大，
        // 因为这一条把头盔 / 战靴 / 盔甲都穿上了，六行里有四行有字。
        // 真值 `panel` 那一列现数：38 帧**全在装备页**，所以这一块只有装备名。
        maxPixels: 7528,
        rect: { x0: 119, y0: 246, x1: 264, y1: 378 },
        why: '卷轴上六行装备名的字形（21 号红字）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-value-bar',
        // 实测外接框 (125,391)-(219,490)，单帧最多 1848。
        maxPixels: 3696,
        rect: { x0: 119, y0: 385, x1: 225, y1: 496 },
        why: '卷轴上四项属性的标签与数值（22 号蓝字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'page-list-text',
        // 实测外接框 (548,160)-(730,201)，单帧最多 1361。
        maxPixels: 2722,
        rect: { x0: 542, y0: 154, x1: 736, y1: 207 },
        why: '装备清单（装备名 + 数量）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'bottom-message-text',
        // 实测外接框 (57,561)-(917,580)，单帧最多 5996。
        maxPixels: 11992,
        rect: { x0: 51, y0: 555, x1: 923, y1: 586 },
        why: '底部那两行说明文字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩字形（原版字体未交付）—— 商店插图那一族已由 xl-6lo.15 与 xl-234 接完',
    issue: 'xl-9bd.17 / xl-234',
  },
  'menu-task': {
    status: 'gap',
    // 8 帧（剧本自报 `every: 1`）。唯一一条顶栏走 if 那一支的菜单真值（xl-03x.10）：
    // `setup.scene` = 脚本1，两端顶栏都是「当前任务:快去大活看演出吧」。四页各翻一次。
    // 实测：`tools/compare-frames.sh menu-task`，容差 8；矩形用 `measureGaps.ts --rects`
    // 回核过，硬比区 0 个超容差像素。
    //
    // ⚠️ 这一族全是字形缺口区，**只核像素个数上界** —— 任务文本错一个字，差异像素多半
    // 还在上界里。「错一个字要红」的判据在 `menu/render/taskTitle.test.ts`（逐步字符串对
    // 真值 `task` 列），这里守的是位置、字号与「顶栏之外逐像素相等」。
    gaps: [
      {
        name: 'task-title',
        // 实测外接框 (12,15)-(319,39)，单帧最多 3707（三轮 3707 / 3707 / 3707）——
        // 比别的菜单剧本的 (12,16)-(141,39) 宽得多：它们画的是「当前任务:无」。
        maxPixels: 7414,
        rect: { x0: 6, y0: 9, x1: 325, y1: 45 },
        why: '顶栏「当前任务:快去大活看演出吧」那行字的字形（25 号，原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-level',
        // 实测外接框 (227,179)-(243,201)，单帧最多 167。
        maxPixels: 334,
        rect: { x0: 221, y0: 173, x1: 249, y1: 207 },
        why: '卷轴上的等级数字（30 号红字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-side-text',
        // 实测外接框 (108,252)-(260,372)，单帧最多 2679。真值 `panel` 那一列现数：
        // 2 帧装备页 + 2 帧物品页有卷轴（奇术页那两帧卷轴上没有这片字、天书页没有卷轴）。
        maxPixels: 5358,
        rect: { x0: 102, y0: 246, x1: 266, y1: 378 },
        why: '卷轴右侧那片字：装备页装备名 + 物品页「生命值/魔法值」—— 两页画在同一片像素上，都是字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-value-bar',
        // 实测外接框 (125,391)-(219,490)，单帧最多 1796。
        maxPixels: 3592,
        rect: { x0: 119, y0: 385, x1: 225, y1: 496 },
        why: '卷轴上四项属性的标签与数值（22 号蓝字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'bottom-message-text',
        // 实测外接框 (59,562)-(814,588)，单帧最多 5555。
        maxPixels: 11110,
        rect: { x0: 53, y0: 556, x1: 820, y1: 594 },
        why: '底部那几行说明文字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩字形（原版字体未交付）',
    issue: 'xl-9bd.17',
  },
  'menu-func': {
    status: 'gap',
    // 22 帧（剧本自报 `every: 1`）。天书页设定子菜单的展开收起与 BGM 开关 ——
    // 那五组按钮全是**图**，一个缺口区都没有，也就是说它们逐像素相等。
    gaps: [
      {
        name: 'task-title',
        // 实测外接框 (12,16)-(141,39)，单帧最多 1046。
        maxPixels: 2092,
        rect: { x0: 6, y0: 10, x1: 147, y1: 45 },
        why: '顶栏「当前任务:无」那行字的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-level',
        // 实测外接框 (227,179)-(243,201)，单帧最多 167。
        maxPixels: 334,
        rect: { x0: 221, y0: 173, x1: 249, y1: 207 },
        why: '卷轴上的等级数字（30 号红字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-side-text',
        // 实测外接框 (126,252)-(250,372)，单帧最多 2343 —— 合计恰好 2×2343，
        // 因为真值 `panel` 那一列现数是 20 帧天书页 + **2 帧装备页**，而天书页
        // 没有卷轴（`FuncPanel` 的构造函数不建 `Scoll`）。这一块只有装备名。
        maxPixels: 4686,
        rect: { x0: 120, y0: 246, x1: 256, y1: 378 },
        why: '卷轴上六行装备名的字形（21 号红字）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'scoll-value-bar',
        // 实测外接框 (125,391)-(219,490)，单帧最多 1796。
        maxPixels: 3592,
        rect: { x0: 119, y0: 385, x1: 225, y1: 496 },
        why: '卷轴上四项属性的标签与数值（22 号蓝字）的字形',
        issue: 'xl-9bd.17',
      },
      {
        name: 'bottom-message-text',
        // 实测外接框 (59,562)-(793,580)，单帧最多 5555。
        maxPixels: 11110,
        rect: { x0: 53, y0: 556, x1: 799, y1: 586 },
        why: '底部那两行说明文字的字形',
        issue: 'xl-9bd.17',
      },
    ],
    why: '只剩字形（原版字体未交付）；装备插图已由 xl-234 接上，天书页那五组按钮逐像素相等',
    issue: 'xl-9bd.17 / xl-234',
  },

  // ===== xl-yg6.7 补进来的五条场景剧本 =====
  //
  // 这五条走的是原版有分支而此前一份真值都没走到的路：答对 / 答错 / 开宝箱 /
  // 三扇门各选是与否。web 侧**这几层一笔都还没画**（选择框、问题框、宝箱与
  // "得到物品"提示框），所以每一条都是整屏表态的 `gap`，缺口区留给 xl-yg6.12
  // 去量 —— 那张票的正题就是把场景各条剧本的表态换成实测缺口分区。
  //
  // 每个 `maxRatio` 都是 2026-09-09 跑一遍 `tools/compare-frames.sh` 读出来的
  // **最差帧占比 × 2**（余量为什么是 2 倍见文件头）。上界都在 99.96%（全黑图）
  // 之下，所以"这一帧没画出来"仍然会红。

  // ⚠️ **下面四条 xl-yg6.12 换成了分区表态**（2026-09-10，矩形由
  // `scripts/measureGaps.ts` 从差异图上量，容差 8，四条**区外都是 0 个**）。
  //
  // 顺带纠正它们原来 `why` 里的一句：「金陵大学医院.png / 金陵大学装备超市.png /
  // 迷宫1.png / 大活.png 的重编码差异」**不存在**。四张都是 PNG，烘焙器走
  // `-lossless`；区外 0 个就是它的现成反证 —— 真有重编码的账，它会满屏撒在
  // 硬比区里。那一句是把整屏包围盒 (…)-(993,630) 读成了「底图也在差」，其实
  // 包围盒被右下角那块金币 HUD 撑到了右下角。
  'maze-treasure': {
    status: 'gap',
    // 当时的整屏表态：最差帧 #950 0.1527% @ (529,297)-(993,630)，上界 0.0031。
    // xl-yg6.10 把宝箱与提示框画上之后从 3.3424%（#850）掉到那里。
    //
    // 44 帧（`--every 25`）。提示框吐完字之后往右退场，取样的几帧里那行字落在
    // x=529 / 721 / 881 三处（#925..#1000），所以这个区是一条横带。
    gaps: [
      {
        name: 'reminder-text',
        // 实测外接框 (529,297)-(925,319)，单帧最多 449（#950），4/44 帧。
        // 差的只有「得到金疮药 * 1」里 ` * 1` 那一截：全角字按字号整格摆是对上的，
        // **半角的空格与 `*` 原版给的步进比浏览器宽**，`1` 于是落点偏右 ——
        // 与 dorm-intro 旁白末行那六个半角句点同一件事，归字形那一族。
        maxPixels: 898,
        rect: { x0: 523, y0: 291, x1: 931, y1: 325 },
        why: '提示框那行字里半角字符的步进（原版字体未交付，两端的半角宽度不同）',
        issue: 'xl-9bd.17',
      },
      // 实测外接框 (940,617)-(993,630)，单帧最多 563（#0），44/44 帧。
      coinDigits(563),
    ],
    why: '提示框里半角字符的步进与右下角金币数（字形）；宝箱、提示框本身与迷宫底图逐像素相等',
    issue: 'xl-9bd.17',
  },
  'shop-door': {
    status: 'gap',
    // 当时的整屏表态：最差帧 #475 0.7574% @ (292,268)-(993,630)，上界 0.0152；
    // xl-yg6.8 之前是 11.3094%（选择框没画）。
    //
    // 20 帧（`--every 25`）。选择框的框与底图逐像素相等，差的只有框里那几行字。
    // 四行字（y≈268 / 298 / 331 / 358 起）合成**一个**区：行间只隔 5~9 行，逐行
    // 各留余量会互相重叠；四行是同一族账（字形），合并不会混成因。
    gaps: [
      {
        name: 'select-text',
        // 实测外接框 (292,268)-(711,377)，单帧最多 4394（#475），11/20 帧。
        maxPixels: 8788,
        rect: { x0: 286, y0: 262, x1: 717, y1: 383 },
        why: '选择框里那几行字的字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      // 实测外接框 (940,617)-(993,630)，单帧最多 586（#0），20/20 帧。
      coinDigits(586),
    ],
    why: '选择框里与右下角金币数的字形；选择框的框与医院底图逐像素相等',
    issue: 'xl-9bd.17',
  },
  'equipshop-door': {
    status: 'gap',
    // 当时的整屏表态：最差帧 #575 0.8334% @ (292,268)-(993,630)，上界 0.0167；
    // xl-yg6.8 之前是 11.4548%。
    //
    // 25 帧（`--every 25`）。与 `shop-door` 画的是同一个选择框，矩形也是同一个 ——
    // 但它是**这条剧本自己量出来的**，不是抄的：外接框右沿 710（那边是 711），
    // 字不一样。
    gaps: [
      {
        name: 'select-text',
        // 实测外接框 (292,268)-(710,377)，单帧最多 4892（#575），12/25 帧。
        maxPixels: 9784,
        rect: { x0: 286, y0: 262, x1: 717, y1: 383 },
        why: '选择框里那几行字的字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      // 实测外接框 (940,617)-(993,630)，单帧最多 586（#0），25/25 帧。
      coinDigits(586),
    ],
    why: '选择框里与右下角金币数的字形；选择框的框与装备超市底图逐像素相等',
    issue: 'xl-9bd.17',
  },
  'question-answer': {
    status: 'gap',
    // 当时的整屏表态：最差帧 #1925 1.8489% @ (292,83)-(982,630)，上界 0.037。
    // xl-yg6.9 把问题框画上之前 38.0360%，xl-yg6.10 把提示框画上之前 3.2230%。
    //
    // 91 帧（`--every 25`）。问题框里题面与选项一行一行排（行距 30，行间只隔 4~10
    // 行），所以框里的字合成一个区；答对 / 答错那个红字提示框从右边滑进来，
    // 滑过 x=739 以右的那一截单独成区。
    gaps: [
      {
        name: 'question-text',
        // 实测外接框 (292,83)-(738,378)，单帧最多 11696（#1925），45/91 帧。
        // 右沿 738 = 框里题面最靠右的字（732）+ 6。提示框的红字滑过这一段时也
        // 落在这个区里 —— 同一族账（字形），合并不混成因。
        maxPixels: 23392,
        rect: { x0: 286, y0: 77, x1: 738, y1: 384 },
        why: '问题框里题面与选项的字形 + 提示框红字滑过框内那一段（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'reminder-text',
        // 实测外接框 (739,295)-(1023,323)，5/91 帧。**这一格的读数会漂**：三轮单帧最多
        // 依次是 1985（measureGaps）/ 2015 / 2231（两轮 compare-frames），红字随取样帧落在
        // 滑动的哪一拍而变。上界按三轮里最大的 2231 取 2 倍（/code-review 抓的：原先按
        // 1985 定的 3970，对 2231 只剩 1.78 倍余量）。
        maxPixels: 4462,
        rect: { x0: 739, y0: 289, x1: 1023, y1: 329 },
        why: '答对 / 答错提示框滑入时框外那一段红字的字形',
        issue: 'xl-9bd.17',
      },
      // 金币数只剩字形那一族（xl-03x.3 之前还混着「取图页不跑钱包、数值不同」那一族，
      // 那条有理由的例外已删）。**数值由账本对撞守着**（`compare/ledger.ts`），不靠这一格的
      // 上界：答题之前两端都是 10000 时字形差就有 559 个像素，数值错了也不比它多。
      coinDigits(559),
    ],
    why: '问题框、提示框与金币数的字形；问题框、提示框的底图与大活底图逐像素相等',
    issue: 'xl-9bd.17',
  },
  'question-memory': {
    status: 'gap',
    // 实测最差帧 #1625 **23.7225%** @ (0,0)-(1023,639)（98 帧全偏）；上界 2 倍。
    // xl-yg6.9 把问题框画上之后从 38.0360%（#900，与 `question-answer` 同一帧）
    // 掉到这里。最差帧换了位置：t=1616..1685 这条剧本走出了大活、站在大地图上，
    // 底账是 大地图.jpg → 有损 WebP（与 `bigmap-walk` 同一个量级），不是答题。
    // xl-yg6.10 把提示框画上之后重量：最差帧仍是 #1625、23.7175%（只动了小数
    // 点后第三位）—— 提示框那点面积在卷动地图的重编码底账面前排不上号，上界不动。
    //
    // ⚠️ **xl-yg6.12 换成了分区表态**（2026-09-10，98 帧，`--every 25`，容差 8）。大地图段的
    // 重编码归零（near_lossless 40），大活门匾那张遮掩图（(492,428)-(562,473)）随 addMap
    // 那一层画上归零。剩下的与 `question-answer` 同一组矩形，读数是这条剧本自己量的，
    // 区外 0 个：
    gaps: [
      {
        name: 'question-text',
        // 实测外接框 (292,83)-(729,378)，单帧最多 6553（#800），25/98 帧。
        maxPixels: 13106,
        rect: { x0: 286, y0: 77, x1: 738, y1: 384 },
        why: '问题框里题面与选项的字形 + 提示框红字滑过框内那一段（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'reminder-text',
        // 实测外接框 (743,295)-(1023,323)，单帧最多 2207（#1275），2/98 帧。
        maxPixels: 4414,
        rect: { x0: 739, y0: 289, x1: 1023, y1: 329 },
        why: '答对 / 答错提示框滑入时框外那一段红字的字形',
        issue: 'xl-9bd.17',
      },
      // 金币数只剩字形那一族，理由同 `question-answer` 那一格（xl-03x.3）。
      coinDigits(559),
    ],
    why: '问题框、提示框与金币数的字形；大活、大地图、遮掩图与人物逐像素相等',
    issue: 'xl-9bd.17',
  },
  'battle-door': {
    status: 'gap',
    // 实测最差帧 #550 **38.5582%**（93 帧全偏）；上界 2 倍。与同一张地图上的
    // `bigmap-walk`（37.1101%）同一个量级 —— 底账就是 大地图.jpg → 有损 WebP。
    //
    // ⚠️ **这个数原先是 96.3615%**，而那不是"缺口更大"，是**剧本自己的问题**：
    // 头一版挪光标用的是下键，主角脚下那一格走得进去，于是 web 侧（还没有
    // "选择框开着时方向键不给主角"这道闸）把主角挪了一格、视口跟着滚了 32px，
    // 卷动地图上几乎每个像素都不一样了。96% × 2 = 192% 的上界连全黑图都拦不住
    // ——那条表态会变成装饰。改成上键之后主角正上方被 NPC 占着，一步都挪不动，
    // 这一条于是现在就量得准。**状态层那笔账没有被藏起来**：它登记在
    // `state/traceReplay.test.ts` 的 PENDING 里（role / dialogue × battle-door）。
    // （后记：那几格 xl-yg6.8 翻过来，最后一格 audio × battle-door 由 xl-yg6.11 翻过来，PENDING 已空。）
    //
    // ⚠️ **xl-yg6.8 把选择框画上了，这个数同样一个字没变**（重量了一遍，
    // 仍是 38.5582% @ 整屏）：底账是整张卷动地图的有损重编码，选择框那点
    // 面积压根排不上号。
    //
    // ⚠️ **xl-yg6.12 换成了分区表态**（2026-09-10，93 帧，`--every 25`，容差 8）。重编码那
    // 38% 归零之后，底下是 addMap 那一层：体育馆门匾的遮掩图随镜头从屏幕右下游到左上
    // （每帧 6495 个像素，并集铺满整屏，切不成静态矩形），外加顶边一条 (249,0)-(363,1)。
    // 那一层画上之后两样都归零，剩下两块，区外 0 个：
    gaps: [
      {
        name: 'select-text',
        // 实测外接框 (292,268)-(711,378)，单帧最多 6635（#1850），20/93 帧。与两扇商店门
        // 同一个选择框、同一个矩形，读数是这条剧本自己量的。
        maxPixels: 13270,
        rect: { x0: 286, y0: 262, x1: 717, y1: 383 },
        why: '选择框里对话与选项的字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      // 实测外接框 (940,617)-(993,630)，单帧最多 584（#525），92/93 帧。
      coinDigits(584),
    ],
    why: '选择框与右下角金币数的字形；卷动的大地图、遮掩图、主角与 NPC 逐像素相等',
    issue: 'xl-9bd.17',
  },
  // ===================== 存读档面板两条（xl-i06.12 接上 driver=saveload） =====================
  //
  // 从 `unassembled` 换成**真量出来的分区表态**（2026-09-10，`every: 1`，容差 8）。量法同
  // 商店那一批：`web/scripts/measureGaps.ts` 先聚类出差异块的外接框，再拿划好的矩形回核，
  // 区外 0 个。**成因两族，逐区点名，不混写**：
  //
  // 1. **缩略图**（`thumb-*`）—— 原版 `drawImage(img, 100, 100+i*200, 150, 100, this)` 交给
  //    平台自己缩放；本仓库拟合过的两条缩放循环按源图透明通道分岔，而这里的缩小倍率落在
  //    既有拟合区间之外。**不在本票拟合**，整块挂 xl-cpo。矩形就是绘制矩形，四边不留余量
  //    （再往外一像素就是底板，那是硬比区的东西）。实测外接框：槽 0 与它逐边重合；槽 1、槽 2
  //    （宿舍.png）左沿是 101 —— 那一列两端恰好相同，矩形仍按绘制矩形取到 100。
  //    ⚠️ **大迷宫.png 那一格两端逐像素相等**（saveload-start 槽 2），所以**不声明**：一帧
  //    都不差的区不许留着。反证做过 —— 原版位图那一块 4655 种颜色、空槽位置 144 种，
  //    真的画了而且两端画的是同一批像素。读数记在 xl-cpo 上。
  // 2. **字形**（`map-name-*` / `task-*`）—— 地图名与任务提示（`文鼎粗钢笔行楷` 粗体 20 号，
  //    原版字体未交付），归 xl-9bd.17。
  //
  // 另一件量出来的：**别的一个像素都不差** —— 背景（存 / 读两张）、三块底板、队伍三个人的
  // 动画、三颗按钮与光效、鼠标图。动画都停在第 0 格（`replay/main.ts` 的 `LS_FROZEN_FRAMES`），
  // 这条流水线守不住它们怎么循环。
  //
  // 空槽（「无」）画地图名、**不画**缩略图与任务（`drawList.ts`）：所以空槽只有 `map-name-*`。
  'saveload-menu': {
    status: 'gap',
    // 9 帧。槽 0 前两帧是入库样例（大地图.jpg），第 2 步起被这一局（宿舍）覆盖；槽 2 头两帧
    // 是空的，第 1 步存进去之后才有缩略图与任务 —— 所以 `thumb-2` / `task-2` 出现在 7/9 帧。
    gaps: [
      // 实测外接框 (100,100)-(249,199)，单帧最多 567（f000000，大地图.jpg）。
      lsThumb(0, 567),
      // 实测外接框 (101,300)-(249,399)，单帧最多 480（宿舍.png）。
      lsThumb(1, 480),
      // 实测外接框 (101,500)-(249,599)，单帧最多 480（f000002 起，宿舍.png），7/9 帧。
      lsThumb(2, 480),
      // 实测外接框 (100,203)-(159,222)，单帧最多 490（「大地图」三个字，f000000）。
      lsMapName(0, 490),
      // 实测外接框 (101,403)-(139,422)，单帧最多 290（「宿舍」）。
      lsMapName(1, 290),
      // 实测外接框 (100,603)-(139,622)，单帧最多 290（空槽「无」→ 存进去之后「宿舍」）。
      lsMapName(2, 290),
      // 实测外接框 (400,103)-(641,122)，单帧最多 2560（f000000）。
      lsTask(0, 2560),
      // 实测外接框 (400,303)-(561,322)，单帧最多 1689。
      lsTask(1, 1689),
      // 实测外接框 (400,503)-(561,522)，单帧最多 1689，7/9 帧。
      lsTask(2, 1689),
    ],
    why: '缩略图（平台缩放，挂 xl-cpo）+ 地图名与任务提示的字形；背景、底板、人物动画、按钮与鼠标逐像素相等',
    issue: 'xl-cpo / xl-9bd.17',
  },
  'saveload-start': {
    status: 'gap',
    // 5 帧。槽 1 是空的（剧本 `emptySlots: [1]`），槽 2 是 大迷宫.png —— 那一格缩略图两端
    // 逐像素相等，不声明（见上面那段）。
    gaps: [
      // 实测外接框 (100,100)-(249,199)，单帧最多 567（大地图.jpg）。
      lsThumb(0, 567),
      // 实测外接框 (100,203)-(159,222)，单帧最多 490。
      lsMapName(0, 490),
      // 实测外接框 (100,404)-(119,421)，单帧最多 122（空槽的「无」）。
      lsMapName(1, 122),
      // 实测外接框 (100,603)-(159,622)，单帧最多 458（「大迷宫」）。
      lsMapName(2, 458),
      // 实测外接框 (400,103)-(641,122)，单帧最多 2560。
      lsTask(0, 2560),
      // 实测外接框 (401,503)-(663,523)，单帧最多 2893 —— 两边任务里最长的一句，`lsTask` 的
      // 右沿就是按它留的余量。
      lsTask(2, 2893),
    ],
    why: '缩略图（平台缩放，挂 xl-cpo）+ 地图名与任务提示的字形；大迷宫那一格缩略图、背景、底板、人物动画、按钮与鼠标逐像素相等',
    issue: 'xl-cpo / xl-9bd.17',
  },
  // ===================== 读档剧本三条（xl-i06.12 让取图页认读档起手） =====================
  //
  // xl-i06.10 那时它们是 `unpainted`：取图页读不了原版存档，当场抛。现在比对器在 Node 那一半
  // 用状态层判据的同一个读取器把那一份档解好送进来（`compare/saveFixtures.ts`），取图页走
  // 产品读档那一路的 `applyReadBack`（`replay/main.ts` 的 `worldFromSave`）。
  //
  // **三条都只剩右下角金币数的字形**，区外 0 个（2026-09-10，`--every 25` 剧本自报的缺省，
  // 容差 8）。金币数的**数值**两端相同：`applyReadBack` 把存档里的钱写进钱包，而那一列在
  // 状态层对齐着（`state/traceReplay.test.ts` 的 coins × load-slot*）—— 所以这一格不像答题
  // 那两条混着「数值不同」那一族，只有字形。
  //
  // ⚠️ load-slot2 读进 脚本20（大迷宫.png 2865×699，碰撞网格 2848×672），渲染器原先对
  // 「图与网格不等」一律拒绝渲染。追下去是 96 个场景里 22 个对不上：图比网格**大**的这一种
  // 原版画法与相等时相同，放行（`scene/mapSize.ts`）；图比网格**小**的 20 个另立 xl-i06.14。
  // 这一条硬比区逐像素相等，就是「大的那一种放行得对」的证据。
  'load-slot0': {
    status: 'gap',
    // 8 帧。中途读档（先跑一局 脚本1），读进 脚本38。
    // 实测外接框 (941,617)-(993,630)，单帧最多 430，8/8 帧。
    gaps: [coinDigits(430)],
    why: '只剩右下角金币数的字形；读档之后的场景、主角、NPC 与对话框逐像素相等',
    issue: 'xl-9bd.17',
  },
  'load-slot1': {
    status: 'gap',
    // 11 帧。开机读档，读进 脚本1，**旁白一拍都不起**（xl-i06.10 的「跳过旁白」）。
    // 实测外接框 (940,617)-(993,630)，单帧最多 520，11/11 帧。
    gaps: [coinDigits(520)],
    why: '只剩右下角金币数的字形；读档之后的宿舍、主角走动与 NPC 逐像素相等（旁白没起，两端都没画）',
    issue: 'xl-9bd.17',
  },
  'load-slot2': {
    status: 'gap',
    // 9 帧。开机读档，读进无 Dialogue 段的 脚本20（大迷宫）。
    // 实测外接框 (940,617)-(993,630)，单帧最多 500（f000075），9/9 帧。
    gaps: [coinDigits(500)],
    why: '只剩右下角金币数的字形；读档之后的大迷宫（图比碰撞网格大的那一种，照画）与主角逐像素相等',
    issue: 'xl-9bd.17',
  },
  // 结局面板（driver=end，xl-czb.5 的真值，xl-czb.6 接上面板与取图页）。
  //
  // 2026-09-10 实测（剧本自报 every 8，容差 8）：49 帧、偏离 0/49、最差 0.0000% ——
  // 而且**逐像素全等**（32112640/32112640 个像素单通道差 0，连 25 张 JPG 过场画都是：
  // 烘成无损 WebP 之后，浏览器解出来的与原版 Toolkit 解出来的逐字节相同）。
  // 「零差异」与「两端都没画」长得一样，所以回原版位图做了反证：#0 整帧全黑（进来那一步
  // 字幕在 y=640、侧栏在 y=−1920，都在屏外，过场画还没读），其余 48 帧每帧 96–97% 的像素
  // 非黑、与 #0 相差 63 万像素以上 —— 两端画的是同一批东西，不是都空着。
  //
  // ⚠️ every 8 取到的是 #0..#384，**定格之后那 7 步（#385..#391：再推 3 拍、回车、叫醒两次、
  // 退出键）一帧都没取**。那一段的画面由状态层守：`end/endTrace.test.ts` 的「画面冻在最后
  // 一帧」逐步断言绘制清单与定格那一步相同。一次性的 `--every 1` 读数见 xl-czb.6 的提交说明。
  'end-credits': {
    status: 'match',
  },
  // ===================== 标题页（xl-whk，第七支驱动器 start） =====================
  //
  // 取图页画的是**产品那一份 DOM**（`StartPanelView`），不是另写的渲染器。
  //
  // 头一轮（2026-09-12）两条都**第 0 帧起**偏离：四颗按钮的左沿。那是产品的真错 ——
  // 原版 `drawButton` 先画按钮图、后画高亮，web 反了。改过之后 `start-about` 的 #0–#20
  // （卷轴开播之前）**逐像素相等**，剩下的只有一笔账：
  //
  // **卷轴的有损 WebP**（`start/assets.ts` 的 `START_SEQUENCES`，xl-bbs 裁定 q80）。纸纹的噪点
  // 被抹平，差异像素散在整张纸上。展开到头的那几帧纸面伸到 x=1085（`卷轴/10.png` 1486 宽，
  // 画在 −320），`反向卷轴` 整段别名到它 —— 所以这笔账的范围就是**整屏**，切不出分区：
  // 实测把缺口区划到 (0,0)-(989,639)，区外 x≥990 那一条仍有 1190 / 3689 个超容差像素。
  //
  // ⚠️ 整屏上界**看不见小面积的回归**：按钮次序那一处只差四颗按钮左沿几百个像素，远在上界
  // 之下。它由 `StartPanel.test.tsx` 从 GBK 的 `drawButton` 现读次序钉着，不靠这里。
  //
  // 上界按仓库规矩取最差帧的 2 倍。
  'start-about': {
    status: 'gap',
    // 50 帧（剧本自报 every 1），偏离 26/50（#21 起，卷轴开播）。最差 #27 4.3253% → 8.6506%。
    maxRatio: 0.086506,
    why: '卷轴的有损 WebP（q80）：展开之后纸面铺满整屏，切不出分区；卷轴开播之前逐像素相等',
    issue: 'xl-bbs',
  },
  'start-newgame': {
    status: 'gap',
    // 47 帧（every 1），偏离 39/47（#8 起；展开之后「反向卷轴」第 0 帧一直画着，一直有差）。
    // 最差 #14 4.8404% → 9.6808%。
    maxRatio: 0.096808,
    why: '卷轴与反向卷轴（别名）的有损 WebP（q80）：展开之后纸面铺满整屏，切不出分区',
    issue: 'xl-bbs',
  },
  // ===================== 图比碰撞网格小的三种缺法（xl-czb.3，还 xl-i06.14） =====================
  //
  // 原版对这批场景的画法与相等时逐像素相同：源矩形 (0,0)-(1016,632) 取不到缺的那几像素
  // （实测见 `scene/mapSize.ts`）。**判据就是硬比区**：地图的右 / 下边缘（x ≥ 1016、
  // y ≥ 632 那一圈，以及整张底图）都在缺口区之外，少一个像素就红。
  //
  // 三条都只剩右下角金币数的字形（2026-09-10，measureGaps，剧本自报缺省 every 25，容差 8）：
  // 每条 4 帧、一块、实测外接框一律 (940,617)-(993,630)，4/4 帧；区外 0 个。
  'mapshort-width': {
    status: 'gap',
    // 仙一教学楼一楼.png 1022×640（右边缘缺两列）。单帧最多 575。
    gaps: [coinDigits(575)],
    why: '只剩右下角金币数的字形；1022 宽的底图连右边缘在内逐像素相等',
    issue: 'xl-9bd.17',
  },
  'mapshort-both': {
    status: 'gap',
    // 仙二205夜 的 教室2.png 1023×639（右、下各缺一）。单帧最多 586。
    gaps: [coinDigits(586)],
    why: '只剩右下角金币数的字形；1023×639 的底图连右、下边缘在内逐像素相等',
    issue: 'xl-9bd.17',
  },
  // xl-03x.13：NPC 数据有毛病的 仙二205 —— 原版吞掉异常、只建出前 3 个 NPC，web 侧照此复刻。
  // 实测（2026-09-11，measureGaps --rects，剧本自报缺省 every 25，容差 8，76 帧）：
  // 三块之外 0 个超容差像素。地图、三个 NPC（打坐的文科男 的素材两端都缺、都不画）、
  // 太极老师那一格的空、主角走动、对话框的边框与名字牌本身，全部逐像素相等。
  // 区的矩形与 dorm-walk 同一对（样式 1 对话框），不是另量的。
  'npc-defect': {
    status: 'gap',
    gaps: [
      // 外接框 (940,617)-(993,630)，单帧最多 586，76/76 帧。
      coinDigits(586),
      {
        name: 'speech-name',
        // 外接框 (161,3)-(249,33)，单帧最多 1323，1/76 帧。
        maxPixels: 2646,
        rect: { x0: 0, y0: 0, x1: 271, y1: 40 },
        why: '名字牌滑入路径上的名字字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
      {
        name: 'speech-text',
        // 外接框 (273,3)-(710,105)，单帧最多 4966，3/76 帧。
        maxPixels: 9932,
        rect: { x0: 272, y0: 0, x1: 752, y1: 192 },
        why: '样式 1 对话框里的正文字形（原版字体未交付）',
        issue: 'xl-9bd.17',
      },
    ],
    why: '对话正文、名字牌与右下角金币数的字形（字体不在，基线也不同）',
    issue: 'xl-9bd.17',
  },
  'mapshort-height': {
    status: 'gap',
    // 基础实验楼乙五层.png 1024×639（下边缘缺一行；主线 脚本7 / 7+ / 26 同一张图）。单帧最多 493。
    gaps: [coinDigits(493)],
    why: '只剩右下角金币数的字形；639 高的底图连下边缘在内逐像素相等',
    issue: 'xl-9bd.17',
  },
}

/**
 * 存读档面板一个槽的三块缺口区（xl-i06.12）。三个槽**纵向恰好相隔 200**（原版
 * `paint()` 里的 `i*200`），实测三个槽的外接框也逐个差 200，所以矩形按槽号推、只量一次：
 *
 * - `lsThumb` —— 缩略图的绘制矩形本身，四边**不留余量**：再往外一像素就是底板（硬比区该管
 *   的东西）。实测外接框落在它里面（槽 0 逐边重合，槽 1/2 左沿 101）。
 * - `lsMapName` —— 地图名。上沿贴着缩略图的下沿（缺口区不许重叠），离实测最靠上的字形
 *   像素 y=203 只有 **3 px**；其余三边 6 px。右沿按最长的「大迷宫」三个字（159）留。
 * - `lsTask` —— 任务提示。四边 6 px（下沿 5 px：两条剧本里最低的字形像素在 523）；右沿按
 *   两条剧本里最长的那一句（663）留。
 *
 * 矩形共用、数不共用：`maxPixels` 是逐条剧本、逐个槽实测的「单帧最多」的 2 倍。
 */
function lsThumb(slot: number, single: number): GapRegion {
  const dy = slot * 200
  return {
    name: `thumb-${slot}`,
    maxPixels: single * 2,
    rect: { x0: 100, y0: 100 + dy, x1: 249, y1: 199 + dy },
    why: '缩略图：平台自带的缩放，两条循环按源图透明通道分岔，且这里的缩小倍率落在既有拟合区间之外（不在 M6 拟合）',
    issue: 'xl-cpo',
  }
}

function lsMapName(slot: number, single: number): GapRegion {
  const dy = slot * 200
  return {
    name: `map-name-${slot}`,
    maxPixels: single * 2,
    rect: { x0: 94, y0: 200 + dy, x1: 165, y1: 228 + dy },
    why: '地图名的字形（粗体 20 号，原版字体未交付）',
    issue: 'xl-9bd.17',
  }
}

function lsTask(slot: number, single: number): GapRegion {
  const dy = slot * 200
  return {
    name: `task-${slot}`,
    maxPixels: single * 2,
    rect: { x0: 394, y0: 97 + dy, x1: 669, y1: 128 + dy },
    why: '任务提示的字形（粗体 20 号，原版字体未交付）',
    issue: 'xl-9bd.17',
  }
}

/**
 * 磁盘上有哪些剧本。**分母从源头数**，不抄一份名单：别人往
 * `tools/traces/scripts/` 里加一条剧本，这里立刻就知道，而它没在 `EXPECTED`
 * 里表态就会红。名单抄两份，迟早对不上。
 */
export function scriptNames(): string[] {
  return readdirSync(repoPath('tools/traces/scripts'))
    .filter((f) => f.endsWith('.json'))
    .map((f) => f.replace(/\.json$/, ''))
    .sort()
}

export function expectationOf(name: string): Expectation {
  const e = EXPECTED[name]
  if (!e) {
    throw new Error(
      `剧本 ${name} 没有在 src/compare/expected.ts 里表态。` +
        `新剧本必须先说清楚它现在应该全过（match）还是应该差（gap，并写明差在哪、归哪张票）。`,
    )
  }
  return e
}
